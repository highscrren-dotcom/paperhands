# DECISIONS & HANDOFF — лог решений и точка входа в новую сессию

> **Читать первым** в новой сессии (вместе с [../MORNING-SUMMARY.md](../MORNING-SUMMARY.md)
> и [../CLAUDE.md](../CLAUDE.md)). Кратко: где мы, все решения владельца, проверенные
> факты, что делать дальше. Обновлять в конце каждой сессии.

## Session 24 (2026-07-30) — фаза D закрыта: координации нет, ранность — PLAUSIBLE

145. **👥 ТОП-20 АВТОРОВ ПО ЛИЧНЫМ КОНТРОЛЯМ (запрос Петра 30.07 22:30) — ответ
     `session24/MESSAGE_PETR_TOP20.md`, замер `authortop.py` (74 автора × досье).**
     Планка после №143: экспертность = личные перм-тесты, не пул. На каждого:
     p_dir (его стороны против жребия на его входах, 200 розыгрышей), p_time (его
     даты против случайных дат его сделок, 100), парный B&H, годы, волна.
     (а) **ГЛАВНЫЙ ФАКТ: p_dir ≤ 0.05 у 14 из 74 при ~4 шумовых — направленческий
     скилл персонален и реален; p_time ≤ 0.05 у 3 = уровень шума.** Эксперты ленты
     знают КУДА, но не КОГДА — финальное подтверждение №143 с другой стороны.
     (б) Верх списка: mrsignalll (единственный с двумя осями: 0.015/0.010, 345 сд.,
     5/5 лет в плюсе), CryptoSkullSignal (p_dir 0.005, лифт над лонгом t +2.70,
     3/3 года), InvestingScope, melikatrader94 (4/4 года, 325 сд.), tomas_jntx.
     (в) «Звёзды ранности» (coinpediamarkets, DHLawrence, bryandowningqln,
     jhonnybrah) не имеют НИ ОДНОЙ личной значимости — их плюс был бетой. Анти-лист
     подтверждён: salahuddin20041, Solldy (t_bh −4.11!), thetradinglord28.
     (г) Честность: 74×2 теста → 5-7 имён из 20 могут быть шумом; рекомендация
     Петру — Sweep по топ-5 (~1300 сделок суммарно) на его правиле.
     (д) Пункт 1 Петра (качать TG-каналы) принят: нужен список каналов и решение
     о доступе (Telethon-сессия или бот-админ) — паттерн стора с firstSeen готов.

144. **📡 СБОР ДАННЫХ ЗАПУЩЕН (владелец: «собирай данные», 30.07 ~12:00Z).** Полевой
     сбор TV-идей на CT105 сверх авторского: `agent/tools/tv-scrape-field.sh` +
     юнит `quant-tv-field.timer` (каждые 10 мин на :03, разведён с авторским :00).
     12 символов (BTC/ETH/SOL по 2 страницы, остальные по 1), сторы append-only в
     `/data/backtests/_agent/feed/stores/<SYM>.jsonl` — СЫРЬЁ без нормализации
     (перпы .P и чужие котировки остаются, маппинг при анализе). Сид 360 идей
     (firstSeen=сид, помечены общим значением), первый автономный тик проверен
     фактом (+0 новых / 48 обновлено / Finished). Диск ~180К на старте, копейки.
     Это фундамент под «первую новость в тишине» (№143г): через 2-3 мес будет лента
     с честными firstSeen/lastSeen/lagSec — плотность, удаления и задний ввод
     видны без бэкфил-смещений. Авторский скрейп (№142) оставлен параллельно.

143. **⛔ ВНЕШНЕЕ РЕВЬЮ ПЕТРА ОКАЗАЛОСЬ ПРАВО: ОТБОР ПО РАННОСТИ = БЕТА МЕСЯЦЕВ.
     Ветка закрыта по заранее объявленному гейту (`shiftsel.py`), ТРЕТИЙ смертельный
     гейт фазы D.** Ревью (30.07 16:21) предложило недостающий контроль: «случайные
     даты входа при том же исполнении» НА ОТОБРАННЫХ сделках. Сделано немедленно
     (гейт объявлен до прогона: p<=0.05 или ветку закрывать):
     • K=2/12 мес: настоящие входы +3.859 (271 сд. с полной кромкой) против медианы
       случайных дат +3.483 (5..95 пцт +1.7..+6.1), **p = 0.406**;
     • K=3: +1.830 против +2.014, **p = 0.574** — случайные даты даже лучше.
     Время постов не добавляет НИЧЕГО сверх экспозиции в удачные (месяц×символ×
     сторона) корзины. Согласуется с B&H-лифтом t=0.83 (№137) и «серферы середины»
     (№140).
     (а) **УРОК МЕТОДОЛОГИИ (в копилку к fmix32 и survivorship):** permsel (жребий
     по АВТОРАМ, p=0.005) пропустил бету, потому что рандомизировал не ту ось —
     другие авторы сидят в других корзинах месяцев. Контроль обязан рандомизировать
     ИМЕННО ту ось, на которой стоит утверждение («ранние знают КОГДА» → сдвигай
     ВРЕМЯ у тех же сделок). Отныне контроль случайных дат на отобранном сабсете —
     обязательная часть любого гейта отбора.
     (б) Полевой тайминг-сигнал фазы C (вход по посту против сдвига вперёд,
     p 0.02–0.05 на ВСЕЙ ленте) этим НЕ отменён — он живёт на поле, но собрать его
     отбором авторов не удалось (эхо вывода №134: «эдж в ленте есть, способ сбора —
     нет»).
     (в) **Рекомендация по стенду: paper-юнит паузнуть** (он теперь меряет бету, а
     не эдж; решение за владельцем/Петром — стенд ставился по их прямому указанию),
     **скрейпер с firstSeen ОСТАВИТЬ** — данные копятся и нужны при любом раскладе.
     «Больше авторов той же методологией» до нового сигнала бессмысленно:
     методология размножает бету.
     (г) Живое направление, согласованное с интерпретацией Петра («Грааль =
     новостная лента»): тестировать «первую новость в тишине» на ПОЛНОМ поле (все
     авторы, без отбора) — и с гейтом случайных дат С ПЕРВОГО дня. Плюс живой
     firstSeen как единственный несмещённый датчик.

142. **▶️ PAPER ЗАПУЩЕН НА CT105 30.07 ~09:16Z (владелец: «запускай на C105, у Петра
     тоже доступ»).** Стенд: `/data/backtests/_agent/paper/paperhands` (клон ветки,
     npm install 15 с), юниты `quant-paper-early.service` (paper BTCUSDT, --ui на
     0.0.0.0:60050, Restart=always, enabled) и `quant-tv-scrape.timer` (*/10 мин:
     ленты доверенных → сторы firstSeen → фид). Верифицировано фактом: журнал
     «trusted authors loaded [coinpediamarkets, DEXWireNews]», UI HTTP 200, dump
     пишется, первый скрейп 48 идей → 24 в фиде.
     (а) **КРИТИЧЕСКОЕ ОТКРЫТИЕ ПО ДОРОГЕ: окно 60 мес для live НЕПРИГОДНО** —
     чистая ранность длинного окна выводит в топ УШЕДШИХ с платформы: Solldy и
     Avramis молчат с конца 2023, drcrypto14 — с 02.2024. Walk-forward это скрывал
     (сделки были в 2022-2025), а paper молчал бы вечно. Переключено на ИСХОДНУЮ
     валидированную ячейку 12 мес / K=2 (p=0.005): допуск ≥10 идей/год сам
     гарантирует живых. Доверенные сейчас: coinpediamarkets (78 идей, 60 % ранних)
     + DEXWireNews (15, 60 %). Активити-фильтр поверх 60 мес = невалидированная
     гипотеза, в прод не тащить. Петру 60-мес спека уже отправлялась — ПОПРАВИТЬ
     в следующем письме.
     (б) Ожидание стенда: ~2-3 идеи/мес на BTCUSDT от этой пары; месяцы тишины —
     норма. Один инстанс из-за порта UI 60050 (headless умирает без --ui);
     ETH/SOL/XRP — после решения вопроса порта.
     (в) Первый сид фида: 48 идей обоих авторов получили firstSeen=сейчас
     (артефакт первого прогона, честный firstSeen пошёл со следующего тика
     таймера); старые не триггерят — окно свежести 30 мин.
     (г) Смена месяца: 1-го числа `scripts/trusted.mjs` по свежему пакету датасета
     + рестарт юнита. Данные Петра на CT105 не тронуты, всё в `_agent/paper/`.

141. **🚀 PAPER-СТЕНД ФАЗЫ D ПОСТРОЕН по запросу автора («пора делать paper», 30.07
     13:41), образец — его jun_2026.strategy.ts.**
     (а) **Стратегия `example/content/early_trust.strategy/`**: топ-3 по ранности из
     артефакта `assets/trusted.authors.json` (канон = fullearly.out, окно 60 мес),
     вход по посту доверенного в его сторону, холд 14 сут, lock 20 / trail 8 (порт
     семантики SIMULATE_TRADE_FN через listenActivePing), стопа нет —
     `setConfig(CC_MAX_STOPLOSS_DISTANCE_PERCENT=100)` СОЗНАТЕЛЬНО снимает защиту
     капитала, для live решение о стопе отдельное. Фид = `assets/tv-ideas.normalized.jsonl`,
     перечитывается раз в минуту (TTL) — скрейпер пишет снаружи. В paper-режиме
     триггер «свежая и невиданная идея» (окно 30 мин, seenIds), в backtest — точная
     минута, как в образце. Генератор артефакта `scripts/trusted.mjs` (1-го числа);
     PnL-фильтр запрещён кодом и README (№138).
     (б) **Смоук ФАКТОМ**: рамка 23–28.06.2026, подменный список (EARLY_TRUST_AUTHORS,
     доверенные в июне молчали) — вход по идее TradingShot 23.06 10:52 (минута после
     поста 10:51:01), SHORT 62290, дедуп/слот удержали 3 следующие идеи, закрытие
     time_expired 28.06 +3.23 %. Грабля: cwd движка = каталог стратегии, но путь
     entry в CLI — от `example/`.
     (в) **Скрейпер Петра поправлен** (`session24/tv-ideas.firstseen.mjs`, его
     tv-ideas.mjs + режим `--store`): firstSeen ставится при ПЕРВОМ появлении id и
     неприкосновенен, lastSeen обновляется (стоп роста = маркер удаления), исчезнувшие
     из выдачи остаются в сторе, lagSec = готовое распределение «сколько идей доезжает
     задним числом», запись атомарная. Самотест пройден. Рекомендованный крон
     */10 мин × 2 страницы. Отдать Петру вместе со стратегией.

140. **📊 ВОПРОС АВТОРА «лидеры или артефакт сигмы» (30.07 13:05) — ОТВЕЧЕН ЗАМЕРОМ
     `whereonwave.py`, ответ `session24/MESSAGE_PETR_WAVE.md`.** Положение постов на
     волне (back/fwd в направлении идеи, медианы, 10 167 идей): поле постит В КОНЦЕ
     хода (back14 +0.8/+1.1, fwd14 +0.2), отбор топ-2 по ранности — В СЕРЕДИНЕ
     (back14 **+5.8**, fwd14 **+3.1**). Т.е. не «начало ускорения» и не провидцы —
     стабильные серферы неисчерпанных движений; отличие от толпы в том, что ход
     после их постов ПРОДОЛЖАЕТСЯ. Против сигма-артефакта: walk-forward p=0.001
     (ярлык из прошлого окна → деньги следующего месяца), split-half r=+0.77, все
     годы 2022–2025 в плюсе. Топ неоднороден: drcrypto14 и TraderEngineering реально
     ДО движения (back14 −1.2/−1.1), DHLawrence — лотерейный профиль (медиана −0.8,
     среднее +4.7), Avramis — догоняльщик (back14 +10.6, впереди пусто) — его минус
     объяснён. Живые месяцы нужны не против случайности, а против survivorship-ленты
     и подгонки определения (№139).

139. **💬 АВТОР ОСПОРИЛ НУЖНОСТЬ firstSeen («порядок постов это поле ts», 30.07
     12:22) — ответ `session24/MESSAGE_PETR_TS_VS_FIRSTSEEN.md`.** Про порядок он
     прав (всё и так считается по ts, формулировка в письме была кривая), но ts не
     видит двух вещей, обе уже пойманы в этом датасете (факты сессии 21, №[памятка
     sweep-plan-analysis]): (1) удаления — выдача TV ~1100–1200 идей на символ,
     старше = хвост выживших (BTC: 167–235 авторов в полном окне против ≤40 в
     старых месяцах; jun_2026 BTC = 463 идеи в свежем снапшоте против 421 в старом);
     (2) задний ввод — jul_2026 склеен из двух снапшотов 19.07, отличить «виден
     сразу» от «доехал через сутки» нечем (класс бага = Tavily-полночь). Для
     ранности это не косметика: плотность в старых месяцах считана по прореженной
     в 4–5 раз ленте. Вывод письма прежний: firstSeen на лету.

138. **📬 ЗАПРОС АВТОРА (30.07 12:05): полный список доверенных по ранности, окно
     60 мес — ВЫПОЛНЕН, ответ `session24/MESSAGE_PETR_TRUSTED.md`.** Замеры
     `fullearly.py` (полная таблица 74 авторов) + `permearly.py` с окном обучения
     60 мес (спека автора):
     (а) **окно 60 мес СИЛЬНЕЕ 12-мес:** K=2 +3.464 p=0.001, K=3 +3.388 p=0.001
     (жребий, 1000 розыгрышей), «только ранние» p 0.001–0.002, по годам минусов нет
     (2022 +0.4, 2023 +3.1, 2024 +7.0, 2025 +3.6; в 2026 сделок нет — топ окна не
     постил). Претензия «2025 в минусе» из №137 относится к 12-мес окну.
     (б) **ЛОВУШКА ДЛЯ ПРОДА, проверена до отправки: фильтр «PnL>0 в окне» поверх
     ранности УБИВАЕТ сигнал** — K=2 p=0.242, лифт над лонгом −1.01 (t −1.44). Это
     тот же рейтинг по доходности, что не работал в фазе C. В ответе Петру сказано
     прямо: чистая ранность, механически, без PnL-фильтра.
     (в) Доверенные на aug_2026 по правилу K=2: **Avramis, Solldy** (K=3 +drcrypto14)
     — оба исторически убыточны, и это НЕ противоречит (а): walk-forward платит на
     ротации топа, а не на этих двоих персонально. Развязка — только живой firstSeen.
     (г) B&H-лифт и на 60-мес окне незначим (+0.44, t +0.37) — оговорка №137 в силе:
     эдж отбора в большой мере лонг-волна, не сторона.

137. **🎯 ФАЗА D ЗАКРЫТА ЦЕЛИКОМ ЗА СЕССИЮ. Отчёт
     [notes/tradingview-dataset/session24/REPORT_PHASE_D.md](notes/tradingview-dataset/session24/REPORT_PHASE_D.md),
     письмо автору — `session24/MESSAGE_PETR_PHASE_D.md` (не отправлено, ждёт «ок»).**
     Все гейты объявлялись до прогонов (№136), все сработали:
     (а) **Контроли отбор по ранности НЕ убили — впервые за 4 фазы.** `permearly.py`,
     два нуля по 1000 жеребьёвок: K=2 даёт +3.060 %/сделку против жребия p = 0.005;
     вариант «только ранние идеи» +4.286 против раннего поля +0.644, p = 0.001. Низ по
     ранности не хуже жребия — рейтинг видит ХОРОШИХ (зеркало фазы C, где рейтинг по
     PnL видел только плохих). НО статус PLAUSIBLE, не CONFIRMED: значимость сидит в
     заранее заявленной ячейке «≤2 чужих / 24 ч» (соседние ≤1/≤3/окно 6 ч — тот же знак,
     p 0.03–0.60), 2025 год в минусе (−1.37), лифт над теми же входами в LONG +0.881
     при t = +0.83 незначим. **В рецепт не вносить.** Решающая проверка — живой скрейп
     с per-idea firstSeen: 2–3 месяца накоплений дадут OOS, которого при подгонке не
     существовало.
     (б) **Вход по началу кластера — координации в торгуемом смысле НЕТ** (главный тест
     гипотезы автора, `clusterentry.py`, сетка 3/5/8 постов × 6/24 ч). Начало кластера ≈
     вход по посту (+0.04 в лучшей ячейке); плацебо по времени проходит ВЕЗДЕ
     (p 0.48–0.75 на 100 общих сдвигах тикеро-месяца); кластеры ЧУЖИХ символов платят
     больше своих (+1.7…+2.5) — «кластерность» ловит общий дрейф, не монету; b&h от
     начала кластера убыточен во всех ячейках. Ветка закрыта по гейту.
     (в) **Фильтр «поздний вход» рецепт портит** (`latefilter.py`, поминутный порт
     правила, базлайн воспроизвёл фазу C: 264 сд., +1.493, t +2.28; сверка с движком
     174 ячейки). Пороги по распределению (P50=3, P67=4): лифт падает до +1.137 / +1.031.
     Стоп-условие №136(в) сработало — в рецепт не вносить.
     (г) **Ботов в данных не видно.** Заголовки внутри кластеров обычные (шаблонных
     4.6 % при нуле 3.2 %, метод автора — косинус на TF char-3-граммах, т.к. Ollama на
     CT101 остановлен — там теперь vLLM Qwen3.5-9B без эмбеддингов); единичные пары-клоны
     (Apex_Legends↔XAUxBTC_Pro и др.) — карлики, не сеть. Совстречаемость есть (24 пары
     p≤0.01 против ~6 шумовых; BitCoinGuide перед 6 авторами), но неотличима от общей
     реакции на одно движение — после (б) читается именно так.
     (д) Стенд: всё юнитами на CT105 (`quant-permearly/-latefilter/-clusterentry/
     -whoposts/-earlyrobust`), скрипты и выводы в `/data/backtests/_agent/phaseD/` +
     копии в `session24/`. Грабля сети: три WireGuard-профиля ноутбука с одним 10.8.0.2
     дрались за маршрут (мёртвые `rh`/`redoubt-home` перехватывали у живого
     `redoubt-full`) — сняты `nmcli connection down`; вернуть `nmcli connection up`.

## Session 23 (2026-07-28..29) — фаза C: холд 14 суток оказался оптимумом, а не потолком

136. **📊 ЭТАП 0 ФАЗЫ D ЗАКРЫТ ТУТ ЖЕ: всплеск перед постом ЕСТЬ, но связь с
     доходностью ОБРАТНАЯ.** Предпосылку гипотезы автора видно на голых временных метках,
     без единого вектора, поэтому она померена не откладывая (`precluster.py`, копия и
     вывод — `session23/`). 10 167 торгуемых идей, наивная модель фазы C, холд 14 сут,
     издержки 0.4 % на круг. Плотность ЧУЖИХ постов по тому же символу за 24 ч до идеи:
     0 постов → **+0.854** (1832 идеи), 1–2 → +0.314, 3–5 → −0.102, 6–10 → −0.261,
     11+ → **−0.380**. **Монотонно.** На окне 6 ч так же (+0.294 → −0.916 к плотности
     6–10), на 168 ч у 72 % идей плотность 11+ и они дают −0.393 против +1.025 у
     одиночных.
     (а) **Чем громче толпа уже отписалась, тем хуже последующий ход.** Это ровно та же
     картина, что дал сдвиг назад в фазе C, — к моменту поста движение уже случилось.
     (б) **Гипотезу автора это НЕ подтверждает и НЕ опровергает.** Координация и обычная
     реакция толпы на уже случившийся памп дают неотличимую плотность. Разделить их
     может только один тест, и он объявлен ДО прогонов: **кластер обязан предсказывать
     движение ВПЕРЁД от своего начала**, а не объяснять движение назад. Не бьёт вход
     по посту — координации в торгуемом смысле нет.
     (в) **Побочно выпал пригодный фильтр** — «не входить, если перед постом уже шумно».
     Он проверяется дешевле всего и первым: рецепт фазы C плюс порог по плотности, порог
     брать заранее по распределению, а не по результату. Стоп-условие: лифт не выше
     фазы C (+1.493 при t = +2.28) → фильтр не работает.
     (г) Порядок фазы D, контроли и стоп-условия — `session23/PLAN_PHASE_D.md`,
     точка входа новой сессии — `notes/tradingview-dataset/HANDOFF_PHASE_D.md`.
     (д) **АВТОР УТОЧНИЛ ВОПРОС (29.07 19:20): «должны быть паттерны: вот в эту первую
     половину пока толпа ещё не поперла — кто именно пишет».** Померено сразу
     (`whoisearly.py`, вывод — `session23/whoisearly.out`), и это самая сильная зацепка
     фазы D:
     • **ранность — УСТОЙЧИВОЕ свойство автора**, а не ярлык на шуме: корреляция доли
       ранних постов между первой и второй половиной его ленты **r = +0.765** на
       74 авторах с ≥20 идеями;
     • **отбор авторов ПО РАННОСТИ (в PnL не заглядывая вообще!), walk-forward по
       прошлым 12 мес:** K=1 → +2.071 (116 сделок), **K=2 → +3.060 (304 сделки)**,
       K=3 → +1.495, K=5 → +1.017, K=10 → −0.814. Отбор по PnL в фазе C давал на K=2
       +0.675 и был неотличим от жребия;
     • поле: ранних идей 4712 → +0.524 %/сделку, поздних 5455 → −0.191;
     • **`isScript` в датасете равен 0 у ВСЕХ постов** — как прокси «ботности»
       бесполезен; `authorIsPro` у ранних даже НИЖЕ (63.2 % против 68.5 %), то есть
       «сначала боты, потом Pro-автор» простой проверкой не подтверждается.
     ⚠️ **Не переоценивать до контролей.** Ранность определена через низкую плотность, а
     низкая плотность сама по себе даёт +0.524 — значит правильная база сравнения не
     общее поле (+0.311), а раннее поле, и лифт надо считать от него. Плюс K=10 уходит
     в минус, то есть эффект сидит в малых K — ровно тот узор, который в фазе C оказался
     подгоном. Перестановочные тесты (`permsel.py`: топ-K по ранности против случайных
     K из тех же допущенных) и парный buy & hold на этой выборке НЕ прогонялись. Это
     первое дело новой сессии.

135. **💬 АВТОР ПРОЧЁЛ РЕЗУЛЬТАТ ФАЗЫ C И ДАЛ НОВОЕ НАПРАВЛЕНИЕ — ФАЗА D.**
     Реакция на находку «вход за неделю ДО поста бьёт пост в 100 случаях из 100»
     (28.07 20:10): **«Это значит они загоняют толпу в монету… Нужно искать паттерны
     предшествующие публикации перформящих блогеров. Например, сначала постят боты,
     а потом сам автор чтобы обмануть алгоритм рекомендаций»**. И метод (29.07 12:39):
     **«нужно собрать посты в кластеры, например, по методу K ближайших векторов»**,
     ссылка — https://habr.com/ru/articles/801885/.
     (а) **Ссылка — обычный учебный разбор KNN** (sklearn, Iris/Diabetes, евклидова/
     манхэттенская/косинусная метрики, KD-Tree и BallTree). То есть автор указывает на
     МЕТОД группировки, а не на готовое решение под нашу задачу. Кластеризация постов по
     тексту — второй шаг, а не первый.
     (б) **У гипотезы есть предпосылка, которую видно на голых временных метках.** Если
     координация есть, перед постом перформящего автора должна стоять аномальная
     плотность ЧУЖИХ постов на том же символе. Это меряется по полю `ts` без единого
     вектора, и если всплеска нет — строить эмбеддинги незачем. Замер — №136.
     (в) **Что переиспользуется целиком:** объединённый склад свечей (12 символов,
     16.3 млн минуток, `netCalls = 0` на горизонте 27 суток), наивная модель доходности
     (две свечи на идею, сверена с движком построчно), перестановочная машинерия
     (`perm.py`, `permsel.py`, `permshift.py`) и раскладка прогона по памяти. Новое нужно
     только под кластеризацию.
     (г) **Данные под гипотезу есть в датасете:** у каждой идеи `id`, `ts`, `symbol`,
     `direction`, `author`, `authorIsPro`, `isScript`, **`title`** и `url`. Текст заголовка
     — единственное текстовое поле; тела идеи в датасете нет. Эмбеддинги можно считать
     локальным Ollama на CT101 (192.168.1.30:11434) — бесплатно и без внешних ключей.
     (д) **Главная ловушка направления, которую держать в уме с самого начала:** «боты
     постят перед автором» и «пост стоит в конце движения» — это ДВЕ РАЗНЫЕ гипотезы, и
     вторая уже подтверждена без первой. Обычная реакция толпы на уже случившийся памп
     даёт ровно ту же картину плотности, что и координация. Отличить их можно только
     контролем: кластер должен предсказывать движение ВПЕРЁД от начала кластера, а не
     объяснять движение назад. План — `session23/PLAN_PHASE_D.md`.

134. **🎯 ФАЗА C ЗАКРЫТА ЦЕЛИКОМ. Отчёт
     [notes/tradingview-dataset/session22/REPORT_PHASE_C.md](notes/tradingview-dataset/session22/REPORT_PHASE_C.md),
     письмо автору — `session22/MESSAGE_PETR_HOLD27.md`.**
     Ответ на запрос автора «увеличь холд до 27 дней»: **не надо, 14 суток — оптимум,
     а не потолок** (обоснование в пункте «з», оно перевернуло промежуточный вывод).
     Сырое поле (10 845 идей, 412 посчитанных тикеро-месяцев из 415, отбора нет): на
     сделку 14 сут → +0.334, 18 → +0.626, 21 → +0.606, 24 → +0.691, 27 → +0.778, но НА
     СУТКИ удержания 0.0238 / **0.0348** / 0.0289 / 0.0288 / 0.0288 — рост суммы это
     просто более долгое сидение в рынке, t везде около 2.
     (а) **Гейт приёмки раздвоился, и это главный результат.** На сырой ленте гейт
     ПРОХОДИТ: лифт над теми же сделками в LONG положителен на всех холдах (+0.088…+0.310),
     плацебо не проходит ни одно. На рецепте (отбор топ-K) НЕ проходит: лифт уходит в минус
     (−0.163 на 27 сут, парный t = −0.12), перестановка направления проходит свободно.
     (б) **Направление автора значимо лучше жребия.** Точный перестановочный тест (в углу
     сетки набор сделок не зависит от стороны, поэтому перестановка меток точная): p =
     0.017 / 0.004 / 0.012 / 0.009 / 0.005 по холдам. Случайная расстановка тех же 72/28
     убыточна везде.
     (в) **Момент поста — лучший из ДОСТУПНЫХ входов, но ход уже случился.** Сдвиг всего
     тикеро-месяца общим смещением, 100 розыгрышей, кромка вырезана у всех одинаково.
     ВПЕРЁД (единственный торгуемый): вход по посту +0.28…+0.78 против медианы сдвига
     +0.01…+0.19, p 0.02–0.13 — ждать нельзя. НАЗАД (заглядывание вперёд, только
     диагностика): **100 розыгрышей из 100 бьют настоящий вход**, медиана +1.73…+4.11.
     Пост стоит в конце движения. ⚠️ Первая редакция контроля сдвигала КАЖДУЮ идею на свой
     срок — это ломает кучность постов и раздувает число сделок на 4 %, выводы получались
     обратные. Сопоставимая форма — общий сдвиг на пакет.
     (г) **ПРАВКА РЕЦЕПТА: рейтинг видит плохих авторов и не видит хороших.** Прямой тест
     против жребия (400 жеребьёвок, K случайных из тех же допущенных): p топ-K = 0.11–0.86
     по всем холдам и K = 1,2,3,5,10 — значимости НЕТ НИГДЕ; p худших K = 0.002–0.045 —
     значимо хуже жребия почти везде. Отсюда: **чёрный список вместо белого**, «все
     допущенные минус худшая тройка» даёт 2860 сделок против 272 у топ-2 и на сделку не
     хуже (+0.572 против +0.528 на 14 сут, +1.841 против +1.548 на 27).
     (д) **Контроли считаются ВНЕ движка и сверены с ним построчно.** В углу «лок 0 /
     трейл 100 / стоп 99» выход всегда по концу холда → нужны две свечи на идею, а не
     38 880. Угол включён в сетку намеренно ради сверки: на `may_2022 BTCUSDT` все 40 строк
     питона совпали с движком **до последнего знака** (`diff`, не заявление). Иначе четыре
     контроля стоили бы ещё четырежды по 17 часов.
     (е) **Склад свечей достроен, гейт `netCalls = 0` пройден.** Поминутная проверка нашла
     2 190 115 дыр против 1413 суток посуточной разведки (сутки числились «есть» по одной
     минутке); докачано 1 932 098 минуток за 2024 запроса. Осталось недоступным 258 017 и
     это не недоработка: HYPE делистнут с Binance spot (`Invalid symbol`, выкинут целиком —
     47 идей), PUMP старше листинга, и **7 окон техобслуживания Binance** по 79–284 минуты.
     Месяц с окном даёт `net=25` и не рвётся: движок ловит неполный чанк как конец истории.
     (ж) **Замер: 23.0 с и 12.5 МБ кучи на идею** (36 идей × 840 точек × горизонт 38 880 =
     827 с, пик RSS 541 МБ). Шарды разложены по ПАМЯТИ, а не по времени: пик шарда = его
     самая крупная задача, поэтому два самых тяжёлых тикеро-месяца (449 и 344 идеи) лежат
     в ОДНОМ шарде и не могут считаться одновременно; сумма пиков ~12.1 ГБ из 16.
     Пятью воркерами на квоте 4 ядра выходит 27–28 с на идею, прогон ~17 ч, узкое место —
     процессор (разбор поминутных JSON), не диск.
     (з) **ПРОГОН ЗАКРЫЛ ВОПРОС, И РЕКОМЕНДАЦИЯ ПО ХОЛДУ ПЕРЕВЕРНУЛАСЬ: 14 суток —
     ОПТИМУМ, а не «пока не проверили дальше».** 840 точек × 412 тикеро-месяцев за 18 ч,
     3 нерасчётных (PUMP до листинга, 5 идей). Парный бета-контроль на НАСТОЯЩЕМ правиле
     рецепта (лок 20 / трейл 8 / стопа нет, топ-2): холд 14 — лифт **+1.493 %/сделку,
     парный t = +2.28** на 264 сделках; холд 18 — +0.059 (t 0.08); холд 27 — +0.812
     (t 0.86). То есть эдж над buy & hold есть ТОЛЬКО на 14 сутках. Сетка движка говорит
     то же своим счётом: средний лифт отбора +0.258 на 14 сутках и минус на всех
     остальных (18 −0.445, 21 −0.227, 24 −0.386, 27 −0.170). Оговорка: холдов проверено
     пять, с поправкой на множественность порог был бы t ≈ 2.6, поэтому «14 лучше
     остальных» — твёрдо (сходится у трёх замеров), «14 значимо бьёт buy & hold» — на
     границе.
     (и) **ПРАВКА К №133: риск-менеджмент НЕ бесполезен.** Фаза B заключила «работает
     направленная экспозиция, правила ничего не добавляют» — это верно для суммарного PnL
     (лучшая точка снова села в выключенный угол лок 0 / трейл 100 / стоп 99, +3233 % на
     холде 27), но НЕВЕРНО для эджа над рынком: в углу лифт над buy & hold на 14 сутках
     +0.099 при t = 0.50, а на паре «лок 20 + трейл 8» — +1.493 при t = 2.28. Замок с
     трейлингом и делает эдж.
     (к) **Отбор авторов на длинном холде разваливается.** Против фазы B на том же
     протоколе: лифт отбора > 0 у 361 точки из 840 (43 %) против 9544 из 11520 (83 %);
     в плюсе ≥4 лет из 5 — 78 против 4032; все 5 лет — 7 против 1181; лучшая точка топ-10
     даёт 2 года из 5 против 5 из 5.

## Session 22 (2026-07-27..28) — фаза B: убыточность делал СТОП, а не сигнал; синхрон 17.6.0

133. **🎯 ФАЗА B ЗАКРЫЛА ВОПРОС: стенкой была ось стопа. Отчёт
     [notes/tradingview-dataset/session22/REPORT_PHASE_B.md](notes/tradingview-dataset/session22/REPORT_PHASE_B.md),
     письмо автору — `MESSAGE_PETR_FINAL.md`.**
     11 520 точек (лок 16 × трейл 15 × стоп 12 × холд 4) × 440 тикеро-месяцев, тейкерские
     издержки 0.1/0.1, 440/440 без ошибок.
     (а) **В плюсе 711 точек из 11 520**, лучшая **+1690 %** — при том что на сетке Петра
     (стоп ≤ 10) прибыльных 0 из 21 280, и в фазе A 0 из 450. Ось стопа монотонна к «стопа
     нет»: 6 → −1772, 10 → −1946, 16 → −1463, 40 → −1306, **99 → −1097**. Замок фиксирует
     небольшой плюс, стоп режет крупный минус — при hitRate 0.66 это отрицательное
     матожидание при любых настройках.
     (б) **Проверка нуля сработала.** Выключенные уровни (лок 0, трейл 100, стоп 99)
     закладывались намеренно: если максимум сядет туда — риск-менеджмент не даёт ничего.
     **Он туда и сел**, 98 % выходов по истечении холда. Эдж = направленная экспозиция на
     14 суток, а не правила управления.
     (в) **НО просадка меняет вывод.** Сумма pnlPercent — не доходность (каждая сделка =
     ставка фиксированного размера). Наивный угол: +1690 % при просадке **−2818 %**
     (отношение 0.60), winRate 0.49, приб. месяцев 30/67. Точка **лок 20 / трейл 8 /
     стоп 99**: +1568 % при просадке **−1573 %** (отношение **1.00**), winRate 0.58,
     35/67. Значит: **не нужен СТОП-ЛОСС**, а замок с трейлингом при отсутствии стопа
     сильно улучшает риск почти без потери доходности. winRate 0.49-0.58 при
     положительном матожидании = выигрыши крупнее проигрышей, обратно исходной
     конструкции.
     (г) **Отбор авторов держится:** в плюсе на сделку 6648 точек из 11 520, лифт > 0 на
     9544. Лучшая — лок 20 / трейл 14 / стоп 20 / холд 10 сут: +1.552 %/сделку,
     +581.9 % за 52 мес, 5/5 лет.
     (д) **Сверка с фазой A сошлась:** её лучшая точка (лок 8/трейл 8/стоп 10/холд 14)
     даёт здесь −480 против −490, независимый прогон, расхождение в пределах округления.
     (е) **Единственный неисчерпанный край — холд:** 7 сут → −2242, 10 → −2002,
     12 → −1402, **14 → −924**. Растёт и дальше, но 14 суток = потолок кэша (горизонт
     профиля = `max(holdMinutes)`), прогон за 14 суток потребует докачки свечей. Лок и
     трейл исчерпаны — внутренние оптимумы ≈ 20 и ≈ 7.
     (ж) **Оговорки:** просадка огромная (прибыль/просадка ≈ 1.0 за 5.5 года), прибыльных
     месяцев меньше половины, отсутствие стопа реализуемо только на споте без плеча, всё
     на бэкфиле (два `firstSeen`, оба 19.07.2026), множественность 11 520 точек.

## Session 22 (2026-07-27) — фаза A: край сетки пройден, находка устояла, числа с21 поправлены

132. **🔄 СИНХРОН С АВТОРОМ: 17.1.0 → 17.6.0, БЕЗ ПЕРЕПИСЫВАНИЯ ИСТОРИИ; live
     перезапущен, ребут больше ничего не ломает.**
     (а) **17.6.0 ЛОМАЕТ API: `Simulator` полностью удалён, заменён на `Sweep`.**
     В `types.d.ts` upstream слово `Simulator` встречается **0 раз**, `Sweep` — 147:
     `addSimulatorSchema`→`addSweepSchema`, `ISimulatorGridAxes`→`ISweepGridAxes`,
     `SimulatorValidationService`→`SweepValidationService`, тесты переименованы,
     добавлен `packages/mcp` (PR #78). Наш `run_edge.mjs` и оба письма Петру написаны
     под 17.5.0 — **перед отправкой переписать под `Sweep`, сверяясь с фактическим
     `types.d.ts`, а не по памяти**. Прогон фазы B это не задевает: на CT105 в песочнице
     Петра стоит 17.5.0.
     (б) **Ребейз 169 наших коммитов на upstream/master — БЕЗ КОНФЛИКТОВ.** Работает
     ровно потому, что ядро автора мы не трогаем: `git log ... -- src/ types.d.ts test/`
     по нашим коммитам пуст. `agent/`, `CLAUDE.md`, пины `example/` (17.0.0) уцелели.
     **Гейт автора пройден точно по эталону: 1059 ok / 0 not ok** (мерить
     `grep -c '^not ok'`, код возврата всегда 255).
     (в) **origin приведён в порядок БЕЗ `push --force`.** Локальная
     `agent-night-20260706` была 179/176 относительно origin — это след прошлого
     ребейза (SHA переписаны, содержимое то же). Вместо форс-пуша заведена
     **`agent-night-20260728`** + страховочная `backup-presync-20260728`, старая ветка
     на origin не тронута. Поллер `/opt/quant/bin/autodeploy.sh` на CT102 переключён на
     новую ветку (оригинал в `.bak-20260728`), paper пересобран: `8863fb13→089bdaba`.
     (г) **live НЕ апгрейдили** и это осознанно: `@backtest-kit/cli` в npm выпущен только
     до **17.0.0** (библиотека — до 17.6.0), потолок апгрейда ниже текущей ломки API.
     Бот перезапущен по отмашке владельца при 0 сигналов / 0 отложенных; рестарт тремя
     часами ранее оказался не сбоем логики, а сетью (`getaddrinfo EAI_AGAIN
     fapi.binance.com` + таймаут телеграма → штатный self-heal). После рестарта 200 за
     17 мс, ошибок 0.
     (д) **Ребут больше не ломает стенд** (раньше ломал всё): частота закреплена
     `quant-cpu-thermal.service` на хосте (enabled), фаза B возобновляется
     `quant-phase-resume.service` внутри CT105 (enabled) — идемпотентно, по меткам
     `done_*.json`. Куча воркера в возобновлении **6144 МБ вместо 2560**: на сетке
     11 520 точек `jul_2026 BTCUSDT` (449 идей) в 2560 не влезает и падает с **rc=134**
     (SIGABRT V8, мимо наших обработчиков — файла ошибки не остаётся).
     (е) vibe-trading подтянут ff-merge на 26 коммитов (наших там 0). Остальные девять
     форков с upstream синхронны.


131. **🌡️ CT105 СЧИТАЕТ ПОД ТЕПЛОВЫМ ПОТОЛКОМ; КОНВЕЙЕР ПОД systemd.** Хост
     безвентиляторный (ARBOR FPC-790X, 35-Вт корпус на 65-Вт i7-7700 — записано
     в шапке `redoubt-temp-alert.sh`), и стоял в `governor=performance`: процессор
     прибит к 4 ГГц круглосуточно, простой 65–68 °C, под нагрузкой 88 °C при
     паспортных 80. Лечится двумя записями в sysfs — `powersave` + `max_perf_pct=70`
     (2.5–3.0 ГГц): простой 54–57 °C, полная наша нагрузка 64 °C. **Это настройка,
     а не костыль** — в корпус лезть не надо. Нагрузку резать не числом воркеров,
     а `pct set 105 -cpulimit N`. Поставлен ДЕЙСТВУЮЩИЙ сторож
     `/usr/local/bin/quant-phase-throttle.sh` (cron ежеминутно): не убивает прогон,
     а двигает квоту 1↔4 по коридору 66/74 °C — существующий `redoubt-temp-alert`
     только алертит в телегу на 85/95. Долгий счёт запускать
     `systemd-run --unit=... --collect` ВНУТРИ CT105 — переживает ssh, сессию и
     выключение ноута. Грабли: **`pct exec 105` сломан** (namespace без `/proc` и без
     `/data/backtests`, `df` при этом врёт из устаревшего `/etc/mtab`) — только
     `lxc-attach`; `pkill -f` внутри контейнера убивает сам себя (дважды обожглись) —
     гасить явными PID, сперва `worker.sh`, потом `node`; правку `worker.sh` на ходу
     не делать — bash дочитывает скрипт по смещению. Правки частоты живут до
     перезагрузки, юнитом не закреплены.

130. **📐 ФАЗА A ОТРАБОТАНА: лифт за краем сетки выходит на ПЛАТО, находка
     подтверждена; но стенкой оказалась сама сетка. Отчёт
     [notes/tradingview-dataset/session22/REPORT_PHASE_A.md](notes/tradingview-dataset/session22/REPORT_PHASE_A.md).**
     450 точек (лок 4–8, трейл 3–8, стоп 8–10, холд 12–14 сут) × 440 тикеро-месяцев,
     стоковый пакет 17.5.0: сетка через `gridAxes`, издержки через `setConfig`,
     `node_modules` НЕ правились. Вывод компактными панелями из `onAuthorsTrained` /
     `onGridPoint` — 92 МБ вместо терабайтов `result_reports.jsonl`. Ось `hold` вверх
     не двигали намеренно: горизонт профиля = `max(holdMinutes)`, поэтому 410 из 440
     прогонов прошли на кэше Петра, 30 докачали 159 чанков (дыры кэша: начало 2021,
     mar_2023, кромка jul_2026). Те же 5 нерасчётных тикеро-месяцев, что и у автора
     (идеи старше листинга PUMP/HYPE) → 440/440.
     (а) **ПРАВКА ЧИСЕЛ СЕССИИ 21: сделки и PnL были завышены ровно вдвое.** Якорное
     правило попало и в 3-й, и в 4-й проход (по 7137 сделок в каждом файле), а анализ
     склеивал все три. Верно **398 сделок / +223.9 %**, а не 824 / +431.6 %; погодовую
     таблицу `REPORT.md` делить на два. Лифт не пострадал (отношение): +0.720 → +0.765.
     Новый прогон совпал с очищенной старой панелью **до последней цифры** — это и
     проверка раннера, и локализация ошибки в склейке, а не в движке.
     (б) **Лифт за 5 % не падает.** Маргиналы: лок 5–8 = +0.47/+0.45/+0.35/+0.48,
     трейл 4–8 = +0.45/+0.48/+0.43/+0.33/+0.39; лок 4 (+0.24) и трейл 3 (+0.30) хуже —
     **край мешал снизу, а не сверху**. Максимум по локу размазан (на 5 в 25 профилях
     из 90, на 6 — 23, на 8 — 25). Лифт > 0 на **430 точках из 450**. За краем по
     трейлингу выбираются **95–96 % тех же авторов** — плато на одних людях.
     (в) **EDGE ЕСТЬ, но в отборе, а не в правиле** (ответ на прямой вопрос Петра
     27.07 18:12 «из отчётов не понять, есть edge или нет»). Правило само не
     зарабатывает НИГДЕ: 0 из 21 280 старых и 0 из 450 новых точек. Отбор топ-3:
     **в плюсе на 300 точках из 450 (67 %)**, медиана +0.141 %/сделку против поля
     −0.251; лучшая точка (лок 8/трейл 8/стоп 10/холд 14) = **+0.939 %/сделку,
     +375.7 % за 52 мес, 400 сделок, плюс во все 5 лет**, чистыми (0.1 % комиссия +
     0.1 % слиппедж) и вне выборки. Объём мизерный — ~7.4 сделки/мес.
     (г) **Сырая сетка — склон, максимум ВСЁ ЕЩЁ снаружи.** Лучшая новая точка −490 %
     против −1948 % у лучшей старой (вчетверо), рост монотонный по обеим осям и на
     кромке не выдыхается; максимум сидит в углу нового бокса в 10 срезах из 15.
     (д) **Комиссия пересчитывается аналитически ТОЧНО** — она не входит ни в один
     уровень выхода (уровни считаются от `entryFill`, туда входит только слиппедж),
     поэтому смена `CC_PERCENT_FEE` сдвигает PnL сделки ровно на `2·Δf`. Мейкерские
     0.02 %: старая сетка **0 из 21 280** в плюсе (лучшая −1948 → −674), наша
     **35 из 450** (лучшая −490 → **+579**). Прибыльная область существует только за
     границами осей Петра. Слиппедж так не пересчитать — он двигает сами уровни.
     (е) **A2 ОТРАБОТАНА (мейкер 0.02/0.05, 440/440, ошибок 0) — ИЗДЕРЖКИ И БЫЛИ
     ПРИЧИНОЙ УБЫТОЧНОСТИ.** В плюсе **132 точки из 450** (тейкер: 0), лучшая
     **+1371 %** (лок 8/трейл 8/стоп 10/холд 14, 6673 сделки), медиана −583 % против
     −2279 %. **Поле выходит в плюс** — средний НЕотобранный автор на локе 8 даёт
     +0.087 %/сделку (при тейкерских −0.25 везде), то есть правило зарабатывает само,
     без отбора. Отбор сохраняется сверху: в плюсе 418 точек из 450, лучшая +1.096 %
     на сделку / +408.9 % за 49 мес / 5 из 5 лет. Слиппедж важнее комиссии: аналитика
     по одной комиссии давала +579 %, полный прогон +1371 % — он двигает сами уровни
     выхода, аналитически не пересчитывается. ⚠️ Мейкерский вход лимиткой НЕ
     гарантирован — это оценка запаса, не доказательство прибыльности. Поверхность
     СНОВА упирается в край (лок 4 → −876, лок 8 → +159; трейл 3 → −1069, трейл 8 → +85).
     (ж) **ЗАМЕР: размер сетки почти бесплатен, время держит ЧИСЛО ИДЕЙ.** На одном
     тикеро-месяце (may_2022 BTC, 36 идей): 1 точка 449 с, 245 точек 446 с, 2475 точек
     477 с, 9900 точек 571 с при пике RSS 418 МБ. Всё время — построение профилей
     (чтение свечей поминутными JSON-файлами, 36 идей × 20160 мин ≈ 726 тыс. файлов);
     перебор точек это арифметика в памяти, память растёт ~195 Б на сделку. Ось `hold`
     ВНИЗ бесплатна (горизонт = `max(holdMinutes)`). Поэтому фаза B поставлена не на
     245 точек, а на **11 520**: лок 16 × трейл 15 × стоп 12 × холд 4, с выключенными
     уровнями (лок 0, трейл 100, стоп 99) — если максимум сядет в тот угол, значит
     риск-менеджмент не добавляет ничего и весь эффект чистая направленная экспозиция.
     Конвейер — см. №131. Черновики Петру: `MESSAGE_PETR.md`, `MESSAGE_PETR_EDGE.md`.

## Session 20 (2026-07-24) — запуск роевого эксперимента по PLAN-petr

129. **🕯️ ЖИВОЙ СВЕЧНОЙ СТОР ПОСТРОЕН И НА КРОНЕ (решение владельца 25.07:
     «скрипт, раз в день, докачай пробелы»)** — cfy:/opt/quant/data/candles/,
     формат датасета, BTC/ETH/SOL/DOGE. (а) candles-update.py (stdlib
     python3, cron.d/quant-candles 05:10Z, лог candles-update.log):
     append-only продление кромки, только закрытые минуты, flock,
     идемпотентен; первый запуск докачал 19.07→25.07 (+8244×4). (б) Аудит
     ПРАВИЛЬНЫМ методом (отсутствующие сутки, не «неполные» — первый аудит
     №128-дописки этим грешил и дыры ПРОГЛЯДЕЛ) вскрыл дыры краула в
     датасете Петра: SOL-2025 — 22 дня (фев-июнь), DOGE — 2 дня 2025 (6 и
     31.12 — оба в Q4 верификации!) + 16 дней 2026, ETH-2025 — 1 день; BTC
     чист; все дыры — целые сутки (посуточный краул), паттерн Петру полезен
     как фидбек (PS в DRAFT-petr-2). (в) fill-gaps.py залатал всё (биржа
     отдала 100% — пауз торгов не было), стор непрерывен: 821136 минуток на
     тикер, 0 разрывов, проверено. (г) Пересчёт verify на полных свечах:
     ETH/SOL — бит-в-бит (дыры вне Q4, SOL 61.9% ПОДТВЕРЖДЁН), DOGE
     51.7→52.6% (n 1187→1259) — FAIL остался FAIL, выводы №128 не
     изменились. (д) Этапы 2-3 датасетных дыр не видели (движок качает сам).
     hourly теперь собирать из data/candles-live/ (rsync стора), не из
     датасета. Инвентарь и команды — README стенда.

128. **🎲 РАСШИРЕНИЕ п.9 ОТРАБОТАНО (ETH/SOL/DOGE, по отмашке владельца
     «гонять монетки») — как класс рой не подтверждён, но SOL дал
     единственный двойной плюс; разбор
     [findings/2025-tickers/](../swarm/findings/2025-tickers/FINDINGS.md).**
     Та же точка (close, БЕЗ перекалибровки — единое правило), тест-окно
     зеркально BTC (апр-дек, объявлено до прогона), шаблоны параметризованы
     символом из фида (ассерт одно-символьности), раннер SWARM_ROOT
     (ff17a463). Гейт данных прошли все (734/61, 802/261, 760/237; backfill
     100% везде). Числа (WF-сумма | мес+ | b&h | гейт Q4): BTC −28.4 | 2/9 |
     +10.0 | 55.0 PASS(=aS); **ETH −29.0 | 4/9 | +69.5 | 43.4 FAIL (хуже
     монетки, LONG-антисигнал)**; **SOL +34.2 | 5/9 | +7.6 | 61.9 PASS —
     двусторонний лифт +10.5/+11.2пп, НЕ наклон (aS 57.3)**; DOGE −40.1 |
     2/9 | −18.4 | 51.7 FAIL (лента 84% LONG в −60% году). Скепсис к SOL:
     рой = фактически CobraVanguard (5/6, допущенных 4), FLAT 54% часов,
     shift-null 33–69 накрывает, survivorship-риск размазанной ленты
     максимален. Двусторонний лифт над базами повторился на 3/4 тикеров —
     слабый тайминг-сигнал, не переживающий стат-контроль и фрикшн (кроме
     SOL). Проверяемый прогноз живого сегмента: CobraVanguard должен
     продолжить бить базы. Порог months≥3∧rate≥0.5 вырождается на всех
     тикерах. Механика: 63 месячных прогона цепочкой ~1.5ч, ноль сбоев;
     кеши ~2G/тикер на cfy. Драфт-2 Петру:
     findings/2025-tickers/DRAFT-petr-2.md (отправка за владельцем).
     ⚠️ ЭКСПЕРИМЕНТ НЕ ЗАКРЫТ (решение владельца 25.07): открытые нити —
     вахта CobraVanguard на живом сегменте, ответ Петра про порог допуска,
     вариации; стенд/кеши сохранены. Свечной аудит 25.07: база = 1m
     (датасет 313 символов до 2026-07-19 12:11Z, дыр в 2025-26 по четвёрке
     нет + движковый кеш на cfy); 5m/15m/30m/1h отдельно НЕ качать —
     агрегируются из 1m (build_hourly — образец); единственная будущая
     потребность — продление кромки после 19.07 к запуску вахты
     (ccxt-фетчер или апдейт датасета Петром). Инвентарь — в README стенда.

127. **🐝 РОЕВОЙ ЭКСПЕРИМЕНТ ОТРАБОТАН ЦЕЛИКОМ ЗА ДЕНЬ — гейт Петра пройден
     буквально, но торгового преимущества НЕТ; полный разбор
     [agent/swarm/findings/2025-btc/FINDINGS.md](../swarm/findings/2025-btc/FINDINGS.md).**
     Механика 17.0.0 безупречна: ~370 изолированных Simulator.run (44 мин год)
     + 9 Simulator.test + 3 калибровочных теста — ноль падений. Числа:
     (а) этап 3 walk-forward апр-дек: **2/9 месяцев в плюс, −28.4% vs b&h
     +10.0%** — OVERFIT по тройному OOS-гейту; апр +15.4 (b&h +14.1) и ноя
     +2.97 (b&h −17.6!) — но дек −18.8 при широчайшем допуске; (б) этап 6
     Q4-хвост (рейтинг янв-сен, 2208 час-снапшотов): **гейт Петра ПРОЙДЕН —
     55.0% > CI монетки 52.1%**, НО совокупность = always-SHORT хвоста
     (54.98%); разбивка двусторонняя (+3.8пп LONG / +6.6пп SHORT над базами) —
     внутри разброса shift-null 43.8–58.5 → не значимо; «грязный» контроль
     (рейтинг видел Q4) 54.66% — лика нет; (в) допуск вырождается: allowed
     10→32 из ~57 (база прибыльного месяца ~50% → months≥3 ∧ rate≥0.5 со
     временем пропускает почти всех); (г) знакомцы №121 в допуске: пермабык
     InvestingScope 3/4, BitCoinGuide 4/7 — помесячный PnL пермабыков в
     зелёные месяцы пропускает. Кавета над всем: 100% фида = бэкфилл.
     Итог: протокол работает, TV-предмет на BTC снова пуст (созвучно №121,
     теперь и в формате Петра). Честная проверка — живой сегмент скрейпера
     (~июнь 2026+). ETH/SOL/DOGE — за владельцем, ожидания скромные.
     Драфт Петру: findings/2025-btc/DRAFT-petr.md (отправка за владельцем).
     Стенд: контейнер убран, кеш свечей 2.1G оставлен под возможное
     расширение, дампы на cfy:/opt/quant/data/swarm.

126. **🎯 ЭТАП 1 ЗАКРЫТ: authorMetric = "close" (калибровка янв-мар 2025).**
     Правило выбора объявлено ДО прогона (план session 20): (а) санити по
     стабильности winners месяц-к-месяцу; (б) из уцелевших — max
     totalPnlPercent OOS-марта (трек янв+фев, Simulator.test), tie 0.1пп →
     sharpe. Прогон: SWARM_METRICS="close,pnl,trail" одним контейнером
     (node:24, /opt/quant/data/swarm, 9.5 мин на 3 месяца, кеш свечей 194M).
     Числа: winners close 11/6/5 (разброс 6, ср 7.3, ок), pnl 12/8/7 (5/9.0,
     ок), trail 9/3/4 (разброс 6 > ср 5.3 — ВЫБЫЛ, played рухнул 12→4).
     OOS-март: close −7.49% (sharpe −1.02, 7тр, allowed 4) > pnl −18.95%
     (−1.71, 18тр, allowed 11); trail справочно −2.40% (вне решения). Все
     кандидаты в минус (март медвежий) — выбор относительный, законен.
     Совпало с наблюдением Петра «close в июньских выстрелах», но теперь
     выбрано экспериментально, как он просил. point.json зафиксирован,
     янв-мар для walk-forward теста СГОРЕЛИ. Инструменты стенда:
     run_months.sh (чекпойнт-раннер), calibrate_report/calibrate_test,
     swarm_rank + опционный срез месяцев (нужен этапу 6: рейтинг голосующих
     не должен видеть хвост верификации; дефолт 0..11 = канон не тронут).
     Утренний чек session 20: цепочка 04:40Z чистая (classify ok=33 failed=0,
     mongo-sync 05:21Z upserted=107), live плоский ($94.33, 0 ордеров,
     MTProto-блип 11:50Z самовосстановился за 1с), paper на 350a8d26/17.0.0
     healthy (полная обкатка цепочки — завтра 04:40Z); release-watch снят из
     крона (отработал: kit 17.0.0 11:40Z, cli 12:00Z).

## Session 19 (2026-07-23..24) — №121 закрыл TV-ветку; поворот: роевой стенд по PLAN Петра

125. **🚀 17.0.0 ВЫШЕЛ И РАЗВЁРНУТ (research); live ОСТАВЛЕН на 16.5.0 —
     решение подтверждено Петром.** Его ответы 24.07: «17.0.0 меняет только
     симулятор, адаптер и бектест остаётся как есть», «более не меняю, можешь
     работать»; на вопрос «файл в проде?» отвечено (в проде наш 5695e38 =
     его v2 + market(symbol) + minNotional 5; проверка типов: брокерный
     контракт 16.5.0≡17.0.0 байт-в-байт — наш адаптер ляжет на 17 как есть).
     Почему live не катим: релизу ноль строк пользы для торговли, мажор без
     истории эксплуатации, цена рестарта без причины (№122-123 паттерны).
     СДЕЛАНО: (а) paperhands смержен с upstream (2f11a25b: PR#76 simulator,
     новые demo, example на 17.0.0 локстепом), пуш → paper автопересобран на
     17.0.0 = первая обкатка релиза (безденежный контур, откат = revert);
     (б) шаблоны стенда финализированы по новым demo (a6641d78): корзины
     reports.<metric>, SWARM_METRICS-калибровка одним прогоном, нюанс §6 —
     при lock=0 reach/retain исключаются, калибруемы close/pnl/trail (все
     пять = вопрос Петру); npm-смоуки 17.0.0 в example и agent/swarm пройдены;
     (в) crontab-форк смержен и запушен (639a7dd) — ⚠️ следующий РУЧНОЙ деплой
     live поедет на 17.0.0, это отдельное решение владельца; (г) остальные
     4 форка синканы и запушены без конфликтов; (д) Пётр просил закинуть ему
     боевой live.module — файл: crontab-форк content/jan_2026.strategy/
     modules/live.module.ts@5695e38, отправка за владельцем. Эксперимент —
     ОТДЕЛЬНОЙ СЕССИЕЙ по README-чеклисту стенда (промт выдан владельцу).

124. **🐝 РОЕВОЙ СТЕНД: ФУНДАМЕНТ ПОСТРОЕН, этапы 1-3 ждут релиза 17.0.0.**
     Пётр ответил на наш стенд-дизайн двумя файлами (24.07): BREAKING.md
     (ветка simulator → 17.0.0: движок оценивает букашку СТРОГО изолированно,
     всё межавторское вырезано — aligned/weight/wilson/banCriteria; 5 метрик
     close/reach/retain/pnl/trail; окно грейдинга = холд точки, IDEA_TRIM_DAYS
     нет; --tune = one-shot тест замороженного {point, authorStats}) и СВОИМ
     PLAN.md «рой букашек на 12 месяцах» — канон исполнения (наш прежний
     hourly-tilt дизайн свёрнут, его механика: изолированный помесячный PnL
     автора → трек = сыгранные/прибыльные МЕСЯЦЫ → допуск months≥3 ∧ rate≥0.5 →
     вес голоса = wins, последняя рекомендация в окне holdMinutes → верификация
     почасовыми снапшотами против цены). «Дождись пока опубликую 17.0.0» —
     блокер; «Торговлю это не затронет» — live не касается. СДЕЛАНО (фундамент,
     [agent/swarm/](../swarm/README.md)): этап 0 целиком (build_feed: BTC-2025
     канонизацией №101, 1683 идеи/62 автора/69% направленных LONG;
     split_months: 12 папок янв_2025..декабрь_2025 канона, ассерт сумм прошёл);
     point.json (этап 1, authorMetric=null до калибровки янв-мар); юзерспейс
     этапов 4-6 готов и проверен (lib_track/lib_vote/swarm_rank/swarm_vote/
     verify_verdicts; смоук vote + ручная python-сверка 1:1, NEUTRAL-замолкание
     проверено; верификатор несёт наш аддитив РЯДОМ с гейтом Петра: CI монетки,
     always-LONG, momentum, shift-null); шаблоны этапов 2-3 — драфты с
     TODO(17.0.0), финализировать по обновлённым demo/simulator+demo/tune;
     часовые свечи BTC 2025-26 из 1m датасета (13549 ч, 100% полных);
     segmentation.json: **100% фида 2025 = бэкфилл** (firstSeen>ts+24ч) —
     обязательная кавета к любым результатам, чистый сегмент с ~июня 2026.
     Release-watch перенацелен на 17.0.0 (телега; ⚠️ live НЕ катить). Оба файла
     Петра восстановлены из битой кодировки в agent/swarm/_reference/.
     ПРОДОЛЖЕНИЕ — НОВОЙ СЕССИЕЙ по чеклисту README после релиза. Чистка по
     решению владельца: серверные bigtune/stability/simulator-oos удалены
     (репо-артефакты целы), диск 22%.

123. **🌙 Live-рестарт 23.07 18:25:42Z — не инцидент, self-heal.** MTProto-разрыв
     телеги (recvLoop) → «Bot is offline» → движковый вотчдог SIGKILL'нул
     процесс (не OOM: docker OOMKilled=false, на хосте 7.8G свободно) →
     restart-политика подняла за ~10 сек, дальше чисто. Счёт был плоский,
     позиций не было. Паттерн штатный: телега умерла → вотчдог → супервизор.

## Session 19 (2026-07-23 день) — эксперимент №120: устойчивость авторов — сигнала НЕТ, TV-ветка закрыта

122. **🧹 УТРЕННИЙ ЧЕК + ЧИСТКА ДИСКА CT102 (−34ГБ, 78%→28%).** Цепочка 04:40Z
     чистая (mongo-sync 05:19Z, 561 запись, без 429). Live за ночь: SOL-лонг
     закрыт биржевым OCO-SL ~07:16Z (−1.4%, −$0.28 при cost=20); после
     редеплоя 13:16Z два шорта канала (PENGU 13:26Z, SOL 13:31Z) корректно
     отвергнуты спот-гардом адаптера, движок 16.5.0 дропнул БЕЗ ретрая —
     фикс ретрай-шторма работает в бою; TG 409 разовый (перехлёст контейнеров
     при деплое). Счёт плоский: $94.33 free USDT, ордеров нет. 16.6.0 не вышел.
     Чистка по ОК владельца: труп bigtune-контейнера (9.9ГБ свечного кэша!),
     build cache 18.8→3.7ГБ, 24 старых образа Coolify (паттерн: каждый пуш =
     2×1.67ГБ); оставлены запущенные + по одному предыдущему на откат.
     Предложение владельцу: еженедельный builder prune кроном — не решено.
     Доменный срез news (fit-gate READY): ячейки ещё НЕ значимы (max n=9 из
     порога 10, cointelegraph×market-structure); kitco повторяет паттерн
     «Sharpe растёт с горизонтом» (0.54 на 24h, n=5); conf=0.9 бакет S≈0.6
     (n=7) — следить; повторить срез при первой ячейке n≥10.

121. **🏁 ЭКСПЕРИМЕНТ №120 ОТРАБОТАН: межмесячной устойчивости авторов НЕТ —
     TV-ВЕТКА ЗАКРЫТА ПОЛНОСТЬЮ (5/5 вариаций пусто).**
     [notes/tradingview-dataset/stability/stability-findings.md](notes/tradingview-dataset/stability/stability-findings.md).
     Дизайн-док исполнен дословно, критерии не менялись, все 4 варианта
     (K∈{4,6}×rate∈{0.55,0.60}) в отчёте: 0/4 прошли. Факты: (а) за 55 месяцев
     фильтр «лучших из лучших в каждом месяце» прошли ДВА автора, вайтлист
     всегда ≤1 (масштаб «1 счастливчик на пул» из дизайна); (б) InvestingScope —
     пермабык (longShare 0.98-1.0), его форвард СОВПАЛ с always-long бейзлайном —
     прогнозной ценности ноль; BitCoinGuide в форварде −10.8/−1.8/−9.1%, хуже
     always-short во всех месяцах; (в) placebo с матч-профилем активности
     проходит фильтр в 30% реплик (85/280), real от плацебо-медианы неотличим
     (p=0.69-1.0). Цепочка №101→110→113→119→121 замкнута монотонно — вопрос
     «есть ли сигнал в TV-авторах» закрыт окончательно; протокол Петра работает,
     предмета нет. Техурок: свечной диск-кэш движка (JSON на 1m-свечу) на
     RAM-фиде = ENOSPC (первый прогон умер на 26-м месяце, ~10ГБ слоя; у
     bigtune был такой же) — фикс PersistCandleAdapter.useDummy() заодно
     ускорил фазу A в ~30 раз. Кросс-чек фазы B независимой python-репликой
     сошёлся 1:1. Ответ Петру — драфт собран, отправка за владельцем.

## Session 18 (2026-07-22 ночь) — KAN-эксперимент: мета-лейблинг сигналов движка — эдж НЕ показан

109. **🧪 KAN/мета-лейблинг ОТРАБОТАН ЗА НОЧЬ, вердикт отрицательный —
     [notes/kan-metalabel-experiment.md](notes/kan-metalabel-experiment.md).**
     Песочница kanlab на CT103 redoubt (LXC+GPU, харнесс строителя; наш фикс
     негативного слайса `abc0be2`). Датасет: 615 закрытых сделок движка
     (готовые отчёты + 19 ночных бэктестов 6 TA-стратегий × ETH/SOL/BNB/XRP,
     юнит kanlab-waves ~5ч; feb_2021 патологична, исключена) + 21 фича по
     свечам строго до openTime. Кажущийся эдж на полном v2 (logreg AUC 0.726,
     p=0.0476, PF 1.64→2.89) **разоблачён контролями**: (а) пофолдово — вся
     сила в 2026-фолдах с wr 0.84-0.99 (in-sample-тюненные месяцы), честный
     фолд 2024-25 = 0.436; (б) однородный срез oct_2021 (n=340) — ВСЕ модели
     в плацебо-пучке (logreg 0.565/p=0.14, kan_cheby 0.444/p=0.81); (в) без
     фич-отпечатков стратегии AUC не падает → эпоху выдают рыночные фичи.
     Механизм: модель учит «какая эпоха», не «выиграет ли сигнал». KAN сверх
     бейзлайнов не дал ничего (kan_eff — худшая на v2). **В live не тащим;
     авторский канон «Claude ≠ стратег» не нарушен** — контур движка не
     трогали, только офлайн-analysis. Если возвращаться: гонять стратегии на
     чужих МЕСЯЦАХ (не только символах) — снимет in-sample-загрязнение
     лейблов; раннер и экстрактор сохранены в [tools/kanlab/](tools/kanlab/).
     Боевое не затронуто (кроны/бот/Mongo/Tavily — 0 касаний). Файлы этой
     сессии НЕ закоммичены (ждут «комить» владельца).

111. **🔴 LIVE-ИНЦИДЕНТ 22.07 ~05:15Z: ретрай-шторм SOL-лонга, 148 реальных
     createOrder отклонены Binance `-1013 NOTIONAL`.** Сигнал канала SOL/USDT
     LONG $77.53–78.39 (пост → mongo 6a605575…), риск-слой follow, цена в
     диапазоне → движок каждую минуту: открывает сигнал → createOrder →
     Binance reject «Filter failure: NOTIONAL» (стоимость ордера ниже
     биржевого минимума — тот же корень, что InsufficientFunds 20.07: сайзинг
     cost=100 vs реальный free ≈$96.5) → движок помечает «terminal business
     rejection, retry skipped», но стратегия на СЛЕДУЮЩЕЙ минуте переиздаёт
     сигнал с тем же id → шторм 1/мин: ~150 отклонённых ордеров + спам в ТГ
     «Order Filled (Open)» (вводит в заблуждение: филла НЕТ). Денег не
     потеряно, позиции нет. Прекратится сам при выходе цены из диапазона
     или таймауте сигнала. Опции владельцу: (1) стоп quant-live в Coolify;
     (2) пополнить спот-USDT > cost+запас — следующая попытка встанет
     реальным ордером (и наконец проверит адаптер+brackets в бою); (3) ждать;
     (4) правильный фикс = сайзинг в live.module (cost ≤ free с запасом на
     фильтры) + ручной деплой. Фидбек Петру: «Order Filled» при реджекте +
     переиздание сигнала 1/мин без backoff — движковая каверза.
     **РЕШЕНИЕ ВЛАДЕЛЬЦА (22.07 утро): движковую часть просим у АВТОРА**
     (ретрай-шторм после терминального реджекта + ложный «Order Filled») —
     драфт репорта: notes/live-retry-storm-bugreport.md, отправка за
     владельцем (прецедент: фантом heatmap починен автором за сутки).
     Оговорка зафиксирована: фикс автора остановит шторм, но live не начнёт
     торговать, пока cost $100 > free ≈$96.5 — наш слой (пополнить спот ИЛИ
     снизить cost), отдельное решение владельца. Live-бот пока НЕ трогаем
     (шторм безопасен: ордера не встают, 1 req/мин).

120. **🧭 ДИЗАЙН СЛЕДУЮЩЕГО ЭКСПЕРИМЕНТА: устойчивость авторов через месяцы
     (наводка Петра).** Пётр на big-tune вердикт: «нужен белый список авторов;
     кормить симулятор один символ+один месяц → лучшие авторы; брать лучших
     из лучших В КАЖДОМ месяце; работать по ним окном; промежуточные точки
     закодить агентом» (+его скрейпер dump_ideas.mjs для свежих данных;
     --brokerdebug включит в 16.6.0). Анализ: межмесячная устойчивость —
     статистически ДРУГОЙ фильтр (монеточник: 1 мес P≈0.3, K мес P≈0.3^K —
     multiple testing давится экспоненциально), одномесячный и не должен был
     работать. Три ловушки с обязательными контролями: плодовитость
     (placebo с матч-профилем активности), устойчивость НАПРАВЛЕНИЯ
     (always-long/short бейзлайны; 2022-пермабиры), тюн критериев по
     результату (4 варианта объявлены заранее). Дизайн-док:
     notes/tradingview-dataset/bigtune/author-stability-design.md —
     4 фазы с чекпойнтами (помесячные authorStats без грида → матрица →
     rolling-форвард с фикс-выходами демо → контроли), критерий успеха
     объявлен. РЕАЛИЗАЦИЯ — ОТДЕЛЬНАЯ СЕССИЯ (это её точка входа).

119. **🏁 BIG-TUNE ЗАВЕРШЁН: переносимого сигнала в tune-подходе НЕТ.**
     54/54 пары (2022-01..2026-07), инструменты Петра as-is, placebo×5/пара.
     Медиана real −3.19%/пара (в плюсе 17/54) ПРИ медиане placebo −1.38% —
     real ХУЖЕ случайных вайтлистов; бьёт placebo-медиану 24/54 (знаковый
     p=0.83, монетка); по годам плюс только 2022. June-демо Петра = удачная
     выборка (соседняя пара 05→06: −5.08%). Цепочка замкнулась монотонно:
     сырые идеи Sharpe≈0.01 (№101) → симулятор 9/20 (№110) → tune-пара 7/10
     (№113) → tune-54 24/54 p=0.83. ПРОТОКОЛ работает, предмет — нет.
     Разбор: notes/tradingview-dataset/bigtune/bigtune-findings.md (e1beb6e1).
     Полная картина Петру (адаптер-круг №118а + это) — собрана, отправка за
     владельцем.

118б. **📨 v2-файл Петра (20:49): наш фикс задвоения принят дословно
     (fetchTotalQty free+used ≡ наш balance.total + проверка живых ордеров) —
     РЕДЕПЛОЙ НЕ НУЖЕН, задеплоенное у нас = надмножество его v2 (плюс
     ccxt-фикс market(symbol) и 6 хуков). ⚠️ В его v2 осталась наша находка
     №3: markets[symbol] в getBase/minNotional — упадёт с движковым форматом
     символа; короткий ответ-предупреждение подготовлен (отправка за
     владельцем), чтобы дыра не уехала в доку/16.6.0.

118а. **✅ АДАПТЕР ПРОВЕРЕН БОЕВЫМ КРУГОМ — критерий Петра пройден; попутно
     ТРИ находки.** --brokerdebug в CLI 16.5.0 оказался ЗАГЛУШКОЙ (флаг+help
     есть, диспетчера в билде нет — 8 упоминаний, все текстовые) → написан
     ручной раннер (/opt/quant/data/brokerdebug/: перехват useBrokerAdapter,
     синтетический payload, реальные ордера ~$20). Итог круга: buy+OCO ✓
     (privatePostOrderOco ЖИВ) → повторный open с тем же id БЕЗ покупки ✓
     (реконсиляция; заодно подхватил и обрамил сироту упавшего прогона —
     crash-recovery работает) → close: sweep+продажа всего, стакан чист ✓.
     Цена круга $0.03. Два бага починены по ходу (crontab 5695e38):
     (1) ccxt `markets[symbol]` с движковым «SOLUSDT» = undefined (ключи
     юнифицированные «SOL/USDT») — getBase/fetchFreeQty исторически падали
     во всех sell-путях, minNotional тихо был 1 вместо 5 (след №111);
     замена на market(symbol). Вывод: сегодняшний выход 07:18Z сделал
     биржевой SL-ордер, а не close движка (тот падал на этом баге).
     (2) реконсиляция Петра после успешного входа: монеты заморожены в OCO →
     free=0 → ветка читала «раскручено» и ЗАДВАИВАЛА позицию (поймано
     OPEN#2) — теперь суммарный баланс + проверка живых ордеров. Редеплой
     13:16Z. В копилку Петру: заглушка brokerdebug + двойная покупка в его
     сэмпле + ccxt-индексация.

118. **📨 СХЕМА АВТОРА РАСКРЫТА + правило «один счёт — один бот» + финальный
     адаптер.** (а) Пётр на вопрос о своей стратегии: торгует ПО НОВОСТЯМ
     асимметричной конструкцией — постоянный большой ШОРТ (фьючи, «забыл»,
     платит funding = tail-hedge на обвал «Трамп начнёт бомбить») + лонги на
     единовременных позитивных новостях, чья задача — окупать carry шорта.
     Следствия: его news-стек живёт в роли триггера лонг-ноги с планкой
     «лонги ≥ funding», а не «Sharpe>0»; наш спот схему целиком не
     воспроизводит (шортов нет); наш long-only 0.00% = carry не окуплен.
     Скепсис зафиксирован: прибыльность схемы = вера в частоту обвалов, не
     бэктест. Теория сайзинга шорта посчитана владельцу: S ≤ min(1×маржа,
     доход_лонгов/funding_rate) → на текущих цифрах обоснованный S = 0.
     (б) **Правило Петра, принято законом: ОДИН СЧЁТ — ОДИН БОТ** (dust от
     частичных исполнений лечится только полным выходом из актива — потому
     close продаёт весь free-баланс; ручные операции на счёте бота — только
     при flat/стопе). (в) Развязка SOL: позиция движка закрылась САМА 07:18Z
     (софт-выход, +$15.9), сироту 1.023 SOL владелец продал руками (~$78.5) —
     счёт чистый ~$94. (г) Финальный адаптер = файл Петра дословно (вход/выход
     ГАРАНТИРОВАНЫ: лимитка+полл → cancel-safe → market-добивка; OCO;
     cancel-sweep+verify) + наши 6 хуков на его хелперах (2fd730d); ревью его
     файла отправлено (3 находки: мини-сирота при падении market-добивки,
     возможная депрекация privatePostOrderOco, close-подметание). Деплой
     13:01Z, дальше brokerdebug полный круг.

117б. **🎯 FIXME.md ПЕТРА: истинный корень каскада — БРЕКЕТЫ, обе прежние
     гипотезы сняты.** Он разобрал наш исходник (bundle №2): покупки шли
     ЧЕРЕЗ гейт (филлы через :05-:11 сек после minute-aligned попыток) —
     «лимитник жил в стакане» опровергнут. Механизм: placeBrackets ставил
     TP и SL двумя НЕЗАВИСИМЫМИ sell на один объём → TP замораживал монеты →
     SL падал InsufficientFunds → аварийный market-sell падал о ту же
     заморозку → СЫРОЙ throw = вечный транзиент → движок откатывал open,
     id не потреблялся. Усилитель: гейт attempt>0 у сверки — после дропа
     ретрай-слота свежая строка шла с attempt=0 мимо reconcile, clientOrderId
     исполненного ордера Binance переиспользует → повторная покупка. Всё
     сходится вплоть до «SL только у транша 5» (его профинансировали
     свободные монеты транша 4). Мой утренний патч (000fb9f) закрывал
     TODO 4-5, но корень пропустил — признано. **Порт FIXME целиком
     (crontab 24f2afe):** OCO-брекеты везде (open/restore/partial×2/
     averageBuy), unwind cancel→sell с сохранением типизации исходной
     ошибки, безусловный reconcile + NEW→transient-wait, cancelAllVerified
     во всех продающих хуках, trailing/breakeven/trailingTake пересобирают
     OCO-пару (снятие ноги гасит обе). Критерий готовности Петра:
     brokerdebug ×2 → вторая обязана ответить «уже куплено». ⚠️ Проверить
     на деплое: privatePostOrderOco мог устареть в пользу orderList/oco —
     покажет brokerdebug. Владелец: деплой вечером после закрытия сделок.

117. **🔧 АДАПТЕР ПЕРЕПИСАН ПО ANSWER.md ПЕТРА (каскад №114 разобран до
     конца).** Пётр (ANSWER 23.07): движковая гонка БЫЛА (id потреблялся
     только в памяти; фикс: персист в одной транзакции с открытием — придёт
     в 16.6.0), но в нашем инциденте главная причина — АДАПТЕР: транзиентный
     throw оставлял живой лимитник на бирже (реджекты 22:05/22:25/22:57
     совпали по цене И размеру с филлами через 6-17 мин). Форензика по
     нашему коду: (а) ошибка сети в поллинге/гонка cancel-fill вылетали из
     createLimitOrderAndWait ДО cancel; (б) reconcile не снимал живой NEW →
     новый POST с тем же clientOrderId бился дубль-реджектом, оригинал жил.
     Порт трёх правил в ccxt-модуль (crontab 000fb9f, синтаксис проверен
     esbuild): герметичный helper (любой throw → cancel+сверка), reconcile
     снимает NEW/PARTIALLY_FILLED, терминальный OrderRejectedError на
     attempt>=4, close = раунды отмен + проверка пустоты + продажа ВСЕГО
     остатка (подметает сирот). Петру отправлены live.module.ts + sync-окно
     (bundle №2, ~/Desktop). План вечера: ~23:03Z движок закрывает СВОЙ
     транш (сироты 1-4 движок не знает — закрыть руками после) → деплой
     16.6.0+адаптер на плоском счету (поллер перевзведён). Big-tune не
     затронут (detached, замороженный образ 16.5.0).

116. **🔬 BIG-TUNE ЗАПУЩЕН (ответ на «нужно тестить tune на большем датасете»).**
     Пётр: (а) «сначала додавить интеграцию в биржу» — ждём его fill-consume
     фикс (№114), наша сторона готова; (б) «tune зарешал, скормить больше из
     датасета» — согласны направленно, доказательство = масштаб. Запущен
     rolling walk-forward на ПОЛНОМ датасете: 6578 BTC-идей, 55 месяцев
     (2022-01..2026-07), 54 пары train→test, его Simulator.run (грид
     tune_default, ОДИН пресет на все пары) → freeze → Simulator.test +
     placebo×5 (перестановки authorStats) в каждой паре. Свечи локальные
     (BTCUSDT-2021..2026.jsonl из датасета), контейнер bigtune detached без
     сети, чекпойнт после каждой пары (/opt/quant/data/bigtune/). Раннер и
     фид в репе: notes/tradingview-dataset/bigtune/. Критерий, предложенный
     Петру до результата: медиана real-OOS пар vs медиана placebo-OOS.
     «Пересбор» news-пула НЕ нужен (критика идеи в сессии): чистая
     RSS-когорта достигается фильтром, рестарт осмыслен только с рефитом
     v2.2 (fit-gate READY) через A/B.

115. **🏁 ГЕЙТ ВЗЯТ (BTC=61) — ПОЛНАЯ БАТАРЕЯ СТЕНДА: направленной силы в
     вердиктах v2.1 НЕТ.** Цепочка 23.07 чистая (+23 ok, failed=0; пул 90 ok);
     рамка расширена до 23.07 05:00Z (84eb39b1), b&h +3.09%. Батарея 8
     прогонов (движок 16.5.0): **real −11.19%/WR21 (шорты 1/8!)** | placebo
     ×5: −7.1…+3.6 (ср. −2.72) | long-only 0.00 | invert −4.62. Чтение:
     (1) real ХУЖЕ всех 5 placebo — ВТОРАЯ батарея подряд на дне пучка
     (~1.6σ от среднего); (2) placebo-пучок сам отрицателен при b&h +3.09 —
     механика выходов (SL −1%+trailing+фрикшн) минусует без прогноза, два
     НЕЗАВИСИМЫХ вывода видны только через пучок; (3) invert внутри пучка —
     сигнал не «перевёрнутый», его нет; (4) heatmap 82 вердикта: confidence-
     гипотеза автора подтверждена на значимой ячейке (0.6, n=50, S≈0);
     домены все n<10 — копить (~15 BTC-ok/день). **ЕДИНЫЙ ОТВЕТ ПЕТРУ ГОТОВ
     К ОТПРАВКЕ** (стиль владельца): notes/author-message-draft-news-tv.md —
     TV-вердикт + simulator-OOS + tune-july + стенд-батарея; ждёт ОК.
     Лог-бандл каскада №114 передан владельцу (~/Desktop/cascade-bundle-
     dlya-petra.txt, секреты вычищены) — Пётр запросил, отправка за ним.

114. **🔴 LIVE-ИНЦИДЕНТ №2 (ночь 22→23.07): каскад филлов по одному id на
     16.5.0 — весь депозит в SOL пятью траншами.** После фикса реджект-ветки
     открылась зеркальная: успешный ФИЛЛ не потребляет детерминированный id →
     стратегия переиздаёт сигнал, слот не держит → 5 покупок по ~$20
     (21:55—23:03, средн. вход ~$77.9), USDT free $0.40. Брекеты неровные:
     TP ~117 у траншей есть, биржевой стоп-лимит только у последнего (0.201)
     — 4/5 без биржевого SL (софт-SL движка −1% на тике работает, дыра только
     на гэп между тиками). Телега 16.5.0 при этом штатная — спама нет, филлы
     нотифицированы. **Решение владельца: вариант Б (доставить стопы) →
     скорректирован после разбора спот-механики**: монеты заперты в TP-ордерах
     движка, добавить SL без отмены его ордеров нельзя (OCO-пара ставится
     только вместе) → ордера движка НЕ трогаем, защита = софт-SL −1% +
     таймауты 24ч (с 21:55Z). **Автора предупредить — решение владельца**:
     драфт стилем владельца notes/live-fill-cascade-bugreport.md (ожидание:
     филл потребляет id как реджект; брекеты одинаковые на каждый филл).
     Позиция на момент разбора: −0.6% (−$0.55), SOL $77.47.

113. **🚀 16.5.0 В БОЮ НА ВСЁМ СТЕКЕ + tune-демо Петра разобран и проверен
     межрежимно.** (а) Пётр выпустил 16.5.0 через ~5ч после репорта; поллер
     поймал за 26 мин, синк обеих реп (cost=20 пережил merge), канарейка
     paper → live через обёртку (у агента право), верифицировано: движок
     16.5.0, cost=20, order-шаблоны, лог чист; поллер снят. Сверх ANSWER.md:
     4 канала (Fill/Reject/Continue/Stop), notification.config.mjs CLI
     (order_sync=false — ложные «Order Filled» отключены классом),
     Notification-подсистема прокинута в UI, **Simulator.test** + оси
     profitLockPercent/minWeightAligned. (б) **demo/tune = его ответ на нашу
     OOS-критику**: walk-forward train 70% июня → freeze → один выстрел по
     хвосту, честность структурная (файлы разделены). Его хвост: Sharpe
     2.44→1.65. (в) **Наш межрежимный тест его tune-точки на июле**
     (Simulator.test, его же инструмент; placebo = 10 перестановок имён в
     authorStats): **+3.92%, 7 сделок, ранг 7/10 среди placebo** (пучок:
     mean −2.28, [−10.6…+7.0]). Механика точки ПЕРЕНОСИТСЯ (подпись сделок =
     копия его хвоста: 3× profit_lock +2.20, трейлинги ≈0, ноль хард-стопов),
     вайтлист НЕ доказан (p≈0.3). Деградация монотонна 2.44→1.65→0.89, не
     обвал — tune-протокол устойчивее наивного симулятора (тот: ранг 9/20).
     Вердикт: протокол — большой шаг, эдж не доказан (его же оговорка в
     README). Разбор: notes/tradingview-dataset/simulator-oos/oos-findings.md
     (+tune-july-result.json). (г) Стиль общения владельца зафиксирован в
     памяти агента (правило: драфты Петру — его тоном, дополнять при новых
     чертах).

112а. **⚙️ cost=$20 ЗАДЕПЛОЕН НА LIVE (решение владельца) — с двумя боевыми
     уроками по пути.** (а) Владелец продал SOL-сироту → $95.6 free; решение:
     `CC_POSITION_ENTRY_COST: 20` (до 4 позиций из 12 символов, запас над
     минимумом $5; партиалов в jan_2026 нет). (б) **Урок-1 (перекат при
     активном сигнале):** в 11-сек окно рецикла старый контейнер (cost=100)
     успел минутной попыткой КУПИТЬ 1.208 SOL за $93.35 — позиция осталась
     ГОЛОЙ (брекеты не успел, новый контейнер о ней не знает → сирота №2 +
     шторм InsufficientFunds). Владелец продал обратно (цена круговорота
     ~$0.6). Правило: деплой live при активном сигнале в entry-диапазоне =
     окно двойной логики, учитывать. (в) **Урок-2 (chdir-квирк модулей):**
     первая правка легла в корневой modules/live.module.ts, который live НЕ
     грузит — CLI после --entry делает chdir в папку стратегии, боевой конфиг
     = content/jan_2026.strategy/modules/live.module.ts (улика: «Cost: 100» в
     спаме после первого переката). Фикс 2c3a40e, редеплой, верифицировано:
     контейнер healthy, cost=20 в загружаемом модуле, движок 16.4.0.
     (г) **Агенту дан боевой деплой:** обёртка /opt/quant/bin/deploy-live.sh
     (создал владелец) + permission-правило `Bash(ssh cfy /opt/quant/bin/
     deploy-live.sh)` в settings.local.json — узкое, только деплой;
     deploy-live-ветку двигаю сам (прецедент прошёл). Классификатор прочие
     live-операции по-прежнему режет — правильно. (д) Вахта на первый
     $20-филл взведена (4ч, лог); сигнал жив до 05:15Z 23.07 — цена на
     границе диапазона, возможен чистый боевой тест адаптер+брекеты на $20.

112. **📨 ОТВЕТ ПЕТРА НА №111 ЗА 2.5 ЧАСА: фикс закоден, выйдет в 16.5.0;
     поллер релиза взведён.** Его ANSWER.md (конспект в
     notes/live-retry-storm-bugreport.md): терминальный реджект потребляет
     signalId (whipsaw-гард), новые каналы listenOrderFill (строго после
     confirmed) / listenOrderReject (ровно раз, с причиной), телега CLI снята
     с listenSync («Order Filled» при реджекте невозможен по построению),
     регрессы пинуют; старый тест EDGE DETERMINISTIC пиновал ровно
     инцидентное поведение. ⚠️ Breaking: детерминированные id (наш live
     jan_2026 = mongo-пост) после терминального реджекта не ретраятся —
     желаемое поведение. Владелец: «поставлю на отслеживание, сразу
     задеплою» → **/opt/quant/bin/release-watch.sh + /etc/cron.d/
     quant-release-watch (*/20)**: npm registry kit+cli, стейт-файл, при
     бампе — сообщение в вахта-телегу (токен live.env read-only) с
     чеклистом деплоя. План 16.5.0: синк реп → paper авто-канарейка; live
     руками владельца; после — проверить телегу live (mustache-оверрайды
     совместимы по его ANSWER). Депозит-вопрос (cost>free) открыт за
     владельцем — без него live не торгует и на 16.5.0.

## Session 17-хвост (2026-07-22 ночь) — simulator Петра: OOS-тест его вайтлиста

110. **🧪 SIMULATOR ПЕТРА (16.4.0): in-sample +16% → OOS +0.5%, ранг 9/20 среди
     placebo.** Пётр докинул `--simulator` (сеточный перебор exit-параметров +
     обучаемый вайтлист авторов; июнь BTCUSDT: +15.98%, WR 62%, Sharpe 1.57 на
     вайтлисте 13/154). Разбор кода: механика сделок честная (вход next-minute
     open, выходы по фитилям, stop>trail, фрикшн обе ноги, анти-флуд 8ч,
     time-based Sharpe), но вайтлист обучается in-sample — он сам это
     документирует («lookahead inside train is deliberate», «validation MUST be
     a real engine backtest»). (а) **Синк реп** (владелец): paperhands смержен
     с upstream (057dbe93, 89 коммитов: Simulator+16.4.0+его июньский фид и
     артефакт), crontab-форк 2fa040a; автодеплой проконтролирован, paper
     перекатился на 16.4.0 healthy = канарейка; deploy-live НЕ тронут
     (классификатор зарубил force-push боевой ветки — правильно), live 16.3.0
     до рук владельца. (б) **OOS-тест (вариант А, ОК владельца):** его
     опубликованный вайтлист-13 + замороженные H=7/TT=3/72ч/N=2 на июле
     01–15.07 (564 идеи из HF-датасета, 11/13 авторов активны):
     **+0.52%, 4 сделки, все time_expired** (vs +15.98% in-sample); placebo-20
     (случайные вайтлисты-13, mulberry32): mean +0.77%, [−10.2…+7.5], **real =
     9-й из 20**. Crowd-baseline −5.89% — «фильтр» работает как «торгуй реже»,
     не как отбор скилла. PnL-победитель: +1.62% OOS (vs +19.37%). Вывод =
     подтверждение №101 его же инструментом: месячный hit-rate-отбор авторов
     не переносится. Симулятор как ГЕНЕРАТОР КАНДИДАТОВ легитимен (его же
     оговорка), скачок «in-sample → прод-вайтлист» — нет. Разбор:
     notes/tradingview-dataset/simulator-oos/ (oos-findings.md + раннер +
     result.json; серверная копия /opt/quant/data/simulator-oos). Для
     news-нити: маппинг author=domain готов концептуально, применять только
     с time-split после гейта ≥50. ⚠️ DECISIONS №109 (KAN) в дереве НЕ
     закоммичен — гейт «комить» той сессии, эта запись коммитится вместе с ним.

## Session 17 (2026-07-21 день) — news-verdict: контрольные режимы стенда, real на дне placebo-пучка

107. **🧪 СТЕНД ПОЛУЧИЛ КОНТРОЛЬНЫЕ РЕЖИМЫ; смоуки: real хуже всех placebo,
     пучок сам отрицателен.** (а) **Здоровье пайплайна ✓:** дневная цепочка
     прошла (08:28-09:23Z, ручной week-запуск s16; 429 не порвал), пул
     324 raw / 40 ok / BTC 28; автодеплой жив (8 перекатов paper за утро).
     Расшифровка TZ крона: «09:40» = 04:40Z, RSS `10 */4` = 03:10/07:10/11:10/…Z.
     Сегодняшний 04:40Z ещё падал на ENOENT .env (до фикса) — первый честный
     тест исправленной цепочки = 22.07 04:40Z. **RSS-тик 11:10Z подтверждён
     автоматическим** (new=4, дедуп штатный; decrypt+bitcoinmagazine transient
     fetch failed — скрипт продолжил, 4ч-тик доберёт). Темп RSS-эры
     ~10-14 BTC-ok/день → гейт ≥50 ориентировочно 23.07 (конс. 24-25.07);
     прогноз владельцу дан: батарея тестов закрывается 23-25.07. (б) **NEWS_STAND_MODE** =
     real|placebo|long-only|invert (96692a86+4751c929): placebo = FNV-1a(url+
     seed)+fmix32 — детерминирован; ⚠️ урок: без fmix32 младший бит FNV =
     чётность символов → нечётные сиды ИДЕНТИЧНЫ (поймано санити-чеком на 28
     реальных url до прогонов). backtest.sh пробрасывает env, метит каталог
     режимом; разбор — /opt/quant/bin/bt_trades.py; ⚠️ грабля set -eu:
     `VAR=$(...)`с падающей подстановкой молча убивает скрипт, а `| tail`
     маскирует exit-код. (в) **Смоуки на рамке №106 (b&h +4.05%!), n≤14 =
     НЕ выводы:** real −8.83%/WR25 (№106г) | long-only −0.84%/WR43 |
     invert −3.70%/WR38 | placebo s1..s5: −7.50/−6.21/+3.77/−6.19/+1.18
     (среднее −2.99). Чтение: real на дне пучка (но в ~1σ суммы — не значимо);
     **placebo-пучок сам отрицателен при b&h +4.05% → механика выходов
     (SL −1%+trailing+фрикшн) имеет отрицательный baseline без всякого
     прогноза** — «стенд хуже b&h» ≠ «вердикты плохие», механику и прогноз
     разделять. Long-only внутри разброса (= возможно просто «нет шортов на
     росте»). (г) **sharpe-heatmap №88а на RSS-пуле** (v2.1 n=32, 69 доходн.):
     домены все n<5 («н/д»), confidence 0.3≈0.6 (гипотеза автора держится),
     μ>0 на 4h = маркет-бета лонгового пула (29L/11S) на росте BTC.
     (д) Единый драфт ответа Петру: notes/author-message-draft-news-tv.md
     (TV-вердикт финален, стенд-цифры после гейта; отправка = ОК владельца).
     Дальше: полный прогон real+пучок(≥5 сидов)+long-only+invert на BTC≥50,
     placebo-baseline и в heatmap.

108. **📨 ОТВЕТ ПЕТРА про RSS/Tavily-даты (телега 21.07 22:33) + source-поле в mongo.**
     (а) **Механика дат Tavily раскрыта автором:** их парсер НЕ берёт pubDate
     изданий, ходит раз в 4ч, дата = момент краула. `00:00 GMT` = проставлено
     задним числом, когда парсер пропустил пост, = **look-ahead** — наше
     исключение `midnightUtc` (№66) валидировано, теперь известен механизм.
     Не-полночь = look-ahead нет, но лаг ~2ч от публикации → Tavily-айтемы в
     стенде/heatmap консервативны (входим позже возможного), а 1h-горизонт для
     них разбавлен — резать по source. Его единственная RSS-грабля — «сколько
     людей читают RSS» (охват): для нас некритично, RSS = транспорт честной
     даты тех же статей, не прокси дистрибуции. (б) **source-поле пронесено в
     mongo** (news_mongo_sync: rss|tavily|null) — владелец анонсировал его
     Петру, а в вердиктах его не было (только в raw-журнале); бэкфил всех 324
     записей = одиночный прогон sync после деплоя. (в) Режим владельца на
     накопление: ноут не нужен, заход раз в день (2-5 мин); в день гейта
     батарея через nohup на сервере. Прогноз: тесты 23-25.07.

## Session 16 (2026-07-21 ночь) — ПЕРЕЕЗД НА СЕРВЕР: весь стек на CT102, live ВКЛЮЧЁН

106. **🚀 NEWS-НИТЬ РАЗБЛОКИРОВАНА: пайплайн реанимирован, RSS-джекпот, серверный
     бэктест сошёлся с эталоном.** Владелец: «сначала тестим, автор подождёт;
     бэктест на сервере». (а) **Диагноз простоя с 19.07** (двухслойный): 19-20.07 —
     Tavily-лимит «spent 8/8»; 21.07 — крон в контейнере падал на ENOENT
     example/.env (скрипты требовали файл; пофикшено: .env опционален во всех 4
     скриптах). Week-прогон закрыл дыру: +11 raw, из них ok=0 — датированный
     Tavily-пул выдал НОЛЬ привязываемых новостей за неделю. (б) **RSS-коллектор
     (news_rss.mjs, zero-dep)**: 5 фидов (coindesk/cointelegraph/decrypt/theblock/
     bitcoinmagazine; cryptoslate 403) дали **119 датированных за один прогон** vs
     11/нед у Tavily. После классификации v2.1: пул **12→40 ok** (BTC 10→28),
     depth 14.6д — **гейт глубины 14д ПРОЙДЕН**, top-domain 8/10. В кроне: RSS
     каждые 4ч + в дневной цепочке 09:40. (в) **Серверный бэктест-контур готов**
     (/opt/quant/bin/backtest.sh: paper-образ, cpus=2, кэш свечей
     /opt/quant/data/backtest_candles, результаты /opt/quant/data/backtests/<run>).
     Смоук на старом пуле n=8 **сошёлся с ноутбучным В бит-в-бит: 8 closed,
     −5.2565**. Грабли: ①`--entry` в backtest-режиме ТИХО ИГНОРИРУЕТСЯ (прогон
     «стоит», status_info пуст) — путь стратегии только ПОЗИЦИОННЫМ аргументом;
     ②стенд-фиксы для контейнера: mongoose через INGEST_PKG, mongo-URL через
     NEWS_MONGO_URL. (г) **Первый содержательный прогон** (рамка расширена до
     21.07 09:00Z, commit 44266c9e): **13 opened / 12 closed, PnL −8.83%, WR 25%**
     (μ=−0.74/сделку, по-сделочный Sharpe ≈ −0.69) — стенд снова хуже b&h.
     Разрез: шорты 7/7 в минус (все SL −1.4%), лонги 5 шт −1.83% суммарно.
     n=12 всё ещё мал; главное — контур на сервере ВОСПРОИЗВОДИМ (smoke n=8
     сошёлся с ноутбучным бит-в-бит) и данные теперь прибывают (RSS 4ч-кроном).
     Дальше: копить неделю, placebo-контроль, sharpe-heatmap по доменам,
     отдельно лонг-подвыборка (спот торгует только их). Ответ автору
     (датасет-вердикт) — ПОСЛЕ этих тестов, решение владельца.

105. **⚠️ ИНЦИДЕНТ+ФИКС: Coolify молча увёл аппы на пустые volumes; синк с автором;
     вахта 2ч.** (а) Coolify префиксует named volumes uuid'ом ДАЖЕ при
     external:true → после адопции paper/live/news ~1.5ч работали на пустых томах
     (история цела в quant_*). Фикс: данные → /opt/quant/data/* (host-bind),
     compose всех трёх реп переведены на бинды, редеплой, история видна (85M/118M/
     news.db). Правило: **в Coolify-compose только bind-пути, никаких named volumes**.
     (б) Синк с upstream обеих реп (--theirs): пины 16.3.0, @backtest-kit/mongo
     6.3.0, agent-swarm-kit 6.0.0 (мажор) — paper авто-деплоем (поллер, проверен
     на реальных пушах), live руками, оба healthy. Отставание от автора = 0.
     (в) Вахта: отчёт КАЖДЫЙ 2ч-тик (решение владельца, отменяет вариант C).
     (г) Forgejo выведен из пайплайна (вебхуки удалены, зеркала = пассивный бэкап);
     ноут добит: локальным mongo/redis/pgcluster снят restart=always.
     (д) 🔴 НАЙДЕНО ПРИ РЕВИЗИИ news-нити: **пайплайн новостей заглох с 19.07
     06:52** (0 новых raw за 18-21.07, последний sync до миграции крона) — задача
     №1 следующей сессии. В news-audit 194 вердикта: 12 ok (7 kitco, BTC 10),
     182 rejected, диапазон 06-17.07.

104. **✅ COOLIFY-АДОПЦИЯ ЗАВЕРШЕНА (утро): push→авто-деплой работает.**
     news/paper/live = аппы Coolify (проект quant), билд из ПУБЛИЧНОГО GitHub
     (Coolify превращает «token@host» в SSH → Forgejo-токен для клона не годится,
     зеркала остались приватными). Авто-деплой: SHA-поллер на cfy (cron */5,
     /opt/quant/bin/autodeploy.sh) — news@master, paper@agent-night; **live@deploy-live
     ТОЛЬКО руками**. E2E проверен маркер-коммитом (8778da→001603, деплой ушёл,
     контейнер перекатился healthy). Квирки Coolify: игнорирует container_name →
     network-aliases quant-{news,paper,live}; env-API без is_build_time. Forgejo-вебхуки
     созданы (news#1, paper#2), но дефолт Forgejo запрещает вебхуки на приватные IP —
     включение за владельцем: app.ini CT100 `[webhook] ALLOWED_HOST_LIST = private` +
     restart forgejo (тогда поллер снять). Вахта и алиасы под Coolify проверены
     (msg=5744, tg=ok). Live перекатился без потерь: cli 16.3.0, mongo-стейт в
     volumes, MTProto жив. + Владелец дал локальный Ollama CT101 (qwen2.5:14b/coder)
     для мелких задач — памятка в памяти агента (ollama-local-agent).

103. **🚚 СТЕК ПЕРЕЕХАЛ С НОУТБУКА НА CT102 (Coolify-хост), LIVE ТОРГУЕТ С СЕРВЕРА.**
     Владелец: «переносим весь проект, все в live, все документируем». (а) **Что
     крутится на cfy (/opt/quant, plain compose, сеть coolify):** deps (mongo 8.0.4
     с seccomp rseq→ENOSYS — ядро CT102 тоже 7.0.x!, redis, pgcluster, quickchart),
     news-service (:8080 внутри), paper (feb_2026 BTCUSDT, порт 60052), live
     (jan_2026, порт 60050, **cli 16.3.0**) + cron-контейнеры (supercronic:
     pump-forward, volume-monitor, news-пайплайн 09:40, drift-alert, duty-watch-server).
     Данные мигрированы: mongodump (backtest-pro + news-audit), news.db, dump/леджеры.
     (б) **Ноутбучный контур ПОГАШЕН** (@reboot-строки закомментированы, paper убит,
     докеры остановлены) — ноут = только разработка. Двойного live НЕТ (проверено ps
     на обеих машинах). (в) **Стейдж session 14-15 закоммичен** (integration 8bc934b:
     движок 16.3.0 + сайзинг-фикс live.module + заметки инцидента) — git выровнен с
     реальностью, сервер собран с deploy-live = integration HEAD. (г) **Грабли
     контейнеризации: packages/{core,main}/build/ гитигнорены** → в Dockerfile
     добавлена rollup-сборка (npm install в пакете, как build.sh автора); **MTProto
     session-файл нужен в ТРЁХ путях** (корень + content/jan_2026.strategy/ +
     packages/main/ — воркер читает из своего cwd), монтируется с хоста, в git его
     нет. Верифицировано: MTProto подключён (LAYER 198), вахта полный цикл ✓ (алерт
     «СИРОТА SOL $94.84» штатный — хвост №100), UI отдаёт jan_2026_strategy.
     (д) **Апстрим-чек:** cli 16.3.0 = npm latest (мы на актуальном); у автора
     @backtest-kit/mongo 6.3.0 vs наш 6.1.0 — кандидат на бамп (не ночью);
     paperhands отстал от upstream на 21 коммит (docs/wip), crontab-форк на 2
     («inc» = дep-бампы). (е) **Coolify-адопция аппов** (UI-управление, вебхуки
     Forgejo→Coolify) — СЛЕДУЮЩИЙ ШАГ, ждёт решения владельца по доступу к
     приватным зеркалам (классификатор не дал агенту ни сменить видимость, ни
     создать токены). Пайплайн пока: push → Forgejo-зеркало (10м) → руками
     `git archive | tar` на cfy + `docker compose up -d --build`. Ранбук:
     /opt/quant/README.md на cfy.

## Session 13-хвост (2026-07-20 день) — live рестартнут на 16.3.0 агентом; первый боевой сигнал 5741

102. **LIVE РЕСТАРТНУТ НА 16.3.0 (агент сам) + ПЕРВЫЙ БОЕВОЙ СИГНАЛ 5741.**
     (а) **Первый реальный сигнал канала — пост 5741 SOL/USDT LONG** (вход
     $75.10-75.93, SL $72.60, публ 13:01). gramJS снова завис (лог протух 12м,
     watchdog 22→25) → ingest задержался. **Итог: СДЕЛКИ НЕТ (правильно)** —
     SOL ушёл на $76.31 ВЫШЕ входа $75.93 → entry-check отклонил; signals=0,
     позиции нет. Главная вахта (адаптер+trailing в бою) ВСЁ ЕЩЁ не сработала
     (вход не триггернулся). (б) **Владелец: «перезапускай всё сам».** kill
     БОЕВОГО в этот раз НЕ заблокирован (прошёл); зависший бот умирал медленно
     → pgrep флип-флопил 0↔1 → **гард на `ps` (не pgrep) подтвердил 0 перед
     стартом, двойного бота НЕ создал** (урок №98 сработал, ps надёжнее pgrep
     на агонии). Старт systemd-run прошёл (мне разрешён). Live: pid свежий,
     ОДИН, gramJS чист, **16.3.0** — рестарт ЗАОДНО применил застейдженный
     Session 14 апгрейд (пункт session-15 закрыт). 5741 сразу заингестен
     (mongo 5737→5741). (в) ⚠️ **gramJS-деградация — ПАТТЕРН** (вчера №99б +
     сегодня): 15m-краул канала виснет, рестарт лечит. Follow-up: авто-детект
     зависшего live-fetch-data + авто-рестарт (иначе сигнал в живом окне входа
     будет пропущен).

## Session 14 (2026-07-20 ночь) — TradingView-вердикт + апгрейд 16.3.0 (live owner-gated)

101. **🎯 TradingView-датасет ОТРАБОТАН + апгрейд 16.3.0.** Владелец ушёл спать,
     дал команду «если у автора обновления — синхронизируй и перезапускай всё сам».
     (а) **TradingView (главная задача) — ВЕРДИКТ: торгового эджа НЕТ.** Стянул 3.65ГБ
     (`/home/s1dd1/dev/quant/tradingview-ideas-signals/`), навёл sharpe-heatmap (№88а) на
     **18,323 идеи** (из 51,963; отсеяно 42% не-Binance-крипта / 16% NEUTRAL / 4.6% до-2022).
     Горизонты 1h/4h/8h (бандл fwd +485м). Агрегат: Sharpe≈**0.01**, WR **49%**, μ≈0…+0.04%.
     **Placebo (случайное направление) даёт +0.025% на 8ч** = Jensen-выпуклость
     `(X/E−1)+(E/X−1)≥0` → половина «эджа» — артефакт, настоящий скилл <0.02%/сделку, сам
     placebo статзначимо ≈0. **Фрикшн 0.2%×круг съедает всё (~10×)**, funding+спред не
     моделированы → в реале хуже. Нестабильно по годам, **2026 отрицательно**. «Pro»-бейдж
     НЕ предсказывает. Пара авторов светятся (t>2.7), но 74 теста=multiple testing. Look-ahead
     чист. **Независимый скептик-аудит (Reality-Checker): A−, HIGH, вердикт подписан**
     (byte-verify JS↔Python toBinanceSymbol, return-математика, placebo). НЕ тянуть в live;
     датасет = research-материал. Разбор: `agent/notes/tradingview-dataset/` (findings.md +
     tv_universe/tv_sharpe/verify_tv.py + tv-sharpe-heatmap.md). Память [[tradingview-ideas-verdict]].
     Методбонус Петру: под №88а-Sharpe ставить placebo-baseline. Ответ Петру (спасибо+вердикт)
     за владельцем.
     (б) **Апгрейд 16.3.0** (Пётр релизнул). Дифф 16.2→16.3 (`2c3caecf..3d2058b8 "notify"`):
     **чисто аддитивно** (917/+2− src) — opt-in `setPaused`/`listenPause` (пауза ОТКРЫТИЯ
     новых, старые закрываются) + Notification; наши боты не зовут → поведение не меняется
     (та самая «кнопка стоп» из фидбека №98г). **PAPER: апнут+рестартнут на 16.3.0** (канарейка:
     boot чист, тики свежие, DNS ок). **LIVE: node_modules застейджены 16.3.0 (mongo 6.1.0 не
     трогал), но `kill live` РЕЖЕТ КЛАССИФИКАТОР** — устная авторизация ≠ harness-гейт, и я не
     обхожу защиту боевого счёта. Live был FLAT (0 сделок) → рестарт безопасен = **одна команда
     владельца**, runbook durable `agent/notes/live-restart-16.3.0-runbook.sh` (double-bot §98),
     локи-роллбэк `agent/notes/upgrade-16.3.0-lockbackup/`. Срочности ноль (live штатно на
     16.2.0). git форков НЕ ребейзил (upstream crontab снёс бы наш live.module.ts/duty-watch).
     package.json ×2 обновлён, НЕ коммичено. Память [[upgrade-16.3.0-live-gated]].
     (в) **429 укусил крон news 19.07 09:40** (`spent 8/8` → `&&`-цепочка порвалась, classify/
     dataset/sync не пошли). Сегодня 09:40 (кредиты сброс к 04:40Z) должен пройти — проверить.
     Follow-up «collect толерантным к 429» подтверждён реальным.

> Точка входа session 15: (1) **владельцу: рестартнуть live на 16.3.0** одной командой
> (runbook выше; pgrep=0 сначала!) ЛИБО добавить Bash-права на kill live, если хочет чтобы
> агент рестартил сам. (2) Проверить боевой news-крон 09:40 20.07 (429-каверза). (3) НЕ
> коммичено (ждёт «комить»): DECISIONS №100+№101, duty-watch.mjs (полит.C), package.json ×2
> (16.3.0), notes/tradingview-dataset/*, память. (4) TradingView закрыт — эджа нет; Tavily
> live-канал (№99) работает отдельно. (5) Первый боевой сигнал канала в live — всё ещё НЕ
> проверен в бою (адаптер+trailing).

## Session 13 — ЗАКРЫТИЕ (2026-07-20 ночь) — TradingView-датасет автора + вахта-политика C

100. **🎯 ПЁТР ДАЛ ДАТАСЕТ, ЗАКРЫВАЮЩИЙ ДАТА-ГОЛОД + правки вахты.**
     (а) **Ответ Петра на наш вопрос «датированный источник крипто-новостей?»:**
     https://huggingface.co/datasets/tripolskypetr/tradingview-ideas-signals
     (MIT). **52 963 TradingView крипто-идеи 2014-2026**, каждая: `ts`
     («look-ahead-bias-free»), `direction` (LONG/SHORT/NEUTRAL), `symbol`,
     `title`, `author`/`authorIsPro`/likes; **+ бандл ~40М свечей** (313
     символов, окно −1445/+485 мин) — цена для валидации ВНУТРИ. Решает нашу
     Tavily-дату-проблему ([[tavily-sources-map-kitco]]) в 5000× масштабе.
     Разбор — [[tradingview-ideas-dataset]]. **СЛЕДУЮЩАЯ СЕССИЯ — ГЛАВНАЯ
     ЗАДАЧА:** стянуть датасет → навести нашу sharpe-heatmap (№88а/news_sharpe
     ac0bf53) на 52k идей (direction→движение→Sharpe по author/symbol/isPro);
     проверить честно, есть ли у retail-идей эдж (evidence-first, НЕ хайп, наш
     OOS/Sharpe-гейт решает). Tavily-конвейер (№99) остаётся боевым live-
     каналом; TradingView — исследование/бэктест. Ответ Петру за владельцем.
     (б) **Вахта-политика C (владелец 20.07):** duty-watch.mjs — зелёный
     heartbeat «вахта жива» в 08:23/14:23/20:23 (было: только 08:23; было
     тихо на зелёном → «почему нет отчётов»), red и главная вахта мгновенно
     на любом тике. Крон тикал исправно каждые 2ч, просто молчал на зелёном.
     **НЕ ЗАКОММИЧЕНО** (duty-watch.mjs в crontab-форке) — ждёт «комить».
     (в) Письмо Петру про источники (эмпирика Tavily-два-индекса) отправлено;
     он ответил датасетом.

> Точка входа session 14: DECISIONS №95→100 сверху. ГЛАВНОЕ НОВОЕ:
> TradingView-датасет ([[tradingview-ideas-dataset]]) → sharpe-heatmap на
> масштабе (отдельная сессия). Вахта: первый боевой сигнал канала (адаптер
> 16.1/16.2 + trailing в бою НЕ проверены — канал даёт только TP-отчёты/
> рекламу); news-крон 09:40 первый боевой через сервис (каверза 429 при
> исчерпании credits — №99а, follow-up толерантность); duty-watch heartbeat
> C НЕ закоммичен. Контур: боты 16.2.0, миграция news done (DRY=0), мост
> 6 фаз запушен. Ops-уроки: pgrep=0 перед стартом live (№98 двойной бот),
> kill/запуск раздельно, awk врёт ru_RU, headless без --ui умирает.

## Session 13 (2026-07-19) — апгрейд 16.2.0, миграция news→сервис, сетевой инцидент gramJS

99. **МИГРАЦИЯ NEWS→СЕРВИС ЗАВЕРШЕНА + СЕТЕВОЙ ИНЦИДЕНТ gramJS + серия ребутов.**
    (а) **Миграция news-крона на сервис (план №95, повод — крон пропустил
    сегодняшний 09:40 из-за power-off).** Вариант Б владельца (совместить догон
    с миграцией). Порядок (держит инвариант «Tavily зовёт либо крон, либо
    сервис»): крон-строка `news_collect.mjs day` → `+ --from-service
    http://localhost:8080` (бэкап ~/crontab-backup-2026-07-19-migration.txt);
    `NEWS_SERVICE_DRY=1→0` в news-service/.env (+бэкап) + `docker compose up -d`.
    Догон `collect week --from-service`: сервис реально сходил в Tavily,
    credits 0→**8/8** (лимитер сработал), new=3 raw (все макро/no_symbol →
    fit-gate так и 7/10). Пережила ребуты (dry_mode False держится).
    ⚠️ **КАВЕРЗА (follow-up):** при исчерпанных credits `collect --from-service`
    отдаёт **429**, `&&`-цепочка крона рвётся (classify/dataset/sync не идут).
    На обычном дне (credits свежие в 09:40) не проблема, но collect надо
    сделать толерантным к 429. Проверить первый боевой крон 09:40 20.07
    (зависит от сброса лимита к тому времени). Credits журнальны — рестарт
    НЕ сбрасывает.
    (б) **🟡 СЕТЕВОЙ ИНЦИДЕНТ: деградация telegram/MTProto (gramJS), 4 watchdog
    подряд (12:37/12:54/13:09/13:25).** Диагноз: `live-fetch-data` (крон 15m =
    канальный краул через gramJS) виснет каждый цикл; TIMEOUT в
    `telegram/client/updates.js`, НЕ Binance (ping 200, PENGU не делистнут,
    MTProto :80 достижим) → застрявшая gramJS-сессия, не сеть. Импакт низкий
    (канал тих, торговля работает, позиций нет). Владелец рестартнул live по
    kill→pgrep=0→systemd-run (двойного НЕ было — шаг pgrep=0 соблюдён, урок
    №98). gramJS переподключился чисто, watchdog встал на 13. УРОК: telegram-
    коннект (ingest канала) — отдельный класс отказа; Binance-сторона при этом
    жива; лечится рестартом (сессия, не сеть). HTTP-telegram (алерты/мост)
    работал всё время (другой протокол).
    (в) **Серия ребутов машины (10:34 / 17:19 / 17:53)** — контур каждый раз
    поднимался сам (@reboot live/paper на 16.2.0 + docker restart-policy +
    systemd linger мост + user-crontab duty-watch). Инфра отказоустойчивости
    подтверждена боем. Сессионный scratchpad/монитор стирается ребутом —
    пересоздаётся; durable-вахта = duty-watch cron в канал.
    (г) Пост канала 5740 = РЕКЛАМА (slivaeminfo.org), не сигнал — в mongo не
    попал (5737 верно). Письмо Петру (сарказм про «тыркальник», №98г)
    отправлено владельцем.

98. **АПГРЕЙД 16.2.0 + ФАНТОМ ПРОВЕРЕН + ИНЦИДЕНТ ДВОЙНОГО LIVE.**
    (а) Пётр релизнул 16.2.0 (ядро npm + экосистема crontab d70717ee = чистый
    бамп). Накатили на example и crontab-форк (все @backtest-kit/*: cli/graph/
    pinets/signals/ui + backtest-kit; mongo своя ветка 6.x не тронута).
    **Фикс фантома (replayedIds) проверен фактом**: перегон стенда на 16.2.0 →
    status_info честен САМ, variant B totalTrades 9→**8** (=closed в report),
    totalPnl −6.6599→**−5.2565** (без дубля −1.4034), avgLoss на 6 лоссах.
    Квирк №94-1/№97 ЗАКРЫТ, внешнее вычитание не нужно. Notes стенда обновлены.
    (б) **🔴 ИНЦИДЕНТ: двойной live-бот на боевом аккаунте.** При рестарте live
    под 16.2.0 `systemd-run --unit=live-bot` запустил НОВЫЙ процесс (546823),
    а старый @reboot-процесс (7229, 16.1.0) остался жив — `kill 7229` в тот
    момент не отработал. ~5 мин работали ДВА --live --telegram на одном
    аккаунте (риск двойной покупки). Поймано дежурным чеком (pgrep count=2),
    владелец убил 7229 по команде. Позиций в окне не было (signals=0) —
    двойной сделки не случилось. **УРОК: systemd-run НЕ проверяет, мёртв ли
    старый процесс — перед стартом live ОБЯЗАТЕЛЕН pgrep-чек «старый мёртв»,
    иначе дубль на боевом.** Записано в [[ops-lessons-oom-earn]].
    (в) Итог: live 546823 + paper 553524, оба 16.2.0, по одному инстансу,
    тики свежие, telegram переподключён. Боты на trailing-стратегии.
    (г) **Пётр про мост — САРКАЗМ, не восторг** («тыркальник для
    администрирования портфеля»): его поинт — не текст-бот, а нормальный UI
    (uzse-app) с графиками 1m/15m/1h и ОДНОЙ кнопкой «выйти», страхующей
    деньги, вместо трёх команд; мобильный чек цены без тормозного Binance
    (habr 1049878). s1d спозиционировал верно: мост = доступ к Claude/
    терминалу, не торговый терминал. Письмо-ответ с verified-цифрами
    отправляет владелец (scratchpad/tg-letter-petr-19.md).
    (д) Миграция news-крона на сервис: пре-чек пройден (--from-service в DRY,
    Tavily 0), план 19-20 с DRY=0; fit-gate 7/10, глубина 10.9/14д.

## Session 13 (2026-07-18) — продолжение: телеграм-мост построен+запушен; фантом подтверждён и исправлен автором

97. **ФАНТОМ ПОДТВЕРЖДЁН И ИСПРАВЛЕН АВТОРОМ + ТЕЛЕГРАМ-МОСТ ГОТОВ.**
    (а) **Фантом (№96д) — ответ Петра 18.07 12:38** ([[author-answer-phantom-heatmap]],
    notes/author-answer-phantom-heatmap.md): «фантом увидел, ща накачу фикс».
    Наш разбор верен на 100% (вплоть до «seen-set защищает реплей, не
    инициировавшее событие»). Механизм его словами: эмиссия closed = сначала
    signalBacktestEmitter (Storage пишет строку) → потом signalEmitter (heat);
    первый close-тик await'ит waitForInit-реплей, который уже видит этот close
    в ленте Storage и добавляет, затем addSignal добавляет второй раз. Воспроизвёл
    регресс-тестом test/e2e/heatmap.test.mjs (2 закрытия → totalTrades 3,
    totalPnl 3×). Фикс: consume-once replayedIds в HeatmapStorage (коммит dba88cb,
    HeatMarkdownService.ts +40/-3) — addSignal глотает реплеенный id ровно раз.
    НА NPM ПОКА 16.1.0 — сторож на бамп версии взведён (фон). По релизу: апгрейд-
    цикл (merge+npm ci+рестарт раздельно) + перегон стенда (status_info станет
    честным сам: totalTrades 8, totalPnl A −5.0358/B −5.2565, 6 лоссов) + снять
    квирк. Миграций с нашей стороны НЕ нужно (фантом жил в памяти агрегата).
    Наш вывод «хуже b&h, n мал» не меняется. Отправленные раньше метрики
    (−6.44%) фантом содержали — честная A −5.04% уже была послана.
    (б) **ТЕЛЕГРАМ-МОСТ backtest-claude-telegram ЗАПУШЕН** ([[tg-claude-bridge-built]]):
    все 4 фазы приняты главной сессией фактом (мост headless claude -p с deny-
    профилем; фаза 2 выбор сессий инлайн-кнопками; фаза 3 меню setMyCommands+
    reply-клавиатура; фаза 4 сорт по последнему сообщению + скрытие коротких +
    ярлык=имя). Пуш git@github.com:highscrren-dotcom/backtest-claude-telegram
    (main, e671db3, публичный), секретов в репо 0, токен из боевого .env.
    Решение владельца: авто-память мосту ОСТАВЛЕНА на запись. Осталось: упомянуть
    Петру (scratchpad/pending-author-mentions.md).
    (в) **Системная ночная вахта (№96а закрыт):** duty-watch.mjs в user-crontab
    (23 */2), алерты в канал + утренний deadman-пинг; сессионный 2ч-крон удалён
    (дублировал). (г) ⚠️ ПАРАЛЛЕЛЬНАЯ СЕССИЯ создала notes/tg-status-bot-prompt.md
    (ТЗ на ОТДЕЛЬНЫЙ read-only статус-бот на НОВОМ боте) — пересекается с мостом,
    на реконсиляцию владельцу (НЕ закоммичен этой сессией).



96. **ЦИКЛ «СОВЕТ АВТОРА → СТЕНД B → LIVE» + находка фантом-сделки.**
    (а) Диагноз ночной вахты: машина НЕ спала, bash-монитор работал всю ночь
    честно (изменений не было), но сессионные Claude-кроны при неактивной
    сессии НЕ тикают → вахта «разбудить ночью» должна жить в системном
    cron/systemd — К ВНЕДРЕНИЮ. (б) Посты 5738/5739 = TP-отчёты, не сигналы
    (инжест чист): BTC-шорт 5734 «все 4 цели +63.5%», ETH-лонг 5737 TP1 за
    1ч04м — ОБА винера наш гейт skip'нул (n=2 вместе с лузером PENGU: гейт
    режет обе стороны распределения — данные для стенда, не вывод).
    (в) Синк upstream: e2e-пак ~7900 строк ровно по retry/reconcile;
    задокументированы waitForInit-каверзы (ЛЕНИВЫЙ; re-adopt сироты только
    при чистом getStrategyStatus) и запрет перекрытия тиков (гонка
    retry-слота → двойной open). Репо crontab переехал в org
    backtest-kit/backtest-ollama-crontab (git-redirect работает). vibe-trading
    НЕ пуллен: 154 позади, но контейнер маунтит ./frontend — только при
    остановленном сервисе. (г) Совет автора (17.07 19:44-46, «НИКОГДА не
    ставь TP/SL фикс-окном») → стенд news_jul2026 вариант B: moonbag (hard
    stop −1%) + trailing take (≥1 п.п. от пика в профите) + peak staleness
    (пик ≥1% старше 240м), константы его as-is. Та же рамка: по 8 закрытым
    A −5.04% vs B −5.26% — шум n=8; trailing take не сработал НИ РАЗУ,
    staleness отдал 0.26 п.п. на единственном крупном виннере; оба хуже b&h.
    (д) **НАХОДКА: фантомная 9-я сделка в portfolio-статистике ОБОИХ прогонов
    стенда** — totalPnl = сумма 8 закрытых + точный дубль short-SL
    −1.4034044044 (до 10 знака; A: −5.0358+φ=−6.4392 ✓, B: −5.2565+φ=−6.6600 ✓,
    в B открытых на границе нет вовсе). Квирк №94-1 ПЕРЕОБЪЯСНЁН: «MTM
    открытой по реальной цене» — неверно, открытая в totalPnl не входила.
    Отправленные Петру метрики содержали фантом (честное A: −5.04%, вывод
    «хуже b&h» не меняется). Гипотеза: лог-стрим ×2 с одним id без дедупа в
    агрегате. Черновик Петру: scratchpad/tg-message-phantom-trade.md.
    (е) **LIVE jan_2026 переведён на trailing-выходы по команде владельца**
    («не ждём, делаем сами»): канальные TP(target[2])/SL → hard stop −1% +
    trailing 1 п.п. + staleness 4ч + таймаут 24ч (был Infinity); id сигнала
    из базы СОХРАНЁН (идемпотентность clientOrderId=signalId). Классификатор
    заблокировал мой stop юнита — команды выполнил владелец (раздельно
    stop → systemd-run, урок pkill); рестарт 08:18 местного чист: транспайл
    ок, тики идут, telegram подключён, позиций в момент замены не было.
    (ж) feb paper остаётся на фикс-окне (вопрос владельцу открыт); спот-ответ:
    live торгует ТОЛЬКО лонги (шорты → OrderRejectedError спот-адаптера).

> Точка входа session 14: вахта первого сигнала (адаптер 16.1.0 + новые
> выходы разом!), реакция Петра на фантом и на внедрение его совета,
> миграция news-крона на сервис ~19-20.07 (DRY день 3/3), системный крон
> для ночной вахты (№96а), fit-gate датасета (kitco 6/10).

## Session 12 (2026-07-16 день) — возврат после офлайна: контур сам встал, но ingest снова терял посты

## Session 12 — ЗАКРЫТИЕ (2026-07-17 вечер, контекст на исходе)

95. **ЦИКЛ 16.1.0 (минор-фикс attempt) + приёмка двух параллельных сессий +
    закрытие.** (а) 16.1.0 = фикс Петра: attempt переживает падение в
    200мс-окне; принцип (его 18:56): «РКН жив: restart→6 bounded→exit→
    restart; РКН умер: новый сигнал с нуля»; его 8149e02 = бамп, мы синхронны;
    боевой бот и example на 16.1.0, бот перезапущен (тик 13:56Z), paper
    пересоздан супервизором. waitForInit-marketSell для потерянного
    мониторинга — ПРЕДЛОЖЕНО автором, НЕ делаем: он сам рекомендует руками,
    у нас сирота-детектор в дежурстве (№88-урок) + решение владельца.
    (б) ПРИЁМКА СТЕНДА (коммит 1886564): проверено руками — look-ahead
    publishedAt<=when в коде ✓, trades.jsonl = 17 событий = 8 закрытых
    (5 SL, 2 TP, 1 timeout) + 1 открытая ✓ = отчёт честен; метрики: −6.44%
    total, winRate 22%, PF 0.25, хуже buy&hold (−0.55%); движок САМ не даёт
    Sharpe при n<10 — гейт ядра подтверждает «не значимо». Скрин UI 60054 —
    владельцу до закрытия той сессии (иначе перезапуск по README ~6 мин).
    (в) ПРИЁМКА SHARPE (коммит ac0bf53): каркас честный — v2.1 зрелых n=2,
    вся таблица «н/д» при n<5, плашки на месте; ценность придёт с датасетом.
    (г) Ответ Петру №5 отправлен 17:51 (честное «не гоняли»); его реакция —
    16.1.0 и принципы; стенд-метрики можно слать вдогонку со скрином.

> Точка входа session 13: №91-95 сверху. ПЕРВОЕ: перевзвести дежурство
> («ты на страже» — 2ч-крон сессионный, умрёт с этой сессией; контур живёт
> на systemd/cron сам). Проверить: сигнал канала через новый адаптер (ГЛАВНАЯ
> вахта — attempt/reconcile в бою не проверены), news-cron 09:40 (fit-gate
> к n=7-8/10), сервис DRY день 2/3 (миграция крона ~19-20.07 по шагам из
> чата 17.07), релизы >16.1.0, реакция Петра на стенд-метрики. Стенд и
> heatmap перегоняются руками по мере роста датасета (README у каждого).

94. **NEWS-БЭКТЕСТ-СТЕНД ПРОГНАН И ЗАКОММИЧЕН (17.07 ~18:40, коммит 1886564,
    «комить» владельца).** Стенд content/news_jul2026.strategy (getSignal →
    mongo news-audit, дословная копия фильтра news_query.itemsFor, publishedAt
    <= when; последний непотреблённый ok-item окна 24ч → {direction, TP +1.5%,
    SL −1%, 24ч}; дедуп по url). Frame 06.07–17.07 12:00Z BTCUSDT, движок
    16.0.0: **9 сделок (2 TP / 5 SL / 1 timeout / 1 открыта на рамке),
    totalPnl −6.44%, winRate 22%, maxDD 8.4%, b&h −0.55% (проигрываем); Sharpe
    в UI = N/A — гейт ядра MIN_SIGNALS_FOR_RATIOS=10 («variance too noisy»),
    совпал с нашей оговоркой n=9 = дисперсия, не вывод.** Все 9 item'ов вошли
    в ту же минуту, что publishedAt — конвейер mongo→getSignal работает.
    Деливерабл: notes/news-backtest-stand.md (+снапшоты UI рядом). Квирки:
    (а) открытая на границе рамки позиция в stats оценена по РЕАЛЬНОЙ цене
    момента завершения (wall-clock), не по границе рамки; (б) две запиненных
    promptVersion (v1 7 шт + v2.1 2 шт), в запросе не смешиваются; (в) прямой
    импорт news_query.mjs в стратегию не переваривается babel-транспайлом CLI
    (import.meta) — копия фильтра с пометкой. UI жив: localhost:60054 (процесс
    VSCode-сессии, умрёт с ней — скрин владельца ДО закрытия; перезапуск —
    команда в README стенда, ~6 мин). Метрики UI снимаются и curl'ом:
    POST /api/v1/view/status_info. Параллельная sharpe-сессия: ac0bf53 её,
    правки news_query (+eventType) и её DECISIONS-запись — за ней.

93. **Пётр анонсировал минорный фикс attempt-семантики (17.07 18:15, ждём
    пуш):** инкремент attempt переживает падение системы ДО получения
    HTTP-ответа (окно ~200мс «запрос улетел, ответ не вернулся») — после
    рестарта attempt>0 гарантирован, и железное правило адаптера («сначала
    спроси биржу по clientOrderId, только потом шли заново») сработает и в
    этом кейсе. НАШ АДАПТЕР УЖЕ ГОТОВ: reconcile-ветка при attempt>0 из №92 —
    ровно эта логика (сошлись в дизайне независимо; его скрин = наш код).
    По его пушу: обычный минорный цикл (merge+npm ci+рестарт), правок
    адаптера не потребуется. Также: ответ №5 автору отправлен владельцем
    17:51 (честное «бэктест не гоняли» + план стенда), listenExit убран и
    запушен (1bb4b43), стенд-сессия и Sharpe-heatmap — промты готовы.

92. **ЦИКЛ 16.0.0 ПРОЙДЕН + АДАПТЕР ПЕРЕПИСАН ПОД RETRY-API (17.07 ~17:00,
    OK владельца на диff).** Релиз 16.0.0 = retry-API из №91 (+e2e-тесты в
    ядре). Сделано: движок/CLI 16.0.0 в ollama-crontab и example; сиблинги
    синкнуты (+1 везде); paper перезапущен; **live.module.ts переработан
    (135 строк, наш слой)**: toTypedError (ccxt NetworkError→Transient,
    ExchangeError→Rejected), вход с clientOrderId=signalId, при attempt>0 —
    reconcile ДО отправки через privateGetOrder(origClientOrderId) (НАХОДКА:
    duplicate-ловушка Binance не работает для мгновенно исполненных ордеров —
    id уникален только среди открытых; сообщить автору), шорт/qty=0 →
    Rejected (дроп без 5 попыток), close с классификацией, listenExit.
    Дефолты CC_ORDER_*_RETRY_ATTEMPTS=5 не трогали (канон). Верификация:
    esbuild чист, tsc-шум только в старом коде, бот перезапущен — тики идут
    (17:05). brokerdebug-модуля в репо нет — прогон нового пути будет на
    первом сигнале канала (внимание дежурству!). Не закоммичено: адаптер,
    №90-92, ANSWER.md — ждёт «комить».

91. **ОТВЕТ АВТОРА НА ФИДБЕК №3 — НОВЫЙ API УСТОЙЧИВОСТИ ОРДЕРОВ (17.07,
    ANSWER.md сохранён: [notes/author-answer-retry-api.md](notes/author-answer-retry-api.md)).**
    Закрывает разом п.10, п.1, п.5: (а) три канала биржи получили bounded-ретраи
    (CC_ORDER_OPEN/CLOSE/CHECK_RETRY_ATTEMPTS, дефолт 5, 0=legacy) + attempt в
    payload; (б) триада типизированных ошибок: OrderTransientError (сеть,
    ретрай) / OrderRejectedError (отказ навсегда: open→дроп, close→force-close
    сразу — наш кейс шорта об спот!) / OrderDeletedError (ордера нет →
    терминально); распознавание по Symbol.for-бренду, не instanceof;
    (в) **identity-stable retry: повтор с ТЕМ ЖЕ signalId, слот персистится
    через крэш** — и КЛЮЧЕВОЕ: наш clientOrderId=signalId стал ОФИЦИАЛЬНЫМ
    рецептом адаптера (duplicate→getOrderByClientId→filled→return) — сценарий
    PENGU разруливается без человека; (г) пинги толерантны к сети (обрыв
    больше не убивает позицию — наш №50-класс); (д) listenExit для
    супервизора; (е) п.6 фидбека (rate-limit логов) — через attempt===0.
    За адаптером (§8): clientOrderId+reconcile, выверка после force-close,
    классификация permanent/-1121. Кейс №9 (ротация символов) НЕ решён —
    отдельная тема. ПЛАН по пушу автора (он напишет): (1) апгрейд-цикл с
    preflight; (2) переписать live.module.ts по рецептам §1-3 (наш слой!);
    (3) setConfig трёх ретраев (проверить дефолты); (4) listenExit→telegram;
    (5) brokerdebug сухой прогон; (6) наблюдение на следующем сигнале канала.
    Статус: ждём пуш.

90. **PENGU-сага ЗАКРЫТА: стоп-нога OCO исполнилась (17.07 13:36, 15,914 шт
    @ 0.006007) — итог −$4.4 (−4.3%), депозит $96.51.** Сигнал канала сам
    оказался лузером (его стоп пробит); форвард-модель говорила invert short
    и по НАПРАВЛЕНИЮ была права (в своём реплее, впрочем, «нет входа»).
    **Ответ автора по п.10 (12:22): «не баг движка — нарушена семантика
    onOrderOpenCommit: коллбеки ждут бизнес-ошибки, сеть исключает
    реализация»**; его контракт: (1) стабильный signalId ИЗ БАЗЫ (у нас УЖЕ
    так — id screen-строки), (2) for-ретрай сети внутри хука, исчерпали →
    console.error + process.exit(-1); «продавца надо искать» — императивный
    reconcile на адаптере; не нашёлся — getSignal с НОВЫМ id (актуализация
    цены). Обещал пример кода брокера или апиху ретраев. НАШ РАЗБОР: хук —
    проектный слой (live.module.ts:200, чиним не трогая ядро); сеть течёт
    в 3 местах (buy-wait, TP/SL, unwind); тонкости к его примеру:
    (а) после exit(-1)+рестарта нужен reconcile на старте, иначе цикл
    повторится; (б) clientOrderId=signalId превращает «поиск продавца» в
    точный fetchOrder — предложить; (в) серая зона: RequestTimeout после
    отправки и InsufficientFunds-как-следствие — reconcile, не слепой
    ретрай. РЕШЕНИЕ ВЛАДЕЛЬЦА: ждём его код, сами не чиним («дождусь тебя»).

89. **🎉 PR #1 ВЛИТ АВТОРОМ (17.07 ~12:00) — МЫ КОНТРИБУТОРЫ** его
    backtest-ollama-crontab (4edb889, merge без комментариев). Фикс забран
    в наш integration (merge upstream), live-бот перезапущен на нём —
    per-row try/catch теперь в проде: сбой строки не валит цикл и не
    оставляет item полуобработанным. Также за день: news-service ЗАЛИТ на
    github (highscrren-dotcom/news-service, скан секретов чист) — обкатка
    в DRY 2-3 дня, план миграции крона в README/чате (№ шаги 0-2, инвариант
    «Tavily зовёт либо крон, либо сервис»); крон 09:40 — первый полный
    прогон v2.1+mongo-sync зелёный (+36 новостей, upserted=36, ok=10,
    fit-gate n=6/10 depth 9.9/14 — фит близко); сеть весь день рвётся
    (1160 NetworkError, бот держит). Грабли №3 pkill: паттерн совпал с
    exec-строкой systemd-run в ТОЙ ЖЕ команде (бот убит, шелл убит,
    перезапуск не выполнился — восстановлен отдельными командами); правило
    ужесточено в памяти: kill и запуск — только раздельными командами.

88а. **Позиция-сирота ВЗЯТА ПОД КОНТРОЛЬ (17.07 ~09:00): OCO выставлен мной
    по прямой команде владельца («выполни все сам»)** — orderListId
    23491963972: тейк 0.006334 (TP1 канала, +1%) / стоп 0.006009, лимит
    0.005980 (−4.6%). Скрипт: backtest-ollama-crontab/scripts/oco-pengu-5736.mjs
    (одноразовый). Ночная хронология: стоп канала пробивался (низ ~0.00600),
    к утру цена вернулась к входу. Обе инверт-оценки форварда (BTC 5734
    invert long, PENGU 5736 invert short) созрели: «нет входа» в реплее —
    модель в перевёрнутых зонах налив не получила. Ответ автора (16.07
    21:49-56) разобран: (а) PR он искал не в том репо — дать ссылку на
    backtest-kit/backtest-ollama-crontab/pull/1; (б) новое ТЗ: вынести забор
    новостей в отдельный python-сервис (его main.py + swagger + docker) —
    ждём файл main.py из телеги от владельца; (в) методология валидации:
    НЕ LLM-confidence, а Sharpe-heatmap прогноза против последующего движения
    цены (>1.0 = источник угадывает) — ложится на mongo-worker. Урок ночи:
    awk+LC_NUMERIC=ru_RU сравнивает нули (ложный TP1-алерт) — мониторы только
    на node; в памяти.

88. **🔴 ПЕРВАЯ БОЕВАЯ ПОКУПКА — И СРАЗУ ИНЦИДЕНТ: позиция-сирота PENGU $99
    без стопа (ночь 17.07).** Пост 5736 (PENGU long 18:02Z) прошёл весь
    конвейер: ingest ✓, гейт follow ✓ (momentum +1.93%), стратегия сработала —
    18:16:09Z маркет-бай исполнен (15,930 PENGU @ 0.00627, $99.88, 3 филла,
    fee в PENGU). НО сигнал не зафиксировался (signal-items=0, position-пингов
    0): ответ ордера, судя по всему, потерян сетью (рядом 3× NetworkError
    POST /order «other side closed») → движок счёл покупку неудачной → 114×
    повторных попыток купить (InsufficientFunds, денег нет — только это спасло
    от даблбая) до конца 4ч-окна 22:02Z. ИТОГ: на бирже живая позиция ~$99 БЕЗ
    стоп-лосса/тейка, движок о ней не знает; free USDT $1.01. Фидбек п.10
    (идемпотентность clientOrderId + reconcile). Дежурство: ценовой монитор
    взведён (алерт у SL 0.006009 и TP1 0.006334), пуш владельцу не дошёл
    (Remote Control неактивен) — решение владельца утром: продать / ручной
    OCO (SL+TP как задумано каналом) / держать. Форвард-модель, к слову, дала
    по 5736 invert short — третий инверт; если она права, стоп важнее тейка.
    Побочно: EVALUATE инверта BTC 5734 созреет ~вечером 17.07.

87. **ИНЦИДЕНТ: делистинг HYPEUSDT (16.07 ~12:00Z) ослепил live-бота на 2ч**
    — движок на каждом тике зависал в ретраях -1121 Invalid symbol до
    900с-вотчдога (бот видел рынок ~3 мин из 16; 7 зависаний с 17:07, поймано
    дежурным тиком 18:39 по несвежим тикам). ФИКС (команды выполнил владелец;
    классификатор корректно заблокировал мою автономную правку боевого .env):
    `CC_SYMBOL_LIST` без HYPE в .env (код автора не тронут — дефолт-лист
    остаётся в packages/main), бот перезапущен systemd-юнитом. Проверено:
    тики каждую минуту, HYPE отсутствует, новых вотчдогов 0. Фидбек п.9
    (fail-fast на -1121 + исключение символа из ротации с уведомлением).
    Урок: делистинг — новый класс событий для дежурства; чеклист уже ловит
    его по несвежести тиков. Также в этом тике: канал max=5733 (без новых
    постов), релиз 15.5.0 без изменений, PR #1 без реакции.

86. **Worker принят (проверка главной сессией, оверклеймов нет) и закоммичен
    (e9cb7b5); решения по 3 вопросам строителя: ключ url оставляем (расширение
    до (url,promptVersion) — только при реальной переклассификации истории),
    дефолт promptVersion=самая свежая оставляем (канон «не смешивать»; для
    фитов версия явно), backfill копим честно (основной фит — не-backfill,
    included только sensitivity-check). Сообщение автору №4 подготовлено**
    (scratchpad tg-message-mongo-worker.md; отправка владельцем): worker по
    канону + v2.1 + два вопроса (ключ аудита; псевдо-каналы анлоки/funding) +
    фидбек №3 (8 пунктов, ссылка) + пинг PR #1. Проверить завтра после 09:40:
    строка [mongo-sync] в news-cron.log (в дежурном чеклисте); после зелёного
    прогона владельцу удалить ~/crontab-backup-2026-07-16.txt.

85. **News→mongo worker ПОСТРОЕН по ТЗ №84 (сессия-строитель 16.07, не
    закоммичено).** Два скрипта в `example/scripts/news_dataset/`:
    `news_mongo_sync.mjs` (идемпотентное зеркало journal→`news-audit`.verdicts,
    localhost, НЕ backtest-pro; upsert по url unique + индексы (symbol,publishedAt)/
    (domain,publishedAt); ВСЕ вердикты вкл. rejected; mongoose через createRequire
    из ollama-crontab) и `news_query.mjs` (`itemsFor({when})`: status ok,
    **publishedAt ≤ when**, backfill исключён по умолчанию, одна promptVersion —
    look-ahead невозможен по построению; ни к чему не подключён). Полный синк:
    127/127, повтор = modified 0; БД 128KB. Смоук when до/между/после: 0→1→2
    items (kitco 21:31Z отрезан при when 15:00Z). Нюансы: fetchedAt взят из
    collectedAt raw (честнее ТЗ-фолбэка classifiedAt, разница минуты);
    tavilyScore уже был в raw (score) — collect не тронут; все 127 записей =
    v1. Крон-строка стала 4-шаговой (README) — обновляет владелец. Открытые
    вопросы: unique-url vs переклассификация новой версией промпта (сейчас
    конфликта нет — журнал идемпотентен по url), полуночные даты в зеркало
    не попадают вовсе (collect режет раньше), судьба v1-затравки при фите.
    Схема/канон — [notes/news-dataset/README.md](notes/news-dataset/README.md).

84. **Три решения владельца по vibe-отчёту (16.07): (1) v2.1 → БОЙ; (2) фидбек
    по рекомендации; (3) mongo-worker — ДА.** Сделано: (1) боевой
    `news_classify.mjs` переведён на v2.1 (схема +event_type/reason, промпт с
    якорями, `promptVersion="v2.1-vibe-2026-07-15"` в журнал; старые записи без
    поля = v1; синтаксис + no-op прогон чисто; крон-строка без изменений;
    отдельные *_v2*.mjs остаются как история эксперимента). (2) Рекомендация
    принята к исполнению: кандидаты №1 (fallback-цепочка бирж) и №2+3 (канон
    publishedAt: подтверждение «knowable date» + поле promptVersion против
    дрейфа) перенесены в author-feedback-3 (п.7, п.8); кандидат №4
    (псевдо-каналы анлоков/funding) НЕ в фидбек — это вопрос автору, задать
    вместе с рассказом про mongo-worker в следующем сообщении. (3) ТЗ
    сессии-строителя: [notes/news-mongo-worker-prompt.md](notes/news-mongo-worker-prompt.md)
    — база `news-audit` (НЕ backtest-pro!), verdicts upsert по url, два времени
    (publishedAt/fetchedAt) + backfill-флаг, promptVersion, read-side
    `itemsFor({when})` с невозможным look-ahead; node, не python (суть канона —
    mongo-контракт; отметить автору).

83. **Ребут 16.07 16:26 — контур поднялся БЕЗ нас** (@reboot: live-бот и
    paper-супервизор с --ui-фиксом; systemd-юниты inactive после ребута =
    НОРМА, канонический запуск — cron). **ИНЦИДЕНТ №2 catch-up скрейпа**
    (улика дописана в фидбек п.2): за офлайн ~сутки канал дал 5731 (TP-отчёт),
    5732 (TAO short), **5733 PENGU ЛОНГ (08:02Z) — потерян полностью**: после
    рестарта в Mongo не попал ни один (ingest слушает только новые сообщения,
    историю не догоняет), 4ч-окно истекло до обнаружения. В дежурный чеклист
    (перевзведён: 2ч в :23, job 428c5e25) добавлена сверка t.me/s/ ↔ Mongo
    max(messageId). News-крон 09:40 тоже был пропущен (ПК выключен) — догон
    вручную (collect week, 8 кредитов): +11 новостей, все 11 отсеяны честно
    (no_symbol, общемакро) → датасет без изменений (items=8, kitco n=4/10,
    depth 7.2д, свежайший item 13.07 — отсев работает, копим дальше).
    Синк: +2 коммита backtest-kit (docs+patch 15.07) смержены, релиза нет
    (15.5.0); PR #1 открыт, реакции автора нет; в live-логе ×2 нового
    вотчдога 15.5.0 («live-fetch-data >120s») — наблюдаем. Vibe-отчёт
    session 11 принят к сведению, три решения владельца в конце
    [notes/vibe-mining/report.md](notes/vibe-mining/report.md): v2.1 на крон?
    / перенос 4 кандидатов в фидбек / ТЗ mongo-worker.

## Session 11-vibe (2026-07-15) — майнинг Vibe-Trading + дежурство + плановое выключение

82. **Плановое выключение машины владельцем 15.07 (~вечер) на 1–2 дня.**
    Восстановится само после включения: docker (mongo `node-ollama-agent-swarm-
    mongo-server` restart=always, redis, pgcluster; vibe-trading и hummingbot
    unless-stopped), @reboot-crontab (live-bot и paper-feb-супервизор, sleep 90),
    системные кроны (forward :25, volume */15, news 09:40, drift :40). НЕ
    восстановится: сессионный 2ч-дежурный крон — перевзвести в новой сессии
    («ты на страже»). Заметки владельцу: (1) открытая live-позиция при
    выключенной машине не управляется (SL/TP не исполнятся) — проверить
    TG/дашборд :60050 ПЕРЕД выключением; (2) news-датасет получит дыру за дни
    простоя (окно day) — после включения можно разово `news_collect.mjs week`
    для добора (дедуп по url защитит, те же 8 кредитов); (3) контейнер
    vibe-trading запущен НЕ vibe-сессией (Up с ~23:00 14.07, restart=
    unless-stopped) — гасить ли, решение владельца; (4) статистика paper-feb
    за простой будет дырявой (как 08–13.07). Закоммичено двумя коммитами
    (данные кронов; vibe-майнинг+DECISIONS) и запушено в origin/master
    («пуш тоже все в меин» — владелец, 15.07 ~15:25).

81. **Дежурство session 11: контур зелёный весь день** (журнал —
    [notes/vibe-mining/watch-journal.md](notes/vibe-mining/watch-journal.md)).
    Ночью 03–05ч эпизод транзиентных fapi-фейлов binance («other side closed»,
    ~6 шт → ~900KB стек-трейсов в live-лог за 2ч — материал к п.5 фидбека №3);
    live-bot пережил, тикал без пропусков. News-крон 15.07: +36 url, **ok=0/36**
    — проверено глазами: нулевой крипто-день (сплошь золото/ФРС/акции/нефть),
    fit-гейт честно стоит (top-domain n=4/10, depth 7.2/14д). Метод проверки
    тика paper-feb без лога (лог пишет только события): CPU/rchar-дельта
    процесса за ~2 мин против live-контроля.

80. **Vibe-Trading выпотрошен по трём одобренным пунктам (№75/№76б),
    деливерабл — [notes/vibe-mining/report.md](notes/vibe-mining/report.md).**
    (а) ПРОМПТЫ: настоящий промпт у Vibe один — `skills/event-driven/SKILL.md`
    (якорёная шкала −1..+1, «knowable date» = наш publishedAt-канон их руками,
    дрейф скоринга при смене промпта/модели); построены v2 и v2.1 (боевой
    `news_classify.mjs` НЕ тронут, скрипты `*_v2*.mjs` рядом), два A/B по 80
    новостей против v1: v2 в лоб голодоморит топ-канал kitco (ok 8→4, TA-статьи
    в null), **v2.1 — рекомендация**: ok=8 как v1, agreement 96%, confidence
    калиброван 0.3–0.85 (v1 — кластер ~0.72), + аудит-поля event_type/reason/
    promptVersion. Переключение крона 09:40 на v2.1 — решение владельца.
    (б) FALLBACK-ЦЕПОЧКИ: ядро в их `backtest/loaders/registry.py` (цепочка на
    РЫНОК по IP-ban-риску, дешёвый is_available-предикат, честный fail со
    списком tried, запрет тихой деградации) — кандидат №1 в фидбек автору
    (всего 4 кандидата в отчёте, в author-feedback-3 НЕ перенесены).
    (в) КАТАЛОГ liq/funding/stablecoin/unlocks: у Vibe кодовых интеграций НЕТ
    (только skills-инструкции агенту); бесплатное без ключей — OKX public API
    (funding/OI), DeFi Llama, tokenunlocks.app; развилка: числовые ряды требуют
    «событизации» с фитуемым порогом (риск переобучения) vs календарь анлоков =
    готовые события, но редкий поток. + заготовка требований news→mongo-worker
    (publishedAt И fetchedAt, backfill-флаг ретро-затравки, promptVersion,
    вердикты не перезаписывать) — в отчёте, mongo-worker отдельной сессией.

## Session 10 (2026-07-14 ночь) — дежурство: вскрыт и вылечен crash-loop paper feb_2026

74. **Paper feb_2026 находился в crash-loop с 08.07 (3941 рестарт, до 958/день):
    headless-paper НЕ ЖИВЁТ по дизайну движка.** Процесс тихо выходил с кодом 0
    через ~25-35с (event loop иссякал, `--trace-exit` пуст, unhandledRejection
    нет). Раскопки в два слоя: (а) при цикле 15.4.0 12.07 21:34 подняли
    `example/package.json`, но `npm install` НЕ прогнали — движок в node_modules
    оставался 15.2.0 (донесено `npm ci`, теперь 15.4.0); (б) КОРЕНЬ: свечной
    таймер движка намеренно `unref()` (build/index.mjs:23103, «Do not keep the
    process alive just for the candle clock») — процесс держат только боковые
    сервисы (`--ui`/`--telegram`). Live-бот потому и жив. «5ч-окна жизни» feb —
    ретраи news-advisor'а случайно держали loop. ФИКС: `run_paper_feb.sh`
    теперь с `--ui` на `CC_WWWROOT_PORT=60052` (60050 занят live-ботом),
    супервизор перезапущен systemd-юнитом `paper-feb` (transient; @reboot-строка
    в crontab остаётся для ребута). Проверено: тики идут ежеминутно, процесс
    живёт. Данные paper целы (`--noFlush`), но статистика feb 08–13.07 дырявая
    (тики шли только в короткие окна) — интерпретировать с оглядкой. Кандидат
    в фидбек №3 п.6: headless paper умирает молча — предложить warning/ref.
    Уроки: (1) проверять установку фактом (`node_modules/*/package.json`),
    а не package.json; (2) pkill-паттерн экранировать (`index[.]mjs --paper`) —
    self-match убил мой shell ВТОРОЙ раз (№62 не выучен).

75. **Форк HKUDS/Vibe-Trading изучен, вопрос-предложение автору подготовлено;
    AI-Trader-бенчмарк = подтверждение канона «Claude ≠ стратег» их же
    руками.** Vibe-Trading (21.5k⭐, HKU lab) склонирован в амбреллу
    (`../vibe-trading`, upstream добавлен): research-workspace, LLM-стратег
    (против канона), майнить точечно — news-промпты (skills sentiment-analysis/
    event-driven) для нашего news_classify, fallback-цепочки по IP-ban-риску,
    liquidation-heatmap/funding как данные. Сообщение автору (блок для TG,
    scratchpad) — отправка владельцем; ждём его вердикта, до этого Vibe не
    трогаем. AI-Trader (арена ai4trade.ai, arXiv:2512.10971, 6 LLM, окт–ноя
    2025, $10k): US — MiniMax-M2 +9.56% vs QQQ +1.87% (НО издержки не
    моделированы вовсе, 1.2–6 сделок/день); A-shares — индекс не обыграл
    никто; крипта — ВСЕ шесть −12..−19%. Их alive/reversed/dead ≡ наш
    channelPlan enter/drop/invert. Вывод владельцу дан: альфы там нет,
    ценность — методология и инфраструктура.

## Session 10 — ЗАКРЫТИЕ (2026-07-14 ночь)

79. **PR #1 СОЗДАН владельцем и открыт** (первый PR в истории
    backtest-ollama-crontab!) — ждём ревью/мерджа автора; после мерджа
    забрать фикс штатным синком upstream→integration. **Сессия закрывается**,
    следующая — майнинг Vibe-Trading по промту
    [notes/vibe-mining-prompt.md](notes/vibe-mining-prompt.md) (три
    одобренных пункта: v2-промпт классификатора с A/B, fallback-цепочки,
    каталог liq/funding-источников; mongo-worker — отдельной сессией после).
    ⚠️ С закрытием умирает сессионный 2ч-дежурный крон — контур живёт на
    системных кронах/юнитах (forward :25, volume */15, news 09:40,
    drift-alert :40, live-bot + paper-feb systemd); перевзвести дежурство
    в любой главной сессии («ты на страже»). Незакоммичено (ждёт «комить»):
    DECISIONS №74-79, author-feedback-3 п.6, run_paper_feb.sh (--ui фикс),
    vibe-mining-prompt.md, news-датасет данные за 14.07.

> Точка входа session 11 (главная): №76-79 + ответ автора в №76. Проверить:
> судьбу PR #1, релизы (>15.5.0), отчёт vibe-сессии в notes/vibe-mining/,
> news fit-гейт (n≥10 + 14д → фит отдельной сессией), shadow (≥20
> сделок/ветку → сводка), перевзвести дежурство.

## Session 10 (2026-07-14 вечер) — ответ автора: путь в контрибуторы + канон news-worker

78. **Цикл 15.5.0 ПРОЙДЕН («апдейт обязателен» — владелец, 14.07 ~23:30):**
    PR-коммит 8907ee5 запушен в ветку `fix/live-row-visited-retry` форка
    (создание PR — за владельцем, ссылка выдана). Синк: ollama-crontab merge
    upstream (75ebacc) + npm ci → движок/CLI 15.5.0, mongo 5.5.0; preflight
    зелёный (ключи валидны, USDT 100.89, TG-бот жив); live-bot перезапущен
    23:33, тики идут. paperhands merge upstream (minio-пакет, тесты, example
    уже на 15.5.0 от автора) + npm ci в example + paper-feb перезапущен
    (тикает, оба UI-порта 60050/60052 слушают). Сиблинги: redis-mongo(+3),
    pgpool(+1), monorepo(+1), uzse(+1) подтянуты; pump/volume/garch/trading-
    agents без изменений. НАШ integration try/catch-фикс НЕ содержит — ждём
    мерджа PR автором и заберём через штатный синк (канон «как у автора»).

76. **Ответ автора (14.07 10:31–10:49) на vibe-сообщение и фидбек п.1 — три
    пункта:** (а) потеря сигнала: «mongo — не часть движка backtest-kit,
    атомарность не гарантируется», прод у него пишется отдельно → баг чинится
    в примере, не в ядре; (б) майнинг Vibe-Trading — «ОТЛИЧНАЯ ИДЕЯ» + канон
    интеграции новостей: **python-worker пишет аудит новостей в mongo с
    publishedAt, стратегия читает через getSignal(..., when) — виртуальное
    время бэктеста** (у нас этот паттерн уже жив: SignalMainService.
    getLast4HourSignal(symbol, when) → publishedAt ≤ when; наш news-датасет
    останется look-ahead-safe при переносе jsonl→mongo); (в) **ПРЯМОЕ ТЗ:
    «там где visited:true ошибочно — добавь try/catch и сделай Pull Request,
    добавишься в список контрибуторов»** — первое приглашение в контрибуторы.

77. **PR подготовлен (ЖДЁТ OK владельца на коммит+пуш):** ветка
    `fix/live-row-visited-retry` в worktree `/dev/quant/.worktrees/pr-visited-retry`
    от upstream/master (3ebdcb9), diff — per-row try/catch в RUN_LIVE_FN
    SignalJobService: сбой строки НЕ валит цикл для остальных и НЕ оставляет
    полуобработанной (visited остаётся false → ретрай следующим прогоном),
    warn-лог с rowId/symbol. tsc --noEmit чист. Скоуп честный: чинит ногу
    parser→gate→screen; ногу стратегия→движок (где реально умер WLD 12.07)
    не покрывает — она в ядре, в PR-тексте оговорено. Рабочее дерево живого
    бота НЕ тронуто (worktree). Сопутствующее за день: **релиз 15.5.0**
    (автор бампнул свой прод 12:45; наш бот на 15.4.0 — апгрейд = решение
    владельца, с preflight как в 15.4.0-цикле); пост канала **5726 PENGU
    short** (16:25Z): форвард отсёк momentum-gate, live — спот-адаптер
    штатно отбил шорт (лог-спам 5867 строк — п.5 фидбека №3 пухнет);
    news-крон 09:40 отработал сам (fit-гейт n=4/10, depth 7.2/14д).

## Отдельная сессия (2026-07-13 вечер) — конвейер NEWS→ParserItem (исполнение ТЗ №71)

73. **Конвейер «новости → ParserItem-датасет» ПОСТРОЕН и смоук пройден
    (без коммитов).** Три скрипта в `example/scripts/news_dataset/`
    (collect: Tavily пул-13, 4 clean-класса ETF/FOMC/крах/регуляторка, без
    порога, дедуп по url; classify: Ollama `minimax-m2.7:cloud` строгим
    JSON `{symbol,direction,confidence}`, только текст — без цены/даты,
    идемпотентно, отсев со счётчиками, Binance-листинг кэшем; dataset:
    ParserItem channel=домен, ts=publishedDate, id=url). Данные и README
    с крон-строкой — [notes/news-dataset/](notes/news-dataset/README.md);
    крон ставит владелец (8 кр/день Tavily). Смоук (week-затравка):
    45 собрано → 6 items / 87% отсев — и это фильтр шума в действии
    (общемакро без крипто-актива → null; джекпот-статья kitco = обзор без
    главного актива → тоже отсев). Санити 6/6 направлений глазами ок.
    Fit-гейт печатается каждым прогоном: сейчас top-domain n=3/10,
    depth 4/14д → фит pump-anomaly ЖДЁТ накопления (отдельной сессией).

## Session 9 — ЗАКРЫТИЕ (2026-07-13 вечер, контекст на исходе)

71. **НОВОЕ ТЗ АВТОРА (13.07 12:43, ответ на карту источников): «маппинг
    новостей под ParserItem»** — «много источников не значит, что это не
    шум»; direction добывать LLM'ом (ollama); когда соберётся датасет —
    «поставь pump-anomaly искать закономерности». Замысел: домен = псевдо-
    канал → channelScore/channelPlan сами разложат источники на шум/сигнал;
    порог 0.68 в этом пути не участвует. ПЛАН: конвейер news_collect
    (Tavily пул-13, все датированные, без порога) → news_classify (Ollama:
    symbol+direction строгим JSON) → news_dataset (ParserItem, channel=домен)
    → копить кроном (строку ставит владелец) → фит отдельной сессией при
    n≥10/домен и 2+ неделях глубины. Промт сессии-строителя:
    [notes/news-dataset-prompt.md](notes/news-dataset-prompt.md). Бюджет
    Tavily ~8 кр/день + 2 монитора. Это НОВЫЙ ПРИОРИТЕТ №1 (прямое ТЗ).
72. **WLD-возврат (№69) заблокирован конвейером глубже visited:**
    SignalJobService.RUN_LIVE_FN:52 — существующий screen-item → continue
    (даже без обратного markVisited) → сброс visited недостаточен. Полный
    возврат = удалить screen-item 6a53dd4478b7b00f68c0fc35 (WLDUSDT) из
    боевой Mongo. **РЕШЕНИЕ ВЛАДЕЛЬЦА (13.07 ~21:20): в базу не лезем, TTL
    истёк сам в 23:18** — эксперимент закрыт как урок об одноразовости
    конвейера (пункт 1 фидбека №3). Максимум из него выжат: блокер найден
    на уровне кода (RUN_LIVE_FN:52); сброс visited можно не откатывать
    (screen-item держит скип); WLD остаётся неторгованным везде, кроме
    shadow-леджера. Ответ автору по ТЗ №71 отправлен владельцем ~22:00
    (текст — в истории чата session 9).

> Точка входа session 10: этот блок + №68-70. ПЕРВОЕ ДЕЛО: разобрать ответ
> автора (владелец получил 13.07, в этой сессии НЕ разобран — спросить у
> владельца текст). Затем: проверить исход WLD-эксперимента (№69) в логе
> live и перевзвести дежурство (2ч-крон + монитор ордеров — сессионные).

68. **Сага WLD-лонга (5722, первый торгуемый лонг канала) — сигнал ПОТЕРЯН
    конвейером на сетевом сбое:** 12.07 23:28 гейт пропустил (follow,
    momentum +1.90%), 23:39 обрыв сети (TG reconnect + ccxt-фейлы) убил
    создание сигнала стратегией; item остался visited:true → без ретрая.
    Cron-таймауты 15.4.0 конвейер СПАСЛИ (тики непрерывны — прошлый такой
    сбой = клин №50), но per-item обработка одноразовая. **КАНДИДАТЫ В
    ФИДБЕК АВТОРУ №3:** (а) потеря item на сбое между гейтом и стратегией
    (visited без ретрая); (б) catch-up скрейпа не догоняет посты за офлайн
    (5719 ETH short и 5720 RDW не попали в Mongo); (в) новый дрейф шаблона:
    «Вход в диапазоне» без слова ЛОНГ/ШОРТ (5720) + хвост «- 5% депозита»
    в строке диапазона (парсится, но следить). Плюс позитив: 15.4.0
    таймауты проверены боем — доложить автору.
69. **РЕШЕНИЕ ВЛАДЕЛЬЦА: WLD-сигнал возвращён в конвейер** (visited:false,
    13.07 ~19:05) — «посмотрим как отработает». На момент сброса: цена
    0.4157 (в зоне 0.40-0.42), momentum24h −0.953% — на волоске от порога
    гейта −1% (вердикт может быть и skip). TTL поста до 23:18 местного.
    Вотчеры взвешены в session 9 (сессионные, умрут с ней): исход смотреть
    в live-ingest.log (grep WLDUSDT после строки ~62400) и market-buy.
    Форвард по WLD: модель дала ПЕРВЫЙ сигнал за историю — invert short
    (против лонга канала!), реплей честный: «нет входа» (разметка не
    налилась). Уточнение №67: у модели есть per-signal invert (не только
    channelPlan) — action=invert в plan().
70. **Наш баг: forward EVALUATE реплеил инверт по направлению ПОСТА, а не
    СИГНАЛА** — починено (r.signal.direction), ошибочная WLD-строка
    переоценена. Коммит вместе с этим закрытием.

## Session 9 (2026-07-12 ночь) — shadow-леджер: фьючи и инверт в тени

67. **РЕШЕНИЕ ВЛАДЕЛЬЦА: варианты «фьючерсы» и «инверт» меряем ТЕНЬЮ, не
    торгуя** — `pump_bench/shadow.mjs` (автозапуск хвостом ежечасного
    форвард-крона; observation-only). Три ветки на каждый пост канала после
    заморозки модели (ts>2026-07-07): A = planForAt as-is («модель+фьючи»),
    B = planForAt flipped («модель+channelPlan-инверт»), C = model-free
    replay по разметке поста («сырой канал+фьючи», механика аудита каналов).
    ФАКТЫ, вскрытые при постройке: (а) **planForAt гонит вердикт через тот
    же конвейер гейтов** (buildSignal) — null ≠ «нет филла», это отказ
    модели; №25 «planForAt не гейтится» относился только к сертификации;
    (б) **входной инверт — не переключатель**: channelPlan назначается
    статистикой фита (Yoda = enter: score +0.0055, WR 58.4%, n=147),
    выходной каскад-инверт УЖЕ включён (squeezePolicy:"invert").
    **Первая таблица (6 июльских постов):** A: 0 сделок (momentum-gate,
    |momentum| 0.09–0.88 < 1.18; PENGU гейты прошёл — вход не налился);
    B: 0 сделок (зеркальные моменты тоже < порога — инверт при этих
    моментах модель не торгует); C: **3/3 TP1, +0.81%/сделку сырыми,
    +0.41% после haircut** (n=3 — не статистика, но первая точка в пользу
    «шорты канала кормят»; 3 поздних поста дозреют в кроне).
    Гейты решений: ≥4 недели или ≥20 сделок/ветку; C в плюс нетто →
    предметный разговор о futures live; B в плюс → РЕФИТ (channelPlan
    решает сам), никакого форсирования.

## Отдельная сессия (10–11.07.2026 ночь) — карта источников, ВОЛНА 2

65. **Волна 2 карты источников ВЫПОЛНЕНА (93 кредита из ≤150; сумма двух волн
    229; без коммитов).** Секции 5–7 + записка автору v2 —
    [notes/tavily-sources-map.md](notes/tavily-sources-map.md), сырьё —
    notes/tavily-raw/sources2_*. Главное: (а) **джекпот kitco-ETF-clean
    стабилен 3/3** (заходы 23:57 и 06:30 через 6.5ч + финал в-1; скор 0.689
    детерминирован до тысячной) — но это ОДНА статья 06.07, живущая в
    недельном окне: частота новых пробитий не доказана, нужен мониторинг
    недель; (б) +17 датированных мейнстрим-доменов (apnews 0.270 лучший;
    axios/cbsnews/newsweek/aljazeera самые свежие), новых крипто-СМИ с
    датами НОЛЬ — гипотеза «датированный индекс = мейнстрим» закрыта;
    (в) ⚠️ состав пула может переключить индекс: пул-21 стабильно флипал в
    веб-индекс (даты 0, выживших 0) при работающих top7/пул-13/max30
    (выдача идентична, разбавления нет) → правило: новый состав аллоулиста
    валидировать 1 пробой до деплоя, монотонности нет; (г) финальная
    рекомендация: **пул-13 волны 1 без изменений, стиль только clean**
    (instruction нигде не взял порог и единственный нестабилен между
    заходами). Фоновый заход 2 пережил рестарт VSCode (nohup + вотчер).
    Отправка записки v2 — за владельцем.
66. **РЕШЕНИЕ ВЛАДЕЛЬЦА: волна 3 по доменам ПРИОСТАНОВЛЕНА (не закрыта,
    возврат — по решению владельца)** (карта, секция 8). Аналитика против
    возврата сейчас: 81 домен проверен, пространство почти исчерпано
    (крипто-нейтивы = веб-индекс без дат, мейнстрим = даты со
    скорами ≤0.27); добавка датированных ничего не меняет (max30 ≡ пул-13,
    эксперимент C). Пока пауза, отдачу дают, по отмашке: (1) мониторинг частоты
    джекпотов — 1 ETF-проба пулом-13 в день, 2 кр (~60 кр/мес), скрипт
    sources2_pool_probe.mjs готов; (2) событийные запросы = роутер уровня 2
    из tavily-queries.md (решение за автором); (3) ревалидация пула раз в
    1–2 мес (2–4 кр); (4) правило: новый состав аллоулиста → валидационная
    проба до деплоя.

## Отдельная сессия (2026-07-10 день) — карта источников Tavily руками

64. **Карта источников ВЫПОЛНЕНА (вердикт автора «источники руками», 136 из
    ≤150 кредитов; закреплено коммитом).** Итог + записка автору —
    [notes/tavily-sources-map.md](notes/tavily-sources-map.md), сырьё и скрипты —
    notes/tavily-raw/sources_*. Проверено 55 кандидатов глазами, 44 пробами.
    Главное: (а) **джекпот один — kitco.com: ETF-запрос на пуле из 12
    датированных доменов (advanced, timeRange:week) даёт score 0.689>0.68 с
    валидной датой** — первый выживший через оба фильтра за две сессии;
    (б) у Tavily два непересекающихся индекса: «новостной» с датами и низкими
    скорами (мейнстрим-финансы: kitco/cnbc/forbes/yahoo/fortune/investing/
    marketwatch/thestreet/benzinga + coingape) и «вебовый» без дат со скорами
    0.95+ (почти ВСЕ крипто-СМИ: crypto.news/u.today/ambcrypto/cryptoslate/
    fxstreet/ccn/… — класс stocktwits, дата-фильтр убивает 100%); дизъюнкция
    score↔date из №57 — системное свойство Tavily, формулировками не обходится;
    (в) cointelegraph.com — 0 результатов за 47 прогонов двух сессий (мёртвый
    домен), sec.gov/fed/whitehouse/blockworks — в news-индексе без дат;
    (г) рекомендован ALLOWED_DOMAINS из 13 датированных (карта, секция 4):
    голод по данным снимает (n=12–16, даты 75–100%, fresh24h 2–4 против n=2
    сейчас), но порог 0.68 берётся редко; (д) кандидаты в excludeDomains:
    coindesk/reuters/bloomberg/wsj + cryptoglobe.com (теперь 301→coindesk).
    Методология: одиночные basic-пробы недооценивают скоры (kitco 0.245
    одиночно vs 0.689 пулом на advanced) — мерить пулом. Отправка записки —
    за владельцем.

## Session 9 (2026-07-09 вечер) — интеграция demo/ccxt-факторов, драфты автору

53. **Факторы demo/ccxt интегрированы в paper-контур (observation-only, не закоммичено):**
    (а) `pump_bench/lib.mjs` — `getAggregatedTrades` в ccxt-схему по demo/ccxt;
    (б) новый `pump_bench/factors.mjs` — дословные авторские
    `getExecutedTradesSkew` (volume.predict, 1200/200/0.75) и
    `getVolatilityForecast` (garch, 8 ТФ; garch@2.0.0 уже стоял в example —
    пришёл с синком upstream); (в) `forward.mjs` DECIDE пишет `factors` в
    леджер РЯДОМ с решением — на решения не влияет (оценка ценности факторов
    потом, по накопленному). Смоук: BTCUSDT оба фактора ✓; forward.mjs цел
    (Mongo 3, новых 0 — канал молчит с 07.07).
54. **НАЙДЕН БАГ в авторском demo/ccxt (кандидат в фидбек №2):** ccxt
    implicit-API `publicGetAggTrades` отдаёт все скаляры СТРОКАМИ (проверено
    на авторском пине 4.5.24 и нашем 4.5.64) → `timestamp: t.T` строкой
    роняет volume-anomaly 2.0.0 на `timestamps must be finite numbers`.
    Наш шов: `Number(t.T)` в lib.mjs (задокументировано в коде).
55. **Драфты готовы (отправка за владельцем):**
    (а) фидбек автору №2 — [notes/author-feedback-live-bugs.md](notes/author-feedback-live-bugs.md)
    (TG-клин №50 + timestamp-баг demo/ccxt + fapi-шум HYPE/FARTCOIN);
    (б) промт ресерч-сессии Tavily-запросов (ТЗ автора) —
    [notes/tavily-research-prompt.md](notes/tavily-research-prompt.md):
    уровень 1 статические шаблоны по классам событий + эмпирическая проверка
    через Tavily API (≤150 кредитов, порог 0.68 не оспариваем), уровень 2
    ReAct-дизайн; деливерабл notes/tavily-queries.md + записка автору.
    Запускается ОТДЕЛЬНОЙ сессией.
56. Дежурство: live жив весь день после рестарта 12:07 (pid 85653, тики
    свежие на 20:43), сделок 0 — канал молчит. Дежурный 2ч-таймер перевзведён
    в session 9 (обещание владельца автору от 12:08).
58. **КАНАЛ ОЖИЛ (2026-07-09 22:34 местного) — первый боевой тест
    интеграции №53 пройден:** пост TAOUSDT short (messageId 5713) распарсен;
    forward DECIDE (крон 23:25) записал в леджер решение + ФАКТОРЫ:
    volume-skew = **extreme-аномалия conf 0.98** (совпало с независимым
    volume-монитором: TAO extreme 0.92 в 20:30), garch по 8 ТФ без ошибок.
    Сигнал отсечён авторским momentum-gate («требует свечей до сигнала,
    их нет/мало» — то же, что TAO/SOL в session 5). Live-бот шорт
    отфильтровал штатно (spot, позиций 0). Наблюдение на будущее:
    ⚠️ momentum-gate режет по отсутствию свечей — проверить кэш
    fast_candles, не data-баг ли это (кандидат на разбор session 10).
57. **Ресерч Tavily-запросов ВЫПОЛНЕН (отдельная сессия, 93 кредита) и
    ПЕРЕПРОВЕРЕН по сырым данным (37 прогонов, сырьё сохранено в
    [notes/tavily-raw/](notes/tavily-raw/), токен не светится).** Итог —
    [notes/tavily-queries.md](notes/tavily-queries.md) с запиской автору.
    Главное (всё подтверждено сырыми JSONL): (а) в режиме `startDate/endDate`
    (как в fetchNews сейчас) `publishedDate` отсутствовал у 209/209
    результатов → дата-фильтр режет 100%, включая РОДНОЙ запрос автора
    (19/19 stocktwits, скоры 0.948–0.986 — порог проходят ВСЕ, выживших 0);
    (б) `include_domains` при `startDate/endDate` перестаёт быть гарантией —
    утечка вне аллоулиста query-зависима (3 из 13 запросов: регуляторка 12/20,
    stablecoin 13/20), при `days`/`timeRange` утечки 0/24; (в) в честном
    режиме окна даты есть, но датированные новостные домены дают score
    0.05–0.57 (max 0.571) < порога 0.68 → «дизъюнкция» score/date, выживших
    0 во всех 37 прогонах — голод СТРУКТУРНЫЙ (параметры, не формулировки).
    Перед фиксацией отредактировано 3 оверклейма ресерч-сессии (смягчено
    «выключают»→«не гарантирует», честный скоуп «основной прогон BTC,
    кросс-чеки ETH/SOL», добавлен smoke-факт в записку). Отправка записки +
    фидбека №2 (п.55а) — за владельцем.

## Session 9 — закрытие открытых вопросов (2026-07-10 вечер)

61. **momentum-gate: бага НЕТ, вопрос закрыт.** «Нет свечей до сигнала» в
    леджере был артефактом нашего explainSignals-вызова без словаря свечей
    (у автора такого вызова нет). Реальная причина отказов TAO/SOL (репро с
    полным словарём): направленный momentum −0.88%/−0.62% < обученного
    порога +1.18% — модель честно отсекает шорты при встречном моменте.
    Путь свечей plan() каноничен, порог не трогаем (расхождение с автором).
    Фикс explain в forward.mjs закоммичен (1bcb429): словарь свечей до
    минуты сигнала, lookback — формулой plan() из параметров модели.
    Старые 2 записи леджера не переписаны (append-only), правда — здесь.
62. **РЕШЕНИЕ ВЛАДЕЛЬЦА: trading-agents ПОГАШЕН и убран из @reboot**
    (вариант «а»: полное соответствие автору — «не backtest-kit, обзор
    конкурента»). Процессы убиты, крон-строка удалена (бэкап crontab в
    scratchpad сессии), :8080 не отвечает. Возврат — только руками.
63. **РЕШЕНИЕ ВЛАДЕЛЬЦА: topslivs отложен** (вариант «б»): ночной
    model-free скрининг показал ≈0 после фрикшна, авторский фит не гоняем;
    вернуться при появлении шортов/боковика в канале. В CC_CHANNEL_LIST
    никого не добавляем.

## Session 9 — ночь-5 (2026-07-10, 01:00–09:00): аудит TG-каналов

59. **Ночной аудит каналов-кандидатов ВЫПОЛНЕН (без коммитов — публичный
    репо, эдж владельца; всё в agent/notes/channel-audit/, лог фаз в
    NIGHT-NOTES.md, сводка в MORNING-SUMMARY.md).** Метод: история через
    публичный t.me/s/?before= (без логина, session.txt не тронут) → regex-
    парсеры → консервативный replay по 1m Binance (fill по худшему краю зоны
    за 24ч, TP1-vs-SL, одна свеча = SL, таймаут 72ч, haircut 0.4%).
    Итог: **@topslivs** (292 сигнала/6 мес, шаблон стабилен) — 65% сигналов
    не наливаются; WR 52.5%, +0.41% сырыми → ≈0 после фрикшна; B&H тех же
    окон −1.32% (тайминг-эдж +1.7пп есть); лонг-онли 292:0; каскад целей
    маркетинговый (0 из 101 дошло до цели 2). **@cryptoleakss** — отсев
    (13 сигналов/7 мес, 4 варианта шаблона). **Сеть wallstreetqueen/
    fedrussian/bitcoinbullets — зеркальность подтверждена** (дословные
    копии, лаг ≤1 мин) + формат без зоны/SL → отсев. РЕКОМЕНДАЦИЯ: в
    CC_CHANNEL_LIST никого не добавлять; topslivs — только через
    PumpMatrix.fit + два гейта (дневная задача по отмашке владельца).
60. **Ночь live: канал ожил (2 поста), денег не тронуто, факторы №53
    отработали 2/2.** TAO short 5713 (22:34): skew extreme conf 0.98
    (сошлось с volume-монитором); SOL short 5714 (04:36): skew 0.35
    нейтрален; garch оба без ошибок. Оба short отбиты спот-адаптером
    (№47), но SOL молотил ретраями ~2ч → 492 строки стектрейсов (кандидат
    добавкой к фидбеку №1: объём шума). ⚠️ **momentum-gate отсёк ОБА поста
    с «требует свечей до сигнала, их нет/мало» (2 из 2)** — подозрение на
    data-дыру в пути свечей DECIDE (fast_candles на свежих ts), не на
    честный фильтр; если баг — форвард систематически не торгует.
    КАНДИДАТ №1 на session 10.

## Session 8 — ЗАКРЫТИЕ (2026-07-09 вечер, контекст исчерпан)

> Точка входа для session 9 — этот блок + «Следующие шаги (session 9)» ниже.

52. **Ответ автора на наши 4 вопроса (2026-07-09) + предупреждение — разобран:**
    (1) **Интеграция garch+volume-anomaly+pump-anomaly = `demo/ccxt/src/index.mjs`**
    (лежит в НАШЕМ форке paperhands, 235 строк) — канонический референс:
    `volume.predict(train, detect, conf)` с N_TRAIN=1200 / N_DETECT=200 /
    ANOMALY_CONFIDENCE=0.75 поверх движкового `Exchange.getAggregatedTrades`
    (временной контекст движка → бэктестируемо, аномалия как фактор стратегии;
    мой `example/scripts/volume_monitor.mjs` на прямом ccxt — остаётся дежурным
    live-сканером, но «правильная» стыковка — эта); `volatility.predict` (garch,
    его ЧЕТВЁРТАЯ либа — у нас не установлена) по 8 таймфреймам;
    `pump.PumpMatrix.fit/load/plan/backtest` через getRawCandles.
    (2) **signals = индикаторы: на текущем боковике вредно**, против
    news-sentiment «самолёт vs самокат» → план ТА-стратегии на
    @backtest-kit/signals СНЯТ (подтверждено его статьёй habr 980736 «хаос
    второго порядка»: тысячи одинаковых индикаторных ботов торгуют друг
    против друга в минус на спреде+комиссии).
    (3) **trading-agents-docker — НЕ часть backtest-kit**, это его обзор
    конкурента с плохим результатом (habr 1022562: LLM технически не
    переваривает свалку сырых индикаторов в диалоге — хватается за первый;
    статические новостные запросы мертвы, рынок меняется ежедневно) →
    из торгового конвейера исключён навсегда, остаётся игрушкой-советником;
    гасить ли процесс :8080 — решение владельца.
    (4) **Прямое ТЗ от автора нам: придумать поисковые запросы для Tavily**
    («у меня в example базовые») — драфт выдан владельцу в чат: уровень 1 =
    статические шаблоны по классам событий ({ASSET}-параметризация: ETF
    flows, ликвидации/funding, регуляторка, макро FOMC/CPI, киты/резервы
    бирж, апгрейды/хаки, stablecoin depeg, fear&greed), уровень 2 =
    динамический ReAct-слой (LLM выбирает запрос по состоянию рынка — его же
    вывод из статьи 1022562). Оформить запиской и отправить автору.
    **Предупреждение автора «не полагайся на Claude (особенно sonnet) в
    стратегических вопросах» — принято**: LLM-советы всем = commoditized
    alpha (его же хаос 2-го порядка); разделение труда: стратегические
    концепты = автор, решения = владелец, Claude = инженерия/аудит/
    дисциплина гейтов. Показательно: единственная моя «стратегическая» идея
    (signals-стратегия) автором же и зарублена.
51. **Дежурство 09.07 (владелец отсутствовал, контроль делегирован):** один
    инцидент — утренний клин live после TG-таймаутов (№50), вылечен
    рестартом 12:07. Далее штатно. Статус на закрытие (20:36 местного):
    live тикает (pid 85653, тики свежие <1 мин), СДЕЛОК 0 — канал молчит
    с 07.07 (в parser-items только 3 старых шорта), баланс $100.89 USDT;
    volume-монитор пишет (за сутки 20+ аномалий: SOL 6× extreme, 09.07
    PENGU conf 0.95 и TAO conf 0.92 extreme); trading-agents :8080 ok;
    drift-alert: счётчик 29 = один застрявший пост #MU/USD перечитывается
    (не новый дрейф, косметика). Таймер дежурства погашен вместе с сессией.

### Следующие шаги (session 9)
1. **Интеграция по `demo/ccxt/src/index.mjs`**: volume-anomaly как фактор через
   `Exchange.getAggregatedTrades` + поставить garch + план pump-anomaly
   PumpMatrix — сначала в paper-контуре, ничего своего не придумывать.
2. **Записка автору: Tavily-запросы** (два уровня — статические шаблоны по
   классам событий + динамический ReAct) — его прямое ТЗ, наш вклад за
   консультации.
3. **Кандидаты в фидбек автору** (вместе с запиской): баг «неудачный стартовый
   TG-коннект клинит весь live-конвейер без падения процесса» (№50) +
   постоянные fapi.binance.com fetch-failed по HYPE/FARTCOIN.
4. Решение владельца: trading-agents (:8080) гасить или оставить советником.
5. Ждём: результаты «удочки» владельца по каналам-сиблингам Yoda (промт выдан);
   первую сделку live (канал должен ожить); push репо — только по запросу.
6. НЕ делать: ТА-стратегию на signals (зарублена автором), свой TG-ingest
   (делает автор), доработки trading-agents.

## Session 8 (2026-07-08 день) — ответ автора на tavily-фидбек; полный переход на авторские TG-креды

> Владелец на связи, режим «сделай сам, спешим выйти в live».

41. **Ответ автора на tavily-feedback.md (переписка 2026-07-08):**
    (а) **Telegram-сторону берёт автор** («P.S. телегу уже делаю я, зачем двойная
    работа») — наш собственный TG-API не поднимать; технически из-за VPN
    (ExpressVPN меняет IP на запрос, TDLib требует реалтайм) «по другую сторону
    забора» работает только Tavily. (б) **Рекомендация №1 фидбека (снизить порог
    relevance 0.68) отвергнута**: «score может быть и 0.02 — косвенная реклама»,
    низкий скор = осознанный фильтр рекламного шума. (в) **Диагноз «пайплайн
    голодает» признан**, фикс автор видит в расширении источников поисковых
    запросов (доработка), не в пороге. Рекомендации №2 (guard/no_data) и №3
    (запрос по символу) не оспорены. Открытый вопрос автору: интерфейс его
    TG-фида (эндпоинт/формат/доступ) — до ответа наш локальный ingest живёт.
42. **РЕШЕНИЕ ВЛАДЕЛЬЦА: полный переход на авторские app-креды Telegram.**
    Откат наших guard-правок «свои креды обязательны» в обоих форках:
    backtest-ollama-crontab `954e60a`, backtest-monorepo-parallel `ad6813d`
    (оба integration, НЕ запушены); сборки core/main пересобраны (ловушка:
    alias `@pro/core` грузит `packages/core/build/index.cjs` — старый бандл
    держал guard). Свои TG-креды закомментированы в .env (root/core/main);
    QR-переавторизация под api_id автора 31861455, session.txt разложен
    (корень + content/jan_2026.strategy/ + packages/main), ingest перезапущен
    чисто (лог без ошибок, UI:60050 = 200, @reboot согласован). Бэкапы старых
    session.txt — в scratchpad сессии; старые Desktop-сессии в Telegram
    завершить вручную после 1–2 дней стабильности.
43. **Ключи обновлены владельцем (оба засвечены в чатах — ротировать после
    стабилизации live):** Ollama — 4 файла (crontab root/core/main .env
    `CC_OLLAMA_TOKEN`; example/.env `OLLAMA_TOKEN`); Tavily — 1 файл
    (example/.env `TAVILY_TOKEN`; старый ключ ИСЧЕРПАЛ лимит free-плана —
    это был бы тихий блокер news-пути). `ai-healthcheck.mjs` ✅ оба.
    Помнить: Tavily free = 1000 кредитов/мес, live-forecast не кэширует.
44. **РЕШЕНИЯ ВЛАДЕЛЬЦА — GO LIVE (2026-07-08 вечер): jan_2026, ТОЛЬКО SPOT
    (как у автора — spot везде), юр-вопрос закрыт, VPS отложен (локально),
    ключи не ротируем.** Принятые риски проговорены: (а) правило двух гейтов
    закрыто наполовину (наш OOS ✓ / авторский certified:false) — малый сайз;
    (б) long-only подмножество отдельно не перевалидировано; (в) TP/SL живут
    в движке, не на бирже.
45. **Live-контур jan_2026 собран (crontab-форк, коммиты a35e742+a96aba5):**
    `content/jan_2026.strategy/modules/live.module.ts` — data-схема автора +
    SpotLiveBroker (IBroker 15.2.0: onOrderOpenCommit market-buy только
    type=active; onOrderCloseCommit market-sell остатка; short→throw);
    фильтр `direction==="short"→null` в getSignal (единый для всех режимов);
    `scripts/live-preflight.mjs` (read-only: ключи/права/баланс/бот) — ✅
    exit=0: USDT $100.9, spotTrading=true, withdrawals=false, ipRestrict=false
    (авто-отзыв ключа ~90 дней). Binance-ключи в .env крон-форка (chmod 600).
    Алерты: бот @paperhand1234_bot → группа «сам разберёшься 🟡»
    (-1002501632058, тест доставлен); флаг --telegram в строке запуска.
    **@reboot переключён paper→live** (--live --noFlush --ui --telegram,
    лог logs/live-ingest.log; paper-строка закомментирована для отката;
    бэкап crontab в scratchpad). Дрейф-алерт следит за обоими логами.
    ПЛАН ВЛАДЕЛЬЦА: полное выключение ПК → ребут → всё стартует само
    (mongo/redis restart=always; live @reboot; feb-супервизор @reboot).
    Проверка после ребута: pgrep по live-ingest.pid, лог «SpotLiveBroker:
    ключи валидны», UI :60050, скрейп-тики; paper и live ОДНОВРЕМЕННО НЕЛЬЗЯ
    (один session.txt → AUTH_KEY_DUPLICATED). Kill-switch:
    `kill $(cat logs/live-ingest.pid)` (позиция остаётся на бирже!).
    Сайзинг: дефолт CC_POSITION_ENTRY_COST=$100 → при балансе ~$100 живёт
    только ОДНА позиция за раз (второй open упадёт и будет ретраиться — ок).
50. **Инцидент 09.07 ~09:51–12:07, «клин» live после ребута (сделок не
    потеряно — канал молчал):** сразу после ребута сеть до TG DC
    (149.154.167.41) была недоступна ~10 мин; gramJS отработал 5 ретраев и
    сдох, после чего ДВИЖОК ЦЕЛИКОМ замолчал — ни крауль-тиков, ни оценок
    стратегии (вероятно, упавший/зависший первый crawlLiveFrame блокирует
    общий Cron-конвейер; веб-UI при этом жив и отвечает 200 — ложное
    «здоровье»). Обнаружено дежурной проверкой по отсутствию тиков.
    Рестарт процесса (сеть к тому моменту ок) вылечил мгновенно.
    **Уроки:** (а) health-чек контура = СВЕЖЕСТЬ тиков в логе, не HTTP 200;
    (б) кандидат в фидбек автору: неудачный стартовый TG-коннект клинит
    весь live-конвейер без падения процесса; (в) sleep 90 в @reboot может
    стартовать бота раньше, чем поднялась сеть — рассмотреть sleep 180
    или проверку сети в строке.
49. **Ночь 08→09.07, инцидент OOM (деньги целы, сделок не было):** в 02:23
    systemd-oomd убил cgroup VSCode (сессия Claude раздулась до 13.5GB RSS) —
    умерло всё, запущенное из терминалов VSCode: live-бот и trading-agents.
    Выжило запущенное кроном/докером (feb, volume-монитор, контейнеры).
    Параллельно Binance «Auto-Subscribe» ночью увёл весь свободный USDT в
    Simple Earn Flexible (LDUSDT в споте) — бот остался бы без баланса на
    вход. УТРОМ: владелец сделал Redeem + выключил Auto-Subscribe (проверено:
    $100.89 free, Earn пуст), ребут — live поднялся @reboot'ом (pid cron-scope).
    **УРОКИ (в память):** (а) боевые процессы — ТОЛЬКО через cron/@reboot или
    systemd, никогда из терминала VSCode (умирают с cgroup'ом при OOM сессии
    Claude); (б) на торговом аккаунте Binance выключать Earn Auto-Subscribe;
    (в) @reboot-строка агентов требует PATH до uv (в кроне его нет) — строка
    исправляется на `env PATH=/home/s1dd1/.local/bin:$PATH`.
48. **Вечер 08.07: подключены источники-мониторы + pgpool-контур (без денег).**
    (а) volume-anomaly (источник B): example/scripts/volume_monitor.mjs
    (паттерн scan() автора), крон */15, лог out/volume-monitor.log; первая
    strong-аномалия поймана (ZEC 22:30, conf 0.77). (б) trading-agents
    (источник C): поднят локально через uv на :8080, LLM = Ollama Cloud через
    openai_compatible (OPENAI_COMPATIBLE_API_KEY + TRADINGAGENTS_LLM_BACKEND_URL
    =https://ollama.com/v1); фикс регистра сигнала в форке (c6ed94c); смоук
    HTTP 200, вердикты недетерминированы (Overweight/Hold/Underweight за
    вечер) — использовать как советника, НЕ как сигнал. Известный шум:
    fapi.binance.com klines по HYPE/FARTCOIN стабильно fetch-failed из
    контура гейта (24 за 2ч; сигналам SOL/PENGU/TAO не мешало — следить).
    (в) Новый репо автора backtest-kit-redis-postgres-pgpool-docker форкнут,
    склонирован, проиндексирован; кластер pgcluster :5432 healthy, бэктест
    прошёл, 44665 свечей в Postgres через Pgpool — альтернатива Mongo для
    VPS. (г) Автору отправлены 4 вопроса (volume-anomaly стыковка, пример
    signals-стратегии, место trading-agents, делёж источников; Veles снят —
    его планы). Промт на поиск каналов-сиблингов Yoda выдан владельцу.
47. **РЕШЕНИЕ ВЛАДЕЛЬЦА (после глубокого сравнения фильтров): «оставляем всё
    как у автора, ничего не придумываем»** — наш фильтр шортов СНЯТ,
    `jan_2026.strategy.ts` возвращён к апстриму бит-в-бит; short-сигналы в
    live отклоняет авторский адаптер (throw → откат → ретраи до выхода цены
    из зоны или TTL 1440 мин — это осознанная цена авторского пути: шум в
    логе и занятый слот символа). Paper/backtest снова торгуют шорты
    виртуально, как у автора. waitForInit возвращён к дословному
    (моя fetchBalance-добавка убрана) — свежесть ключей проверяет
    scripts/live-preflight.mjs перед запуском. В live.module остались только
    отличия совместимости с 15.2.0 (шапка файла).
46. **Брокер заменён на ДОСЛОВНЫЙ авторский Spot-адаптер (5e2fb5b) + порядок
    в доках.** Мой промах: писал адаптер по упрощённому примеру из
    `_reference/backtest-kit-docs`, не заметив полной 827-строчной версии в
    `_reference/backtest-kit-skills` (два файла с одним именем). Авторская
    версия сильнее: TP limit + SL `stop_loss_limit` РЕАЛЬНЫМИ ордерами на
    бирже (позиция защищена даже при смерти процесса), fill-poll с откатом
    частичных филлов, восстановление SL/TP при ретраях, dust-гарды. Порт на
    15.2.0 (onSignal*→onOrder*, ccxt v4) — отличия в шапке live.module.ts.
    **Урок (в память): сначала исчерпывающий поиск готового кода автора по
    всему `_reference/` + README, своё — только на доказанных пробелах.**
    Инвентаризация самописного по форкам (сверка diff vs upstream): всё
    оставшееся наше — либо ops/шов без авторского аналога (дрейф-алерт,
    préflight, export-parser-items, seccomp-фикс, CC_CANDLE_EXCHANGE_NAME,
    redis-пароль из env), либо фичи с дефолтом = поведение автора
    (CC_RISK_GATE=llm, CC_CHANNEL_LIST=crypto_yoda_channel), либо
    вынужденное (фильтр шортов: авторский адаптер на short кидает throw →
    вечные ретраи; наш фильтр режет их до движка). ⚠️ Мина на будущее: при
    переходе на pump-anomaly 2.0 с invert-механикой (торговля ПРОТИВ канала)
    фильтр «по direction сигнала» станет неверным — инвертированный short
    канала = наш long-вход; фильтр тогда переносить ПОСЛЕ инверта (и наоборот
    при futures — снимать). Глубокое сравнение фильтра — session 8. Доки: создан
    `_reference/README.md` (карта копий + правило), README-FORK крон-форка
    пп.6-7, статус-блок в live-readiness-checklist.

## Session 7 (ночь-4 часть 2, 2026-07-08 01:17–утро) — mongo добит до корня, дрейф-монитор, мультиканальность

> Владелец спит с ~01:30 («что выберешь — делай»; sudo выдан на диагностику;
> учёт токенов: старт ~50%, лимит паузы 95%). Итоги — [../MORNING-SUMMARY.md](../MORNING-SUMMARY.md).

37. **Mongo-крашлуп НЕ был вылечен образом 8.0.15 (№35 — диагноз неполный):**
    смерти продолжались каждые ~30с (23 рестарта за 10 мин после `up`).
    Настоящая причина (bpftrace): **mongod 8.x SIGSEGV в случайных фоновых
    тредах на ядре 7.0.0 — tcmalloc-google vs syscall rseq**; python-энтрипоинт
    образа глотает крах ребёнка (`p.wait()` без проброса) → ExitCode=0.
    **Фикс: seccomp-профиль rseq→ENOSYS** (дефолт Docker, одна правка) в
    боевом compose uzse (не закоммичено — вопрос владельцу) и превентивно в
    redis-mongo-docker (38de091). Проверено экспериментом на чистых томах +
    аптайм без рестартов, данные целы. Разбор: [notes/mongo-crashloop-fix.md](notes/mongo-crashloop-fix.md).
38. **pump-anomaly 2.0 API разобран** (субагент по коду форка) →
    [notes/pump-anomaly-2.0-api.md](notes/pump-anomaly-2.0-api.md). Поправка
    наших представлений: nested OOS/DSR/SPA — это `model.certification`/`assessEdge`,
    НЕ calibrate; `planForAt()` отдаёт БРУТТО-pnl (baseline history — нетто).
    Граф pump-anomaly переиндексирован (был на 1.0).
39. **Дрейф-монитор PaperTrader встроен в forward.mjs (e21dba4):** CUSUM+KS
    против history модели, нетто-коррекция (−COST 0.1103%), при alarm —
    стоп-флаг `out/DRIFT-ALARM` (DECIDE замирает, EVALUATE продолжается);
    снятие флага/refit — решение владельца. Пока дремлет: вошедших сделок 0
    (PENGU no-fill, TAO/SOL momentum-gate).
40. **Мультиканальность ingest готова (8284bcd, integration):** `CC_CHANNEL_LIST`
    (дефолт crypto_yoda_channel, апстрим-поведение не меняется), скрейп
    последовательный, дедуп по (channel, messageId) уже был. Тесты 33/35 =
    базлайн (2 флака живых данных). Работающий ingest не рестартовал —
    подхватит код при следующем рестарте.

## Session 3 (ночь 2026-07-06) — стенд pump-anomaly: OOS-серия прогнана

> Всё на ветке **`agent-night-20260706`** (master не тронут, push не делался).
> Полный разбор — [notes/pump-bench-night2.md](notes/pump-bench-night2.md);
> итоги для владельца — [../MORNING-SUMMARY.md](../MORNING-SUMMARY.md).

- **Индексация:** все 4 репо амбреллы в codebase-memory (`home-s1dd1-dev-quant-{paperhands,pump-anomaly,volume-anomaly,trading-agents-docker}`).
  Клоны рабочие: pump-anomaly 538/538 тестов, volume-anomaly 733/735 (2 падения — перф-пороги).
- **Стенд `example/scripts/pump_bench/`** (шаг E-2 стек-разбора, без денег): реплей
  через движковый `Exchange.getRawCandles` (шов = README pump-anomaly дословно) +
  быстрый `fast_candles.mjs` для тяжёлых фитов (7ч → 3.2 мин).
- **Ingest-парсер БЕЗ LLM** (`parse_signals.mjs`): 414/416 сырых постов Crypto Yoda
  (apr-2025..apr-2026) → ParserItem; **январь-2026 = 32/32 бит-в-бит с эталоном
  автора**; TZ лога = +05:00 (установлена сопоставлением, захардкожена).
- **Главный результат — walk-forward 4 сплитами** (свежий fit только на train):
  **наш гейт 4/4 HOLDS UP** (OOS mean +0.78..+1.20%/сделка сырыми, Sharpe 0.39–0.54,
  все месяцы в плюсе, обыгран и плюсовый B&H в mar/apr); **гейт автора 4/4
  `certified:false`** (DSR 0.81–0.88 < 0.95; PBO 0.33–0.67). По правилу «два гейта»
  → **не промоутим**; вывод: сильно и последовательно, но паковать в деньги рано.
- Все 4 фита выбрали идентичный exit-конфиг → выбор стабилен при ≥278 постах.
- **Мета-оговорка:** jan..apr-2026 были доступны автору при дизайне библиотеки —
  честный форвард только paper/новые данные.
- In-sample января cascade-invert давал +2.11%/сделку vs +0.63% у enter (n=5);
  в OOS инверт-механизм активен (18+8+4+4 действий).

### Решения владельца (session 3, из переписки ~00:45)
16. **ЦЕЛЬ ЗАФИКСИРОВАНА: повторить экосистему автора (org github.com/backtest-kit +
    backtest-kit.github.io) и привести её в live-торговлю.** Порядок
    backtest→paper→live и гейты сохраняются.
17. **uzse-backtest-app взят в оборот** (форк владельца склонирован, разобран —
    [notes/uzse-app-analysis.md](notes/uzse-app-analysis.md)); автор: «именно эту
    ссылку надо копать», для акций — новостной сентимент.
18. **Пауза по токенам:** ~80% бюджета выбрано к 01:15, ресет ~через 2ч — сессия
    свёрнута штатно, продолжение после ресета.

### Session 4 (2026-07-07 вечер): ingest ЗАПУЩЕН live + задача от автора
21. **Telegram-ingest работает E2E**: QR-авторизация владельца ✓ (session.txt;
    копия нужна в корне форка И в папке стратегии — CLI делает chdir),
    Mongo починен (крашлупил: права на mongo_data), Redis поднят,
    `npm start -- --paper --ui --entry ./content/jan_2026.strategy/...` крутится
    (pid в logs/paper-ingest.pid), крон 15m скрейпит canal → 3 живых сигнала
    2026-07-07 (SOL/PENGU/TAO short) в `parser-items`, 1 прошёл Ollama-гейт в
    `screen-items`. UI: localhost:60050.
22. **Шаблон канала эволюционировал снова**: «Открыть ШОРТ в диапазоне …',
    «СТОП ЛОСС» С ПРОБЕЛОМ → их regex давал extracted:null на всех сигналах дня.
    Фикс в форке (784b1a1, запушен): `/СТОП[-\s]?ЛОСС:/`. Наш parse_signals.mjs
    пробел уже умел; вариант «в диапазоне» у обоих матчится по подстроке «зоне».
    ⚠️ Урок: шаблон дрейфует ~ежемесячно — нужен алерт на «сигналоподобный пост
    не распарсился» (у них уже логируется Parsed/extracted:null — можно грепать).
23. **Задача от автора (переписка 2026-07-07, взамен обещал консультировать):**
    дать фидбек по анализу новостной ленты через tavily.com — т.е. прогнать/
    разобрать news-sentiment путь (feb_2026 forecast, Tavily+Ollama) и написать
    развёрнутый отзыв. Поставлена в очередь после стыковки ingest→стенд.
    Его сторона: закрыл pump-anomaly тестами (774/83 файла, edge-reality:
    флэш-фитиль/UTC-сдвиг/ёмкость notionalQuote/адаптивный refit/channelScore
    селективность) — «сам взял аудит торговых сигналов из каналов».

### Session 6 (ночь-4, 2026-07-08): скоуп сужен до крипто
34. **РЕШЕНИЕ ВЛАДЕЛЬЦА: «мы собрались торговать криптовалютой» — UZSE-ветка
    ЗАПАРКОВАНА.** uzse-backtest-app остаётся форкнутым/проиндексированным как
    инфраструктурный референс (Pine без TradingView), но НЕ входит в live-план.
    Возврат — только отдельным решением (трек «локальный рынок»).
35. **Mongo-контейнер (общий: backtest-pro крипто-контура + uzse) сегфолтился
    в цикле** — 63 рестарта с ребута, mongod SIGSEGV через ~30с (образ
    8.0.4-ubi8 на ядре 7.0.0). Обновлён до 8.0.15-ubi9 (данные в volume целы),
    compose-файл в uzse-форке правлен. Запросы успевали в окна между падениями
    (поэтому всё «работало»), но это была бомба под durable-контуром.
36. Ночь-4 план (крипто-only): разбор pump-anomaly 2.0 API (PaperTrader/
    capital/channelScore/doctor — агент), дрейф-алерт шаблона, подготовка
    мультиканальности ingest (CC_CHANNEL_LIST), утренний отчёт.

### Session 6 (2026-07-08, после ребута): ребут-тест ✓ + Tavily-фидбек готов
31. **Ребут-тест ПРОЙДЕН**: mongo/redis (docker restart=always), ingest-paper
    (@reboot, порт 60050), супервизор feb (@reboot), форвард-крон — всё
    воскресло само. Первый созревший форвард-сигнал оценён: PENGU short —
    сигнал был, зона входа не налилась (честный «no fill», сделки нет).
32. Ловушка пофикшена: `--noFlush` добавлен в супервизор и @reboot-строки —
    рестарт больше не стирает paper-артефакты (8.5ч статистики потеряно на
    уроке; сами логи сохранились).
33. **Tavily-фидбек автору готов** — [notes/tavily-feedback.md](notes/tavily-feedback.md),
    текст под отправку в Telegram. Ключевая находка (проверена прямыми пробами
    API): аллоулист доменов + порог relevance>0.68 несовместимы (скоры на
    аллоулисте ≤0.55) → fetchNews len=0 всегда → форекаст на пустом входе →
    вечный neutral → 0 сделок во всех paper-прогонах. Плюс: query захардкожен
    «Bitcoin» (symbol игнорируется), query-инструкция вместо топика, нет guard
    на пустой вход, ccxt RequestTimeout валит процесс.

### Session 5 (финал, 2026-07-08): durable-контур + РЕБУТ-ТЕСТ
28. Этап B закрыт: Mongo-персистенция состояния уже была у автора (setup.config);
    крон-строки владельца одобрены и стоят: (1) форвард ежечасно :25,
    (2) @reboot ingest-paper, (3) @reboot супервизор feb_2026. Разрешение
    Bash(crontab *) добавлено в /home/s1dd1/dev/quant/.claude/settings.local.json.
29. feb_2026 multi-day paper умирал от необработанного ccxt RequestTimeout
    (8.5ч, 2026-07-06 09:16) → перезапущен под шелл-супервизором
    example/scripts/run_paper_feb.sh (ядро не трогаем). Кандидат в фидбек автору.
30. **РЕБУТ-ТЕСТ по команде владельца («как добавишь перезапусти ПК»).**
    ЧЕКЛИСТ ДЛЯ НОВОЙ СЕССИИ ПОСЛЕ РЕБУТА:
    - docker ps: mongo (node-ollama-agent-swarm-mongo-server) и redis Up;
    - ss -tlnp | grep 60050 → ingest-paper жив (@reboot, ~90с после старта);
    - pgrep -f run_paper_feb → супервизор feb жив;
    - forward-cron: в :25 появится запись в example/scripts/pump_bench/out/forward-cron.log;
    - Mongo parser-items продолжает расти при новых постах канала;
    - леджер и результаты: example/scripts/pump_bench/out/forward-{ledger,results}.jsonl.
    Следующая большая задача: Tavily-фидбек автору (эмпирика в
    content/feb_2026.strategy/dump/report/*.jsonl за 8.5ч + новый прогон).

### Session 5 (продолжение): ФОРВАРД-ОКНО ОТКРЫТО
27. **Стыковка ingest→стенд запущена** (протокол: [notes/forward-protocol.md](notes/forward-protocol.md)).
    Замороженная forward-model-v1: fit на всех 414 постах, pump-anomaly 2.0.0.
    **Паспорт отрезвляет**: 2.0.0 моделирует фрикшн внутри (roundTripCostPct
    0.11%) и после калибровки говорит nested OOS-score −0.0005 ≤ 0, DSR 0.026,
    SPA p 0.059 → «эдж после издержек ≈ ноль». Это расходится с нашим сырым
    WF на 1.0.0 (+0.99%/сделку) ровно на величину честности — арбитром будет
    форвард. Первые живые решения в леджере (2026-07-07): PENGU short → SIGNAL,
    TAO/SOL → momentum-gate. Оценка исходов — через ~24ч (staleMinutes·2).
    acknowledgeUncertified:true = решение владельца («Да, погнали», ускоренная
    программа). Крон-строка — в протоколе (установка за владельцем: политика
    запрещает агенту писать в crontab).

### Session 5 (2026-07-08): синхрон с upstream — автор ушёл далеко вперёд
24. **Все 8 репо синхронизированы с upstream** (решение владельца: «вести
    разработку параллельно»). Масштаб: ядро backtest-kit +22 коммита
    (14.1.0→**15.2.0**: ClientStrategy/Risk/Sizing/Action, toProfitLossDto,
    спек-тесты lookahead/pnl); pump-anomaly +29 (**2.0.0**: capital/ёмкость,
    calibrate, doctor, algo-signature, trained-gate); volume-anomaly +14 (2.0.0);
    прикладные репо — бампы на 15.2.0. Наши ветки ребейзнуты и запушены:
    paperhands agent-night (3ecac20), integration всех трёх форков. Локальные
    master-ы синхронизированы; **пуш master-ов заблокирован политикой — нужно
    отдельное «да» владельца**. ollama-crontab пересобран на 15.2.0, paper
    перезапущен. example переведён на 15.2.0 + pump-anomaly@2.0.0.
25. **ГЛАВНОЕ (2.0.0): автор вшил доктрину «двух гейтов» в библиотеку.**
    `model.deployment` = trade|paper|unknown; при красном сертификате
    signals()/plan() ПУСТЫ без явного `acknowledgeUncertified:true`
    («осознанный форвард на бумаге/микро»). Проверено: наша WF-модель →
    verdict:"paper", с ack:true сигналы идут (32/32). backtest()/planForAt()
    не гейтятся. Это легитимизирует ускоренную программу владельца:
    paper→микро-live на несертифицированной модели = ШТАТНЫЙ путь библиотеки,
    а ack:true = то самое документируемое решение владельца из этапа D.
    774/774 теста зелёные на нашем клоне. Регрессия стенда: plan() без ack
    теперь 0 сигналов — стыковку ingest→стенд строим на новом API.
26. Наш фикс «СТОП ЛОСС» (784b1a1) у автора НЕ появился — мы впереди;
    кандидат в фидбек/PR автору вместе с Tavily-отзывом.

### Подсказка автора №3 (2026-07-06 утро): флаг `--ui`
20. Автор в переписке показал `npx @backtest-kit/cli --backtest --ui --entry <strategy> --cache`
    — веб-дашборд результатов на `localhost:60050`. Проверено на нашем cli@14.1.0
    (флаг есть, entry у нас позиционный): jan_2026 TRXUSDT поднят с `--ui`,
    HTTP 200. Задокументировано в CLAUDE.md «Режимы CLI». Скриншот автора
    (Total PNL 68.90%, 17 сделок) — это jan_2026 на ДЕФОЛТНОМ symbol=BTCUSDT,
    не канонический TRXUSDT-прогон из README (+8.58%) — к его цифре относиться
    как к ещё одному in-sample числу.

### Задание владельца на продолжение (после ресета токенов, ~03:15)
19. Исследовать новые данные (org backtest-kit + backtest-kit.github.io) и выдать
    **обязательный деливерабл: список репозиториев к форку — что форкать на GitHub
    (в аккаунт highscrren-dotcom) и что клонировать локально, с обоснованием по
    каждому**. Всё прописать в документации (agent/notes/ + DECISIONS).
    Таймер поставлен (фоновый sleep 7200); если сессия оборвётся — этот пункт
    и есть точка входа новой сессии.

### Находка session 3 (важно)
- В org backtest-kit **публично лежит `backtest-ollama-crontab`** — Telegram-ingest
  (скрейпер+Ollama), который в session 2 считали непубличным. Это снимает главный
  блокер paper-форварда pump-пути. Первая задача следующей сессии.

### ✅ №19-b ВЫПОЛНЕНО: форки владельца взяты в работу (утро 2026-07-06)
Владелец форкнул 3 репо; агент: клоны в амбрелле + remotes, ветки `integration`
с правками (чужие TG-креды → env с гардом; CC_RISK_GATE=llm|rules|off — llm
дефолт под подписку Ollama владельца; redis-пароли из env; exchangeName-фикс
свечей в redis-mongo; экспорт-адаптер Mongo→ParserItem), README-FORK в каждом,
build ✓ везде, скилл завендорен в `.claude/skills/` с правкой маркетинга,
индексация в графе. Статус и хэши — в [notes/fork-map.md](notes/fork-map.md).
⚠️ Владельцу руками: сделать форк `backtest-monorepo-parallel` ПРИВАТНЫМ (нет
LICENSE); push веток integration — по команде.

### ✅ Деливерабл №19 ГОТОВ — [notes/fork-map.md](notes/fork-map.md)
Вся экосистема разобрана (11 репо org + сайт 923 стр. + личный аккаунт). Итог:
**форкать 3** — `backtest-ollama-crontab` (ingest; чужие TG-креды захардкожены —
менять; их «live» без createOrder!), `backtest-kit-redis-mongo-docker`
(durable-персистенция, 14.1.0-совместим), `backtest-monorepo-parallel` (каркас
раннера; ⚠️ БЕЗ LICENSE → форк приватным); **вендорить с правкой** —
backtest-kit-skills в `.claude/skills/` (вырезать маркетинг «example генерит
профит»); **не форкать** — сайт/docs/PineTS/npm-пакеты. Команды и обоснования — в
fork-map.md. Go-live чеклист автора с сайта: WR>60% + Sharpe>1.0 на 100+ сигналов,
≥30 дней paper, maxDD<20%, SIGINT-хендлер.

### Решения агента (session 3, ревью владельца утром)
13. Тяжёлые фиты — через `fast_candles.mjs` (дневной кэш), движковый шов остаётся
    эталонным путём стенда (провалидирован на jan-2026).
14. Парсер сырых постов — детерминированный regex, НЕ LLM (воспроизводимость;
    эталон 32/32). LLM-fallback не понадобился.
15. Ветка `agent-night-20260706` от master; чекпоинт-коммиты по фазам.

### Открытые вопросы к владельцу (session 3)
- **Paper-форвард pump-пути невозможен без живого Telegram-фида** — его нет ни в
  одном репо (ingest = наша работа). Строить ли tg-скрейпер (userbot/API, доступ
  к каналу)? Это же блокер «настоящего OOS».
- Шорты = ~45% сделок: спот их не умеет → фьючи (funding/ликвидации) или long-only
  (≈ половина эджа). Позиция?
- Юр/налоговый вопрос pump-торговли (🚩 из session 2) — остаётся открытым, блокирует
  paper→live, НЕ блокирует исследование.
- Fast-forward master на `agent-night-20260706` после ревью?

## Session 2 (2026-07-05) — paper-прогон запущен

- **Решение владельца: paper на стратегиях автора (свою пока не пишем).** Выбрано:
  **feb_2026 (AI-news sentiment) на BTCUSDT** — «активный проект», AI-стек живой.
- **`example/content/feb_2026.strategy/modules/paper.module.ts`** создан — переносит
  `CC_MAX_STOPLOSS_DISTANCE_PERCENT:100` + exchange в `--paper` (в paper грузится только
  `paper.module`, не `backtest.module`; без него дистанц-гейт режет moonbag-сигналы).
  Ядро `src/**` не тронуто.
- **Smoke-тест пройден:** boot, живые BTCUSDT-свечи, live-часы, forecast (Tavily+Ollama),
  look-ahead безопасен. feb_2026 корректно вернула `null` на `sideways/not_reliable`.
- **Запущен 5-часовой paper** (`--paper --symbol BTCUSDT`, self-stop по SIGINT). Нюанс:
  forecast кэш 1d → в «тихий» новостной день сделок нет до смены суток UTC.
- **Находка:** feb_2026 захардкожена на Bitcoin (запрос Tavily = константа «Bitcoin
  market sentiment», символ в поиск не идёт) → стратегия BTC-only by construction.

### Решения владельца (session 2)
10. **GRAM — отложен, остаёмся на BTC.** GRAM/USDT торгуется на Binance (~$45M/24ч,
    данные paper тянет), но новостной путь бесполезен (баг запроса + мелкий токен), а
    идентичность токена под вопросом (не заблокированный SEC gram TON).
11. **X API — отложен на неопределённый срок** (дорого $200–5000/мес + botted/пампы;
    рекомендация «не внедрять»). Быстрый путь (домены в allowlist) — тоже no-op.
12. **Новостные EDIT 1/2 под GRAM — НЕ делаем** (не меняем предмет теста).
- Полный разбор GRAM/x.com — [agent/notes/gram-xcom-feasibility.md](notes/gram-xcom-feasibility.md).

### Следующие шаги (session 2)
1. Дать BTC-paper доработать; для реальной сделки нужен многодневный прогон (смена суток
   UTC → forecast пересчитается) или Docker (durable, переживёт рестарт).
2. Опц.: разведочный **GRAM TA-смоук** (dec_2025, ноль правок, read-only) — но Pine
   BTC-tuned → out-of-distribution, цифрам не верить до OOS.
3. Направление сместилось: **paper-тест стратегий автора на BTC**, а не своя jun_2026 ETH
   (Phase-3 ниже — отложена).

## Где мы сейчас (конец сессии 1, 2026-07-05)
- Репозиторий = форк `tripolskypetr/backtest-kit`; локальный минимальный движок «paperhands»
  из первой сессии — в бэкапе scratchpad (не в git).
- `master` **в синхроне с upstream** (`7a50a06 tests`) + 8 наших коммитов сверху. Дерево чистое.
  Не запушено (origin отстаёт на 9 — по договорённости).
- Работаем в [`../example`](../example/) (данные ccxt/Binance публичные; тянет published-пакеты
  `backtest-kit@14.1.0`, не локальный `src/`).
- **Инфраструктура готова, но своей стратегии ещё НЕТ** — следующий шаг Phase 3.

## Решения владельца (session 1)
1. **Пивот на форк** backtest-kit (принят как база). [CLAUDE.md]
2. **Работаем в `example/`**; данные — публичный ccxt (backtest/paper **без ключей биржи**).
3. **Git:** коммиты по фазам, показывать `git diff` перед коммитом, ветка от `master`.
   `master` = наша работа. **Push — только по запросу** (сейчас не запушено).
4. **OOS-порог вердикта (наш гейт):** OVERFIT, если out-of-sample `Sharpe<0` ИЛИ `return<0`
   ИЛИ проигрыш buy&hold. — **подтверждён.**
5. **Первая стратегия:** июнь-2026 **ETHUSDT**, концепт = **трендследящий SHORT**
   (июнь — даунтренд −22%). [agent/notes/jun_2026-eth-analysis.md]
6. **AI-маршрут:** **Ollama Cloud** (`minimax-m2.7:cloud`) + **Tavily Free**. Ключи в
   `example/.env` (проверены, работают). **Claude не используем** — дорого на цикле бэктеста.
7. **Binance trade-ключи:** НЕ сейчас; создаём **прямо перед live** (Phase 5). Backtest/paper
   ключей не требуют. Сам аккаунт/KYC — на владельце.
8. **Навигация по коду:** сначала MCP codebase-memory граф. [CLAUDE.md + глобальный ~/.claude/CLAUDE.md]
9. **feb_2021** (Python-WASM) заблокирован — нужен `wasmtime` в `~/.wasmtime/bin/` (не установлен).
   Опционально поставить, если нужен Python-WASM.

## Проверенные факты (НЕ повторять работу)
- Дословный старт работает (`npm start -- --backtest ...`). [MORNING-SUMMARY.md]
- **Воспроизведение example 5/6 точно** (jan/dec/mar/apr/apr24 ✓; oct21≈NN-недетерминизм;
  feb21 blocked). Урок: apr +67.85% = дисперсия; oct21 +19% проиграл hold +40%.
  [agent/notes/example-reproduction.md]
- **OOS-гейт реально ловит переобучение** (walk-forward dec_2025: Dec +2.4% → Nov +0.14% →
  Jan −4.8% → вердикт OVERFIT). [agent/notes/walk-forward-dec2025.md]
- **AI look-ahead безопасен** — `fetchNews` фильтрует `publishedDate ≤ when`.
- **AI-стек живой** (Tavily + Ollama Cloud). Healthcheck: `example/scripts/ai-healthcheck.mjs`.
- Карта фреймворка — [agent/notes/framework-map.md]. Инструменты — [agent/tools/](tools/).

## Следующие шаги (Phase 3 — писать стратегию)
1. `math/jun_2026.pine` (трендследящий SHORT: EMA-фильтр даунтренда → только шорт; вход на
   откате к сопротивлению; SL над свинг-хаем +~1.5–2×ATR; **TP ≥ 1%, R/R ≥ 2**; таймаут;
   цель **≥1 сигнал/день**; без HOLD/вечного трейлинга) + обёртка `content/jun_2026.strategy.ts`
   + `modules/backtest.module.ts` (frame июнь) + symbol config.
2. Backtest июнь ETH → `agent/tools/parse-report.mjs`.
3. **OOS-гейт** на май/июль → не подгонка ли под падающий июнь.
4. **Code-review отдельным агентом** (perpetual hold / дрейф SL — по доктрине).
5. Затем **AI-news вариант** июня ETH → сравнить обе стратегии одним OOS-гейтом.
6. Далее: **paper** (основной прогон) → **live** (Phase 5: отдельный аппрув, брокер-адаптер
   по шаблону автора, мелкий сайз, стабильный хост/Docker).

## Открытые вопросы к владельцу (на старт новой сессии)
- Тайминг Binance-ключей (дефолт — перед live).
- Пушить ли `master` в origin.
- `feb_2021`: ставить `wasmtime` или пропустить.

## Как синхронизироваться с upstream (напоминание)
`git fetch upstream && git rebase upstream/master` — наши правки в `agent/`+доки с ядром
не конфликтуют. Remote `upstream` = `github.com/tripolskypetr/backtest-kit`.
