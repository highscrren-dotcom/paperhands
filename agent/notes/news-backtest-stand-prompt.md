# Промт для отдельной сессии: news-бэктест-стенд (вопрос автора 17.07)

> Вопрос автора: «гонял бэктест на таком малом количестве источников? Что
> показывает UI? Именно по метрикам». Ответ владельца отправлен (честное
> «не гоняли, n=9»); стенд — обещанное продолжение метриками.

---

Читай paperhands/CLAUDE.md, agent/DECISIONS.md (№84-85, №91-93),
example/scripts/news_dataset/news_query.mjs, доктрину стратегий в CLAUDE.md.
Задача — честный движковый news-бэктест-стенд:

1. Стратегия-стенд content/news_jul2026.strategy/ в example: getSignal(symbol,
when) читает mongo news-audit через логику news_query.itemsFor({symbol, when,
includeBackfill: true}) — СТРОГО publishedAt <= when (канон автора, look-ahead
инвариант абсолютен). Последний непотреблённый ok-item в окне 24ч → сигнал
{position: direction, TP +1.5%, SL -1%, timeout 24ч} (фикс-параметры = честная
заглушка, min TP 1% и R/R из доктрины сознательно упрощены — пометить в шапке).
Один item = один сигнал (дедуп по id в памяти стенда).

2. Frame 2026-07-06..2026-07-17, symbol BTCUSDT (9 из 10 items). Прогон:
npm start -- --backtest --symbol BTCUSDT ./content/news_jul2026.strategy/...
+ --ui (порт CC_WWWROOT_PORT=60054 — 60050/60052 заняты!). Снять метрики UI
(sharpe, pnl, drawdown, число сделок) + сохранить report движка.

3. Деливерабл: agent/notes/news-backtest-stand.md — метрики движка как есть,
БОЛЬШИМИ буквами оговорка «n=9 сигналов = дисперсия, не вывод», как
воспроизвести, что упрощено. Скрин UI — сказать владельцу, где смотреть
(localhost:60054), он сам заскринит для автора.

Ограничения: боевые кроны/бот/news-audit не трогать (mongo только чтение!);
Tavily/Ollama не вызывать; ничего не коммитить без «комить»; ОЗУ — свечи
батчами. Числовые сравнения — node, не awk (ru_RU локаль).
