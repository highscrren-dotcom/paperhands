# Промт для отдельной ресерч-сессии: Tavily-запросы (прямое ТЗ автора)

> Скопировать блок ниже в НОВУЮ сессию Claude Code (cwd = /home/s1dd1/dev/quant/paperhands).
> Написан session 9, 2026-07-09. Итог ресерча вернуть сюда: agent/notes/tavily-queries.md.

---

Читай paperhands/CLAUDE.md и agent/DECISIONS.md (п.52). Задача — прямое ТЗ автора
backtest-kit (2026-07-09): «придумай поисковые запросы для tavily.com, у меня в
example базовые». Новостной сентимент — его главный edge (индикаторы он зарубил:
«самолёт vs самокат»), так что это самая ценная работа, которую мы можем ему сдать.

## Где что лежит (проверено)

- «Базовые» запросы автора: `example/logic/core/advisor/tavily_news.advisor.ts` —
  `TOPIC_QUERIES = { sentiment: ["Bitcoin market sentiment. Can be bullish, bearish, neutral or sideways"] }`.
  Формат расширения: topic → массив запросов, дедуп по url.
- Механика поиска: `example/logic/api/fetchNews.ts` — `tavily.search(query, {topic:"news",
  maxResults:20, searchDepth:"advanced", includeDomains: ALLOWED_DOMAINS,
  startDate/endDate ±1 день})`; далее фильтры: relevance score > 0.68,
  publishedDate обязателен и не 00:00 UTC (иначе результат выброшен).
- Аллоулист: cointelegraph.com, theblock.co, decrypt.co, blockworks.co, sec.gov,
  federalreserve.gov, whitehouse.gov, truthsocial.com, stocktwits.com.
  Запрещены (нет publishedDate): coindesk, reuters, bloomberg, wsj.
- Ключ: `example/.env` → `TAVILY_TOKEN` (free-план 1000 кредитов/мес; live-путь НЕ кэширует).
- Эмпирика session 6 (уже проверена, не перепроверять): базовый запрос на аллоулисте
  даёт скоры ≤0.55 → порог 0.68 отсекает всё → len=0 → форекаст на пустом входе →
  вечный neutral → 0 сделок. Автор голод признал; фикс видит в расширении
  запросов/источников, НЕ в пороге (низкий score = его осознанный фильтр рекламы).

## Задача

**A. Статические запросы (уровень 1) — спроектировать и ЭМПИРИЧЕСКИ проверить.**
1. Прочитать advisor + fetchNews + соседний README, понять формат.
2. Составить шаблоны с {ASSET}-параметризацией по классам событий: ETF flows/approvals;
   ликвидации + funding; регуляторка (SEC/CFTC/суды); макро (FOMC, CPI, ставки);
   киты + резервы бирж; апгрейды сетей/хардфорки/хаки; stablecoin depeg;
   fear&greed. Плюс 1–2 под персоны (truthsocial/stocktwits в аллоулисте не
   случайно — Трамп/инфлюенсеры).
3. Каждый шаблон прогнать РЕАЛЬНО через Tavily API (скрипт в scratchpad, НЕ в
   репо) на 3 ассетах: BTC, ETH + один из наших живых сигналов (SOL/PENGU/TAO).
   Замерить: распределение score, сколько результатов >0.68, какие домены, свежесть.
4. Итерировать формулировки. Цель: на активном новостном дне ≥3–5 результатов
   score>0.68 на класс. Ловушка: score чувствителен к СТИЛЮ запроса — сравнить
   А/Б: «инструкция» (как у автора: "...Can be bullish, bearish...") vs чистый
   новостной топик ("Bitcoin ETF outflows liquidations").
5. Итог — таблица: класс → шаблон → эмпирика (n>0.68, median score, домены) → вердикт.

**B. Динамический слой (уровень 2) — ТОЛЬКО дизайн, без кода.**
ReAct-схема: выбор класса запроса по состоянию рынка (garch-волатильность,
volume-anomaly skew, funding/OI). Обоснование — вывод самого автора (habr
1022562): статические новостные запросы мертвы, рынок меняется ежедневно.
Одна страница схемы, не библиотека.

## Деливерабл

`agent/notes/tavily-queries.md`: (1) таблица уровня 1 с эмпирикой; (2) схема
уровня 2; (3) в конце раздел «Записка автору» — по-русски, кратко, инженерно,
с цифрами проверки. Тон: вклад в его ТЗ, не стратегические советы (его
предупреждение: Claude ≠ стратег; концепты — автор, решения — владелец).

## Ограничения

- Код стратегии/advisor НЕ менять — предложения текстом + эмпирика.
- Порог 0.68 и аллоулист НЕ оспаривать — работать внутри них.
- Бюджет ≤150 Tavily-кредитов (basic search = 1, advanced = 2 кредита);
  вести счёт, итог расхода — в заметку.
- TAVILY_TOKEN не светить в заметках/отчётах. В git ничего не коммитить
  без показа diff владельцу.
- **Следить за ОЗУ** (урок OOM 08→09.07: сессия Claude раздулась до 13.5GB RSS →
  systemd-oomd убил cgroup VSCode; см. agent/DECISIONS.md №49). Практически:
  сырые ответы Tavily НЕ тащить в контекст целиком — скрипт пишет их в JSONL в
  scratchpad, в контекст только агрегаты (счётчики/медианы score); большие
  файлы читать точечно (offset/limit, grep), не целиком; результаты фиксировать
  в tavily-queries.md ИНКРЕМЕНТАЛЬНО после каждого класса запросов (сессия может
  умереть — работа не должна пропасть); раз в ~30 мин проверять `free -g` и RSS
  своего процесса, при дефиците — сворачивать контекст (компакт), не добирать
  новые классы.
