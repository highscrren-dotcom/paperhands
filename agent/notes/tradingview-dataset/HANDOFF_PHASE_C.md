# HANDOFF — фаза C ЗАКРЫТА 29.07

> **Итог: холд увеличивать не надо, 14 суток — оптимум.** Прогон отработал (412 из 415
> тикеро-месяцев, 3 нерасчётных по PUMP до листинга). Парный бета-контроль на правиле
> рецепта: холд 14 даёт лифт над buy & hold **+1.493 %/сделку при t = +2.28**, холд 18 —
> +0.059, холд 27 — +0.812 (не значимы). Отчёт — [session22/REPORT_PHASE_C.md](session22/REPORT_PHASE_C.md),
> письмо автору — [session22/MESSAGE_PETR_HOLD27.md](session22/MESSAGE_PETR_HOLD27.md),
> решение — DECISIONS №134. Ниже — журнал этапов, он оставлен как есть.

## Журнал: состояние на 28.07 13:00 UTC

Запрос автора (28.07): холд не выдыхается на 14 сутках — проверить 27.
План целиком: [session22/PLAN_PHASE_C.md](session22/PLAN_PHASE_C.md).
Итоги фаз A/A2/B: [session22/REPORT_PHASE_B.md](session22/REPORT_PHASE_B.md), DECISIONS №130–133.

## Где остановились

| этап | статус |
|---|---|
| 0. разведка кэша | **готово** |
| 1. объединённый склад свечей | **ГОТОВ** — 12 символов, 16.3 млн файлов |
| 1б. поминутная проверка полноты | **ГОТОВО** — 2 190 115 дыр в прошлом, 199 интервалов |
| 1в. докачка хвоста с Binance | **ГОТОВО** — 2024 запроса, записано 1 932 098 минуток |
| 1г. контрольный прогон `netCalls = 0` | **ГЕЙТ ПРОЙДЕН** |
| 1д. замер памяти | **ГОТОВО** — 12.5 МБ кучи на идею, 23.0 с/идея |
| 2. прогон 840 точек | **ИДЁТ** с 12:33 UTC, юнит `quant-phase-c`, ~15 ч |
| 3. контроли и гейт | **ПОСЧИТАНЫ, ГЕЙТ НЕ ПРОЙДЕН** (см. ниже) |

## Проверенные факты этой сессии (не переделывать)

**Юнит `quant-phase-resume` (фаза B) отключён**, вместо него `quant-phase-c-resume`
(enabled) → `phaseC/pipeline_c.sh`, идемпотентен по меткам `done_*.json`.

**Поминутная проверка вскрыла в 10 раз больше, чем посуточная разведка.** Разведка
считала 1413 докачиваемых суток; `check_minutes.py` нашёл 2 190 115 недостающих МИНУТОК,
собранных всего в 199 интервалов. `fetch_tail.py` их бы не закрыл — он тоже посуточный
(сутки «есть», если есть хоть одна минутка). Написан `fetch_minutes.py`, качающий строго
по списку `missing_minutes.json`.

**Что осталось недокачанным — 258 017 минуток, и это не наша недоработка:**
- `HYPEUSDT` 172 159 — символ **делистнут с Binance spot**, `exchangeInfo` отвечает
  `Invalid symbol`. Из фазы C выкинут: 14 тикеро-месяцев, 47 идей из 10 892 (0.43 %);
- `PUMPUSDT` 82 109 — идеи старше листинга (те же «нерасчётные» месяцы, что и у автора);
- **7 окон техобслуживания Binance** по 79–284 минуты (2021-02-11, 2021-03-06,
  2021-04-20, 2021-04-25, 2021-08-13, 2021-09-29, 2023-03-24) — торгов не было, свечей
  нет ни у кого. Одинаковы для всех символов.

**ГЕЙТ ПРОЙДЕН фактом:**
```
OK may_2022_BTCUSDT_gate points=840 rules=840 net=0 sec=827
пик RSS 541 МБ, rc=0, truncatedCount=0
```
Месяц с окном техобслуживания даёт не ноль, но не рвётся:
```
OK sep_2021_BTCUSDT_gate points=840 rules=840 net=25 sec=593
```
То есть сеть цепляется только на семи окнах биржи, десятками вызовов, а не миллионами.

**Замер, по которому разложены шарды:** 36 идей / 840 точек / горизонт 38 880 мин →
827 с и пик RSS 541 МБ. Это **23.0 с на идею** и **12.5 МБ кучи на идею**. Пик шарда =
его самая крупная задача, поэтому раскладка не по времени, а по памяти: два самых
тяжёлых тикеро-месяца (449 и 344 идеи) лежат в ОДНОМ шарде `b0` и физически не могут
считаться одновременно.

| шард | задач | идей | max задача | пик |
|---|---|---|---|---|
| 0 / 1 / 2 | 129/128/128 | 2461/2457/2457 | 96/93/85 | ~1.3 ГБ каждый |
| b0 | 14 | 2355 | **449** | ~5.7 ГБ |
| b1 | 16 | 2354 | 209 | ~2.7 ГБ |

Сумма пиков ~12.1 ГБ из 16 ГБ контейнера. Кучи: мелкие 2048 МБ, крупные 8192 МБ.

**Сетка фазы C — 840 точек, а не 600.** Заказанные 600 (лок 8..30 × трейл 6..12 ×
стоп 20..99 × холд 14..27 сут) целиком внутри. Добавлены ДВА выключенных уровня —
**лок 0** и **трейл 100**. Зачем: угол «лок 0 / трейл 100 / стоп 99» = «просто досидеть
до конца холда», и он считается вне движка по двум свечам на идею. Без него контроли
пришлось бы гонять движком, то есть ещё трижды по 15 часов. Размер сетки почти
бесплатен, время держит число идей × горизонт.

**Контроли сверены с движком строка в строку.** `naive.py` (питон, две свечи на идею)
против движка на `may_2022 BTCUSDT`, угол лок 0 / стоп 99 / трейл 100: **все 40 строк
совпали до последнего знака**. Это не заявление, а `diff`.

## ГЛАВНОЕ: гейт раздвоился — на ленте проходит, на рецепте нет

> Итоговые числа и разбор — [session22/REPORT_PHASE_C.md](session22/REPORT_PHASE_C.md),
> письмо автору — [session22/MESSAGE_PETR_HOLD27.md](session22/MESSAGE_PETR_HOLD27.md),
> решение — DECISIONS №134. Таблицы ниже — черновая выжимка первой итерации; там, где
> они расходятся с отчётом, верен отчёт (плацебо по времени было пересчитано в
> сопоставимой форме, и вывод по нему изменился на обратный).

Коротко, всё остальное — в отчёте:

- **ось холда:** на сделку растёт до 27 суток (+0.329 → +0.722), НА СУТКИ удержания
  встаёт после 18 (0.0235 / **0.0339** / 0.0270 / 0.0268 / 0.0267), t везде около 2;
- **направление автора значимо лучше жребия** — точный перестановочный тест, p 0.004–0.018
  на всех холдах;
- **момент поста — лучший из доступных входов**: сдвиг вперёд (единственный торгуемый)
  роняет результат до нуля, p 0.02–0.13; сдвиг назад бьёт настоящий вход в 100 случаях
  из 100 — пост стоит в конце движения;
- **но эджа сверх «быть в лонге тогда же» нет**: парный лифт +0.088…+0.310, t ≤ +1.01;
- **отбор топ-K не отличим от жребия** (p 0.11–0.86), а худшие K значимо хуже жребия
  (p 0.002–0.045) → **чёрный список вместо белого**;
- **устойчивость:** отобранная стратегия бьёт и buy & hold, и оба плацебо в 12 ячейках
  из 90, на длинном крае 2 из 36;
- **календарный buy & hold** за окно оценки, равновзвешенно по 12 символам: **+18.7 %**.

## Что осталось доделать

1. Дождаться прогона (юнит `quant-phase-c`, метки `out_c/done_*.json`, лог
   `logs/pipeline_c.log`). Он даёт описательную половину ответа: растёт ли поверхность
   по холду дальше 14 суток и не уезжает ли оптимум по локу/трейлу/стопу.
2. `paired_rule.py` — парный бета-контроль на НАСТОЯЩЕМ правиле рецепта
   (лок 20 / трейл 8 / стопа нет), а не только в углу. Скрипт готов, ждёт `panel_c`:
   отбор берётся из панелей движка, а полная поминутная траектория поднимается только
   по сделкам отобранных авторов (сотни, а не 10 488) — поэтому это минуты, а не часы.
   Внутри сверка с движком: сумма «своего направления» обязана совпасть с его `trd`.
3. Сверить угол движка (лок 0 / стоп 99 / трейл 100) с `panel_nv/trdnv_real.tsv` уже
   на всём датасете, а не на одном месяце.
4. Отчёт `session22/REPORT_PHASE_C.md`, DECISIONS №134, письмо
   `session22/MESSAGE_PETR_HOLD27.md` — **написаны**, дописать в них раздел движка.

## Что где лежит

CT105, `/data/backtests/_agent/phaseC/`:
- склад свечей `union/<SYMBOL>/dump/...`;
- `check_minutes.py`, `fetch_minutes.py`, `missing_minutes.json`, `unavailable_minutes.json`;
- `gate.sh` + `run_gate.sh` (netCalls / пик RSS / секунды на идею), `gate1.out`, `gate2.out`;
- `grid_c.sh` (единственное место, где записана сетка), `mktasks_c.sh`, `worker_c.sh`,
  `pipeline_c.sh`, шарды `shard_*.tsv`, выход `out_c/`, панели `panel_c/`;
- контроли: `naive.py` → `panel_nv/` (в т.ч. поштучная выгрузка `trades_real.tsv`),
  `field.py` (ось холда на сыром поле), `verdict.py`, `robust.py`, `tstat.py`,
  `perm.py` (перестановка направления), `permsel.py` (рейтинг против жребия + чёрный
  список), `permshift.py` (сдвиг входа, знак аргументом), `paired_rule.py`;
- `zerocandle.py` — идеи, у которых пуст ПЕРВЫЙ чанк свечей (их 6: 3 у окон
  техобслуживания, 3 до листинга PUMP).

Копии всех скриптов в git: `session22/runner/phaseC/`.
