# Промт для новой сессии: майнинг Vibe-Trading (одобрено автором 14.07)

> Скопировать блок ниже в НОВУЮ сессию Claude Code (cwd = /home/s1dd1/dev/quant/paperhands).
> Контекст: автор на предложение майнить Vibe-Trading ответил «ОТЛИЧНАЯ ИДЕЯ» и дал
> канон интеграции новостей: worker пишет аудит в mongo с publishedAt, стратегия
> читает через getSignal(..., when) — виртуальное время. Сам mongo-worker — ОТДЕЛЬНАЯ
> будущая сессия; здесь — изучение Vibe и заготовки.

---

Читай paperhands/CLAUDE.md, agent/DECISIONS.md (№71, №73–79 — сверху файла),
agent/notes/news-dataset/README.md и example/scripts/news_dataset/news_classify.mjs.
Задача — выпотрошить форк HKUDS/Vibe-Trading (уже склонирован:
`/home/s1dd1/dev/quant/vibe-trading`, READ-ONLY, приложение НЕ запускать, ключи
не давать, docker не поднимать) по трём одобренным автором пунктам:

## 1. Промпты классификации новостей (ГЛАВНОЕ)

Их skills (`agent/src/skills/`: sentiment-analysis, event-driven,
social-media-intelligence; инструменты `agent/src/tools/stock_news_tool.py` и
соседние) — как они формулируют direction/уверенность/привязку к активу.
Выжать приёмы → предложить **v2-промпт** для нашего ollama-классификатора
(`{symbol, direction, confidence}`, классификатор видит ТОЛЬКО title+content —
никакого look-ahead, это инвариант). **news_classify.mjs НЕ редактировать**
(его крутит боевой крон 09:40!) — v2 оформить отдельным скриптом/файлом промпта
+ прогнать A/B на УЖЕ собранных новостях (news-raw.jsonl, ~45 шт, Ollama-бюджет
ок; Tavily НЕ трогать вообще): agreement с v1, изменение отсева, где v2 лучше
глазами. Переключение крона на v2 — решение владельца в главной сессии.

## 2. Fallback-цепочки источников по риску IP-бана

`agent/src/market_data.py` — как устроены цепочки и порядок «never-banned
first». Задокументировать паттерн и предложить (НЕ реализовывать), как он лёг
бы на наш ccxt/klines-путь (у нас бывают binance-таймауты). Это кандидат в
фидбек/идею автору.

## 3. Каталог данных: liquidation-heatmap / funding / stablecoin-flow / unlocks

Их skills и tools по этим темам: какие ИСТОЧНИКИ данных используют (бесплатные?
ключи? rate-limits?), что из этого могло бы стать доп. псевдо-каналами нашего
news-датасета под канон автора (publishedAt + getSignal(when)). Только каталог
с оценкой применимости — никакой интеграции.

## Ограничения

- ОЗУ: репо 57MB / 1147 py-файлов — точечные чтения, никаких массовых Read;
  выжимки писать на диск в `agent/notes/vibe-mining/` инкрементально.
- Код автора (paperhands src/**, backtest-ollama-crontab) не трогать; наши
  файлы — только `example/scripts/news_dataset/*v2*` и `agent/notes/vibe-mining/`.
- Ничего не коммитить без «комить» владельца.
- `agent/notes/author-feedback-3.md` НЕ редактировать — кандидатов в фидбек
  собирать списком в отчёте, главная сессия перенесёт.

## Деливерабл

`agent/notes/vibe-mining/report.md`: (1) вердикт по каждому из трёх пунктов —
что берём / что шум, с путями к их файлам; (2) v2-промпт + A/B-таблица
(agreement, отсев, 5 примеров расхождений глазами); (3) каталог источников
с оценкой; (4) кандидаты в фидбек автору; (5) заготовка требований к будущему
news→mongo worker (какие поля аудита писать, чтобы getSignal(when) было чем
кормить) — полстраницы, без кода.
